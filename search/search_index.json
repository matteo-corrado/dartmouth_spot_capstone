{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Dartmouth Spot Capstone","text":"<p>Voice-controlled Boston Dynamics Spot robot powered by on-device LLM inference on NVIDIA Jetson AGX Orin.</p>"},{"location":"#what-it-does","title":"What It Does","text":"<p>This system lets you control a Spot robot entirely by voice. Say \"Hey Spot, stand up\" and the robot stands. Ask \"what do you see?\" and it describes its surroundings using a vision-language model. Tell it \"go to the lab\" and it navigates there autonomously using a pre-recorded map.</p> <p>A local LLM (qwen2.5:7b via Ollama) acts as the robot's \"brain\" -- it interprets natural language, decides which actions to take, and generates spoken responses. Everything runs on-device; no cloud APIs required.</p>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>Wake word activation -- \"Hey Spot\" via sherpa-onnx keyword spotter (CPU, ~0ms latency)</li> <li>Natural language understanding -- LLM interprets free-form speech, not just fixed commands</li> <li>Command chaining -- \"stand up, walk forward, then turn left\" executes sequentially</li> <li>Visual scene description -- VLM analyzes camera feeds and describes what Spot sees</li> <li>Visual object navigation -- \"go to the red chair\" uses YOLO + camera servoing</li> <li>Autonomous map navigation -- \"go to the lab\" uses pre-recorded GraphNav maps</li> <li>Tour and patrol modes -- \"visit all locations\" or \"patrol the hallway\"</li> <li>Text-to-speech responses -- Spot talks back via Kokoro TTS</li> <li>Safety-first design -- \"stop\", \"freeze\", and \"e-stop\" bypass the LLM for zero-latency execution</li> <li>Web control panel -- mobile-friendly E-Stop and pipeline management UI</li> <li>Adaptive noise handling -- energy-gated VAD suppresses motor noise during movement</li> </ul>"},{"location":"#quick-start","title":"Quick Start","text":"<p>Terminal 1 -- E-Stop (must stay running):</p> <pre><code>source spot-env/bin/activate\npython scripts/estop_run.py\n</code></pre> <p>Terminal 2 -- Voice control:</p> <pre><code>source spot-env/bin/activate\npython scripts/run_voice_control.py\n</code></pre> <p>Terminal 3 -- Talk to Spot:</p> <p>Say \"Hey Spot, stand up\" into the microphone.</p> <p>See the Quick Start guide for a full walkthrough.</p>"},{"location":"#documentation","title":"Documentation","text":"Section Description Hardware Setup Jetson, Spot, microphone, network Software Setup Python env, Riva, Ollama, dependencies Model Downloads TTS, wake word, YOLO, LLM/VLM models Quick Start 5-minute getting-started walkthrough"},{"location":"#tech-stack","title":"Tech Stack","text":"Component Technology Runs On ASR (speech-to-text) NVIDIA Riva (Canary-Qwen-2.5B) GPU (Docker) LLM (language model) Ollama qwen2.5:7b GPU VLM (vision-language) Ollama qwen2.5vl:7b GPU TTS (text-to-speech) sherpa-onnx Kokoro (kokoro-en-v0_19) CPU Wake Word sherpa-onnx KWS (zipformer-gigaspeech-3.3M, int8) CPU Object Detection YOLO (ultralytics) -- YOLOv8n + YOLO-World CPU Robot SDK Boston Dynamics SDK 5.0.1.1 CPU Platform Jetson AGX Orin 64GB, JetPack 6.2.1 --"},{"location":"#project-structure","title":"Project Structure","text":"<pre><code>dartmouth_spot_capstone/\n    scripts/\n        run_voice_control.py    # Main entry point -- starts everything\n        estop_run.py            # E-Stop keepalive (run separately)\n        web_panel.py            # Web UI for E-Stop + pipeline control\n        setup_riva.sh           # NVIDIA Riva ASR Docker setup\n        setup_kokoro.py         # Download Kokoro TTS model\n        setup_kws.py            # Download wake word model\n        setup_map.py            # Upload GraphNav map to Spot\n        download_map_from_spot.py\n        map_waypoints.py        # List/label map waypoints\n    src/\n        config.py               # Environment variables (.env)\n        session.py              # Spot SDK connection manager\n        estop.py                # E-Stop utilities\n        graph_nav_utils.py      # GraphNav map upload/localization\n        location_manager.py     # Named location persistence\n        voice_control/\n            client_mic.py       # Microphone listener + VAD + main loop\n            server.py           # gRPC ASR bridge (Riva proxy)\n            llm_brain.py        # LLM/VLM inference via Ollama\n            spot_dispatch.py    # Intent-to-robot-action executor\n            intent.py           # Regex intent parser (legacy fallback)\n            spot_tts.py         # Kokoro TTS playback\n            wake_word.py        # sherpa-onnx keyword spotter\n            audio_feedback.py   # Beep tones for status feedback\n            visual_nav.py       # YOLO object seeking + person following\n    models/                     # Downloaded by setup scripts (gitignored)\n        tts/kokoro-en-v0_19/\n        kws/\n    maps/                       # GraphNav maps (gitignored)\n    locations.json              # Saved named locations\n    requirements.txt\n    mkdocs.yml                  # This documentation site config\n    .env                        # Robot credentials (gitignored)\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/","title":"Post-Flash Setup: JetPack 6.2.1 \u2192 Full Voice Control","text":"<p>Complete step-by-step guide from flashing the Jetson AGX Orin to having full voice-controlled Spot with navigation, TTS, and arm capabilities.</p>"},{"location":"POST_FLASH_MIGRATION/#overview","title":"Overview","text":"Component Technology Runs on ASR (speech-to-text) NVIDIA Riva + Canary-Qwen-2.5B GPU (Docker) LLM (intent + chat) Ollama + qwen2.5:7b GPU VLM (vision) Ollama + qwen2.5-vl:7b GPU TTS (text-to-speech) Kokoro ONNX (82M params) CPU Voice pipeline VAD \u2192 ASR \u2192 LLM Brain \u2192 Dispatch \u2192 TTS Jetson Door opening Remote Mission Service (arm/gripper) Jetson + Spot arm"},{"location":"POST_FLASH_MIGRATION/#why-flash-first","title":"Why flash first?","text":"Blocker on JetPack 5.1.2 Fixed by JetPack 6.2.1 CUDA 11.4 (Riva needs 12) CUDA 12.6 glibc 2.31 (wheels need 2.35) glibc 2.35 Python 3.8 (EOL) Python 3.10"},{"location":"POST_FLASH_MIGRATION/#phase-1-flash-the-jetson","title":"Phase 1: Flash the Jetson","text":""},{"location":"POST_FLASH_MIGRATION/#11-pre-flash-backup","title":"1.1 Pre-flash backup","text":"<p>Only one file to save (everything else is on GitHub):</p> <pre><code>cat /home/spotdog/dartmouth_spot_capstone/.env\n# Save the output somewhere safe \u2014 contains robot credentials\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#12-flash-jetpack-621","title":"1.2 Flash JetPack 6.2.1","text":"<ol> <li>Install NVIDIA SDK Manager on a Windows PC</li> <li>SDK Manager auto-configures WSL2, Ubuntu, and USBIPD-Win</li> <li>Install the APX driver when prompted</li> <li>Connect Jetson USB-C port (next to 40-pin header) to Windows PC</li> <li>Put Jetson in Force Recovery Mode (hold REC button + press power)</li> <li>Flash JetPack 6.2.1 via SDK Manager</li> <li>Choose Pre-Config for OEM setup (set username: <code>spotdog</code>, password)</li> <li>This enables headless boot \u2014 no monitor needed</li> </ol>"},{"location":"POST_FLASH_MIGRATION/#13-verify-flash","title":"1.3 Verify flash","text":"<pre><code>nvcc --version        # Should show CUDA 12.x\npython3 --version     # Should show 3.10.x\nlsb_release -a        # Should show Ubuntu 22.04\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#phase-2-clone-and-set-up-the-project","title":"Phase 2: Clone and Set Up the Project","text":""},{"location":"POST_FLASH_MIGRATION/#21-clone-the-repo","title":"2.1 Clone the repo","text":"<pre><code>cd /home/spotdog\ngit clone https://github.com/matteo-corrado/dartmouth_spot_capstone.git\ncd dartmouth_spot_capstone\ngit checkout feature/llm-brain\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#22-restore-credentials","title":"2.2 Restore credentials","text":"<pre><code>nano .env\n</code></pre> <p>Paste saved credentials (must contain these):</p> <pre><code>BOSDYN_CLIENT_USERNAME=&lt;your-username&gt;\nBOSDYN_CLIENT_PASSWORD=&lt;your-password&gt;\nBOSDYN_ROBOT_IP=&lt;your-robot-ip&gt;\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#23-create-python-virtualenv","title":"2.3 Create Python virtualenv","text":"<pre><code>python3 -m venv spot-env\nsource spot-env/bin/activate\npip install --upgrade pip\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#24-install-pytorch-jetpack-6-nvidia-wheel","title":"2.4 Install PyTorch (JetPack 6 NVIDIA wheel)","text":"<pre><code>pip install numpy\npip install torch torchvision torchaudio --index-url https://developer.download.nvidia.com/compute/redist/jp/v62\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#25-install-project-dependencies","title":"2.5 Install project dependencies","text":"<pre><code>pip install -r requirements.txt\n</code></pre> <p>This installs: - <code>bosdyn-client==5.0.1.1</code> (BD SDK, matched to robot firmware) - <code>nvidia-riva-client&gt;=2.17.0</code> (Riva ASR client) - <code>kokoro-onnx&gt;=0.5.0</code> (Kokoro TTS) - <code>sounddevice</code>, <code>webrtcvad</code>, <code>grpcio</code>, etc.</p>"},{"location":"POST_FLASH_MIGRATION/#26-verify-python-packages","title":"2.6 Verify Python packages","text":"<pre><code>python3 -c \"import torch; print(f'PyTorch {torch.__version__}, CUDA: {torch.cuda.is_available()}')\"\npython3 -c \"import bosdyn.client; print('BD SDK OK')\"\npython3 -c \"import riva.client; print('Riva client OK')\"\npython3 -c \"import kokoro_onnx; print('Kokoro OK')\"\npython3 -c \"import sounddevice; print('sounddevice OK')\"\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#phase-3-set-up-nvidia-riva-asr","title":"Phase 3: Set Up NVIDIA Riva ASR","text":"<p>Riva runs as a Docker container serving Canary-Qwen-2.5B on port 50051.</p>"},{"location":"POST_FLASH_MIGRATION/#31-install-docker-nvidia-container-runtime","title":"3.1 Install Docker + NVIDIA container runtime","text":"<pre><code>sudo apt-get update\nsudo apt-get install -y docker.io nvidia-container-toolkit\nsudo systemctl restart docker\nsudo usermod -aG docker $USER\n# Log out and back in for group change to take effect\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#32-run-riva-setup","title":"3.2 Run Riva setup","text":"<pre><code>chmod +x scripts/setup_riva.sh\n./scripts/setup_riva.sh\n</code></pre> <p>This downloads the Riva quickstart and configures for ASR-only with Canary.</p>"},{"location":"POST_FLASH_MIGRATION/#33-initialize-models-15-30-min-first-time-only","title":"3.3 Initialize models (15-30 min, first time only)","text":"<pre><code>./scripts/setup_riva.sh init\n</code></pre> <p>This downloads the Canary-Qwen-2.5B model and builds TensorRT engines.</p>"},{"location":"POST_FLASH_MIGRATION/#34-start-riva-server","title":"3.4 Start Riva server","text":"<pre><code>./scripts/setup_riva.sh start\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#35-test-riva","title":"3.5 Test Riva","text":"<pre><code>./scripts/setup_riva.sh test\n</code></pre> <p>Should print: <code>[TEST] Connection OK \u2014 Riva is ready for Spot voice control</code></p> <p>Note: Riva server must be running before starting voice control. Add <code>./scripts/setup_riva.sh start</code> to your boot sequence or run it manually each time.</p>"},{"location":"POST_FLASH_MIGRATION/#phase-4-set-up-ollama-llm-vlm","title":"Phase 4: Set Up Ollama (LLM + VLM)","text":""},{"location":"POST_FLASH_MIGRATION/#41-install-ollama","title":"4.1 Install Ollama","text":"<pre><code>curl -fsSL https://ollama.com/install.sh | sh\nsudo systemctl enable ollama\nsudo systemctl start ollama\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#42-pull-models","title":"4.2 Pull models","text":"<pre><code>ollama pull qwen2.5:7b       # LLM brain (intent parsing + chat)\nollama pull qwen2.5-vl:7b    # VLM (visual descriptions from cameras)\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#43-verify","title":"4.3 Verify","text":"<pre><code>ollama list\n# Should show qwen2.5:7b and qwen2.5-vl:7b\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#phase-5-set-up-kokoro-tts","title":"Phase 5: Set Up Kokoro TTS","text":""},{"location":"POST_FLASH_MIGRATION/#51-download-model","title":"5.1 Download model","text":"<pre><code>python scripts/setup_kokoro.py\n</code></pre> <p>By default downloads the int8 model (~88MB) to <code>models/tts/</code>. For best quality:</p> <pre><code>python scripts/setup_kokoro.py --full     # 310MB, best quality\npython scripts/setup_kokoro.py --fp16     # 169MB, good balance\npython scripts/setup_kokoro.py --int8     # 88MB, smallest (default)\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#52-test-tts","title":"5.2 Test TTS","text":"<pre><code>cd src/voice_control\npython spot_tts.py\n</code></pre> <p>Should play \"Hello! I am Spot...\" through speakers. If no audio device is available, it will print <code>[TTS] Not available</code> \u2014 that's fine for headless setup, it'll work when a speaker is connected.</p>"},{"location":"POST_FLASH_MIGRATION/#phase-6-set-up-the-microphone","title":"Phase 6: Set Up the Microphone","text":""},{"location":"POST_FLASH_MIGRATION/#61-respeaker-xvf3800-recommended","title":"6.1 ReSpeaker XVF3800 (recommended)","text":"<pre><code># Clone config tool (optional, for mic tuning)\ncd /home/spotdog\ngit clone https://github.com/respeaker/reSpeaker_XVF3800_USB_4MIC_ARRAY.git\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#62-find-your-audio-device-index","title":"6.2 Find your audio device index","text":"<pre><code>python3 -c \"import sounddevice; print(sounddevice.query_devices())\"\n</code></pre> <p>Look for the XVF3800 device. Note its index number.</p>"},{"location":"POST_FLASH_MIGRATION/#63-configure-device-in-client_micpy-if-needed","title":"6.3 Configure device in client_mic.py (if needed)","text":"<p>The default device is hardcoded in <code>src/voice_control/client_mic.py</code>. If your device index differs from <code>24</code>, update the <code>INPUT_DEVICE</code> constant.</p>"},{"location":"POST_FLASH_MIGRATION/#phase-7-connect-to-spot-and-upload-map","title":"Phase 7: Connect to Spot and Upload Map","text":""},{"location":"POST_FLASH_MIGRATION/#71-connect-jetson-to-spots-network","title":"7.1 Connect Jetson to Spot's network","text":"<p>Connect the Jetson to Spot's WiFi or Ethernet. Default robot IP: <code>192.168.80.3</code>.</p> <pre><code>ping 192.168.80.3\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#72-start-e-stop-required-before-any-robot-commands","title":"7.2 Start E-Stop (required before any robot commands)","text":"<p>In a dedicated terminal that stays open:</p> <pre><code>cd /home/spotdog/dartmouth_spot_capstone\nsource spot-env/bin/activate\npython scripts/estop_run.py\n</code></pre> <p>This must stay running the entire time you use Spot. It provides the software emergency stop. If this process dies, the robot will e-stop.</p>"},{"location":"POST_FLASH_MIGRATION/#73-record-a-map-on-the-tablet-if-no-map-exists","title":"7.3 Record a map on the tablet (if no map exists)","text":"<p>On the Spot tablet: 1. Drive Spot to the starting position 2. Open Autowalk &gt; Record 3. Walk Spot through all areas you want in the map 4. Place fiducials (AprilTags) at key locations for localization 5. End recording and save</p>"},{"location":"POST_FLASH_MIGRATION/#74-download-map-from-spot","title":"7.4 Download map from Spot","text":"<pre><code>python scripts/download_map_from_spot.py\n</code></pre> <p>This saves the map to <code>maps/downloaded_map/</code> and auto-extracts waypoint names to <code>locations.json</code>.</p>"},{"location":"POST_FLASH_MIGRATION/#75-name-your-locations","title":"7.5 Name your locations","text":"<pre><code>python scripts/map_waypoints.py\n</code></pre> <p>This lists all waypoints and lets you assign human-readable names (e.g., <code>kitchen</code>, <code>lobby</code>, <code>lab_door</code>). Names are saved to <code>locations.json</code>.</p>"},{"location":"POST_FLASH_MIGRATION/#76-upload-map-to-spots-graphnav","title":"7.6 Upload map to Spot's GraphNav","text":"<pre><code>python scripts/setup_map.py --map-path maps/lab_map2\n</code></pre> <p>For waypoint-based localization (robot must be at a known waypoint):</p> <pre><code>python scripts/setup_map.py --map-path maps/lab_map2 --waypoint-init\n</code></pre> <p>For fiducial-based localization (robot must see an AprilTag from the map):</p> <pre><code>python scripts/setup_map.py --map-path maps/lab_map2\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#phase-8-run-voice-control","title":"Phase 8: Run Voice Control","text":""},{"location":"POST_FLASH_MIGRATION/#81-pre-flight-checklist","title":"8.1 Pre-flight checklist","text":"<p>Before starting voice control, make sure:</p> <ul> <li>[ ] E-Stop is running (<code>python scripts/estop_run.py</code> in a separate terminal)</li> <li>[ ] Riva server is running (<code>./scripts/setup_riva.sh start</code>)</li> <li>[ ] Ollama is running (<code>sudo systemctl start ollama</code>)</li> <li>[ ] Map is uploaded and robot is localized (Phase 7)</li> <li>[ ] Microphone and speaker are connected</li> </ul>"},{"location":"POST_FLASH_MIGRATION/#82-start-voice-control","title":"8.2 Start voice control","text":"<pre><code>python scripts/run_voice_control.py\n</code></pre> <p>This automatically: 1. Checks Riva is reachable on port 50051 2. Starts the ASR bridge server (port 50055) 3. Starts the mic client with VAD, LLM brain, and TTS</p>"},{"location":"POST_FLASH_MIGRATION/#83-test-voice-commands","title":"8.3 Test voice commands","text":"<p>Say \"Hey Spot\" to wake, then try these commands:</p> <p>Basic movement: - \"Stand up\" \u2014 Spot stands - \"Sit down\" \u2014 Spot sits - \"Walk forward 2 meters\" \u2014 Spot walks forward - \"Turn left 90 degrees\" \u2014 Spot turns - \"Crouch\" \u2014 Spot lowers body</p> <p>Single-location navigation: - \"Go to the kitchen\" \u2014 navigates to saved location \"kitchen\" - \"Save this location as front desk\" \u2014 saves current position</p> <p>Multi-location navigation: - \"Loop the map\" \u2014 visits all saved locations once in recording order - \"Go to kitchen then lab then lobby\" \u2014 visits specific locations in sequence - \"Patrol the map\" \u2014 continuously loops all locations until stopped - \"Come back\" / \"Go home\" \u2014 returns to where Spot was before navigating</p> <p>Vision: - \"What do you see?\" \u2014 captures front camera, describes via VLM - \"What's behind you?\" \u2014 captures back camera</p> <p>Safety (bypass LLM, instant execution): - \"Stop\" \u2014 stops all movement, cancels any navigation - \"Freeze\" \u2014 holds position - \"Emergency stop\" \u2014 immediate stop</p> <p>Status: - \"Battery status\" \u2014 reports battery level - \"List locations\" \u2014 lists all saved locations</p>"},{"location":"POST_FLASH_MIGRATION/#84-manual-start-two-terminals","title":"8.4 Manual start (two terminals)","text":"<p>If <code>run_voice_control.py</code> doesn't work, start components separately:</p> <p>Terminal 1 \u2014 ASR Server: <pre><code>cd src/voice_control\npython server.py\n</code></pre></p> <p>Terminal 2 \u2014 Voice Client: <pre><code>cd src/voice_control\npython client_mic.py\n</code></pre></p>"},{"location":"POST_FLASH_MIGRATION/#phase-9-door-opening-with-arm-autowalk","title":"Phase 9: Door Opening with Arm (AutoWalk)","text":"<p>The door-opening service uses Spot's arm to press buttons or grasp handles during AutoWalk missions. This runs as a Remote Mission Service that Spot calls at configured waypoints.</p>"},{"location":"POST_FLASH_MIGRATION/#91-get-jetsons-ip-as-seen-by-spot","title":"9.1 Get Jetson's IP as seen by Spot","text":"<pre><code>source spot-env/bin/activate\nHOST_IP=$(python3 -m bosdyn.client 192.168.80.3 self-ip)\necho \"Jetson IP: $HOST_IP\"\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#92-start-the-door-service","title":"9.2 Start the door service","text":"<p>In a separate terminal (E-Stop must already be running):</p> <pre><code>python scripts/run_door_service.py --host-ip $HOST_IP --port 50052\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#93-configure-on-the-tablet","title":"9.3 Configure on the tablet","text":"<ol> <li>Open Autowalk &gt; Record a new mission</li> <li>Walk Spot to a door</li> <li>Tap \"+\" &gt; \"Create New Action\" &gt; \"Remote GRPC\"</li> <li>Select \"door-opening-service\" from the list</li> <li>Configure parameters:</li> <li><code>door_type</code>: <code>button</code> (handicap button) or <code>handle</code> (door handle)</li> <li><code>door_direction</code>: <code>push</code> or <code>pull</code></li> <li><code>handle_height_m</code>: height of handle in meters (default 1.0)</li> <li><code>button_height_m</code>: height of button in meters (default 1.0)</li> <li><code>button_offset_y_m</code>: lateral offset of button (default -0.3)</li> <li>Continue recording the rest of the mission</li> <li>On playback, Spot will call the door service at the configured waypoint</li> </ol>"},{"location":"POST_FLASH_MIGRATION/#94-supported-door-types","title":"9.4 Supported door types","text":"<p>Push-button doors (recommended starting point):</p> <p>Spot approaches &gt; unstows arm &gt; presses button &gt; waits for door &gt; walks through &gt; stows arm</p> <p>Handle-based doors (more complex):</p> <p>Spot approaches &gt; unstows arm &gt; opens gripper &gt; grasps handle &gt; pushes/pulls &gt; holds open &gt; walks through &gt; releases &gt; stows arm</p>"},{"location":"POST_FLASH_MIGRATION/#phase-10-test-components-individually","title":"Phase 10: Test Components Individually","text":""},{"location":"POST_FLASH_MIGRATION/#test-llm-brain-no-robot-needed","title":"Test LLM Brain (no robot needed)","text":"<pre><code>python src/voice_control/llm_brain.py\n</code></pre> <p>Type commands and verify JSON output: - \"Loop the map\" should return <code>tour</code> action with <code>{\"locations\": \"all\"}</code> - \"Go to kitchen then lab\" should return <code>tour</code> with <code>{\"locations\": [\"kitchen\", \"lab\"]}</code> - \"Patrol the map\" should return <code>patrol</code> action - \"Come back\" should return <code>come_back</code> action - \"Stand up\" should return <code>stand</code> action (no regression)</p>"},{"location":"POST_FLASH_MIGRATION/#test-tts","title":"Test TTS","text":"<pre><code>python src/voice_control/spot_tts.py af_heart \"Testing Kokoro TTS on Spot\"\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#test-riva-asr","title":"Test Riva ASR","text":"<pre><code>./scripts/setup_riva.sh test\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#test-door-service-standalone-no-autowalk","title":"Test door service (standalone, no AutoWalk)","text":"<pre><code>python scripts/test_door_standalone.py\n</code></pre>"},{"location":"POST_FLASH_MIGRATION/#troubleshooting","title":"Troubleshooting","text":"Problem Fix <code>Riva server NOT detected on port 50051</code> Run <code>./scripts/setup_riva.sh start</code> <code>Cannot connect to Ollama</code> Run <code>sudo systemctl start ollama</code> <code>Kokoro model files not found</code> Run <code>python scripts/setup_kokoro.py</code> <code>E-Stop error</code> / robot won't move Ensure <code>python scripts/estop_run.py</code> is running <code>Not localized</code> Run <code>python scripts/setup_map.py --waypoint-init</code> <code>Location 'X' not found</code> Run <code>python scripts/map_waypoints.py</code> to name waypoints <code>No waypoints in map</code> Upload a map: <code>python scripts/setup_map.py --map-path ...</code> ASR gives empty results Check mic is connected, device index is correct, Riva running \"Stop\" doesn't cancel navigation Fixed in latest code, pull and redeploy <code>ModuleNotFoundError</code> Activate venv: <code>source spot-env/bin/activate</code> Robot credentials error Check <code>.env</code> has correct username and password Door service not visible on tablet Check <code>--host-ip</code> is correct (use <code>self-ip</code> command)"},{"location":"POST_FLASH_MIGRATION/#port-reference","title":"Port Reference","text":"Port Service 50051 NVIDIA Riva ASR server (Docker) 50052 Door opening Remote Mission Service 50055 ASR bridge server (custom proto to Riva) 11434 Ollama LLM/VLM API"},{"location":"POST_FLASH_MIGRATION/#boot-sequence-quick-reference","title":"Boot Sequence (Quick Reference)","text":"<p>After rebooting the Jetson, run these in order:</p> <pre><code>cd /home/spotdog/dartmouth_spot_capstone\nsource spot-env/bin/activate\n\n# 1. Start infrastructure\n./scripts/setup_riva.sh start          # Riva ASR (Docker)\nsudo systemctl start ollama            # Ollama LLM\n\n# 2. Start E-Stop (separate terminal, keep open)\npython scripts/estop_run.py\n\n# 3. Upload map + localize (if not already loaded)\npython scripts/setup_map.py --map-path maps/lab_map2 --waypoint-init\n\n# 4. Start voice control\npython scripts/run_voice_control.py\n\n# 5. (Optional) Start door service (separate terminal)\nHOST_IP=$(python3 -m bosdyn.client 192.168.80.3 self-ip)\npython scripts/run_door_service.py --host-ip $HOST_IP --port 50052\n</code></pre>"},{"location":"architecture/overview/","title":"System Architecture Overview","text":"<p>Voice-controlled Boston Dynamics Spot robot running on Jetson AGX Orin 64GB. The system takes spoken commands, processes them through an LLM, and dispatches actions to the robot via the Boston Dynamics SDK.</p>"},{"location":"architecture/overview/#high-level-architecture","title":"High-Level Architecture","text":"<pre><code>graph LR\n    MIC[Microphone&lt;br/&gt;XVF3800] --&gt; VAD[WebRTC VAD&lt;br/&gt;+ Energy Gate]\n    VAD --&gt; KWS[Wake Word&lt;br/&gt;sherpa-onnx KWS]\n    KWS --&gt; ASR[ASR Bridge&lt;br/&gt;server.py :50055]\n    ASR --&gt; RIVA[Riva ASR&lt;br/&gt;Canary-Qwen-2.5B&lt;br/&gt;:50051]\n    RIVA --&gt; ASR\n    ASR --&gt; BRAIN[LLM Brain&lt;br/&gt;qwen2.5:7b&lt;br/&gt;Ollama :11434]\n    BRAIN --&gt; DISPATCH[Spot Dispatcher&lt;br/&gt;spot_dispatch.py]\n    DISPATCH --&gt; SPOT[Spot Robot&lt;br/&gt;192.168.80.3]\n\n    BRAIN --&gt; TTS[TTS&lt;br/&gt;Kokoro via&lt;br/&gt;sherpa-onnx]\n    DISPATCH --&gt; VLM[VLM&lt;br/&gt;qwen2.5vl:7b&lt;br/&gt;Ollama]\n    DISPATCH --&gt; YOLO[YOLO&lt;br/&gt;CPU inference]\n    DISPATCH --&gt; GNAV[GraphNav&lt;br/&gt;BD SDK]\n    DISPATCH --&gt; DOOR[DoorService&lt;br/&gt;AutoPush]\n\n    style MIC fill:#2196F3,color:#fff\n    style SPOT fill:#4CAF50,color:#fff\n    style BRAIN fill:#FF9800,color:#fff\n    style RIVA fill:#9C27B0,color:#fff</code></pre> <p>Data flow summary: Microphone audio streams through VAD and energy gating, passes the wake word detector, gets transcribed by Riva ASR via a gRPC bridge, then the transcript goes to the LLM Brain which returns structured JSON with actions and a spoken response. The dispatcher executes actions on Spot. Vision queries additionally invoke the VLM or YOLO models.</p>"},{"location":"architecture/overview/#design-decisions","title":"Design Decisions","text":""},{"location":"architecture/overview/#1-modular-asr-bridge","title":"1. Modular ASR Bridge","text":"<p><code>server.py</code> acts as a gRPC bridge between the voice client and NVIDIA Riva. The client speaks a custom <code>asr.proto</code> protocol on port 50055; the server translates to Riva's native API on port 50051. This decouples the ASR engine from the rest of the pipeline -- swapping Riva for Whisper, Faster-Whisper, or any other engine only requires changing <code>server.py</code>, not the client.</p> <p>The server also handles post-processing: hallucination filtering, repetitive phrase detection, and minimum duration rejection.</p>"},{"location":"architecture/overview/#2-structured-json-llm-output","title":"2. Structured JSON LLM Output","text":"<p>The LLM Brain uses Ollama's <code>format=\"json\"</code> parameter, which enforces valid JSON output at the grammar level. The model fills in a schema:</p> <pre><code>{\"actions\": [{\"action\": \"walk\", \"params\": {\"direction\": \"forward\", \"distance\": 2}}],\n \"response\": \"Walking forward 2 meters!\"}\n</code></pre> <p>This avoids unreliable tool-calling protocols. Small 7B models are far more reliable at filling JSON fields than performing multi-step function calling. The entire action catalog lives in the system prompt.</p>"},{"location":"architecture/overview/#3-persistent-spot-session","title":"3. Persistent Spot Session","text":"<p><code>src/session.py</code> provides a <code>spot_session()</code> context manager that creates a single long-lived connection to the robot. Authentication, lease acquisition, time sync, and power-on happen once at startup. All commands share this session, eliminating per-command auth overhead (~2s saved per command).</p> <p>The session uses <code>LeaseKeepAlive</code> to maintain the lease and automatically clears stale keepalive policies from previous sessions.</p>"},{"location":"architecture/overview/#4-navigation-in-background-threads","title":"4. Navigation in Background Threads","text":"<p>GraphNav navigation, visual object approach (<code>go_to_object</code>), and person following (<code>follow_me</code>) all run in background threads. This keeps the voice pipeline responsive -- the user can issue new commands (including safety commands like \"stop\") while the robot is navigating.</p> <p>Command chaining (e.g., \"go to kitchen then sit down\") uses <code>_wait_for_nav_complete()</code> to synchronize between actions.</p>"},{"location":"architecture/overview/#5-cpu-only-vision","title":"5. CPU-Only Vision","text":"<p>YOLO-World (open-vocabulary detection) and YOLOv8n (person detection) both run with <code>device=\"cpu\"</code>. This keeps the GPU/VRAM entirely free for the LLM and VLM models. On Jetson AGX Orin, the CPU cores are fast enough for detection at ~2 Hz, which is sufficient for visual navigation and following.</p>"},{"location":"architecture/overview/#6-adaptive-noise-floor","title":"6. Adaptive Noise Floor","text":"<p>The system calibrates ambient noise at startup (2 seconds of silence), then continuously adapts using EMA smoothing (<code>alpha=0.03</code>). During navigation, the energy threshold is boosted by 5x to suppress motor noise. During follow mode, the multiplier is only 2x since the user is nearby and speaking.</p> <p>This prevents false VAD triggers from robot motor noise while still catching speech at normal conversational distance.</p>"},{"location":"architecture/overview/#component-table","title":"Component Table","text":"Component File Purpose Voice Client <code>src/voice_control/client_mic.py</code> Main audio loop: VAD, speech detection, ASR dispatch, LLM orchestration ASR Bridge Server <code>src/voice_control/server.py</code> gRPC bridge to Riva ASR with hallucination filtering LLM Brain <code>src/voice_control/llm_brain.py</code> Ollama structured JSON for intent parsing + conversational response Spot Dispatcher <code>src/voice_control/spot_dispatch.py</code> Executes robot commands via BD SDK (movement, nav, vision, door) Wake Word Detector <code>src/voice_control/wake_word.py</code> sherpa-onnx keyword spotter for \"Hey Spot\" TTS <code>src/voice_control/spot_tts.py</code> sherpa-onnx Kokoro TTS (non-blocking, speaker af_sarah) Audio Feedback <code>src/voice_control/audio_feedback.py</code> Short beep/chime tones for user feedback Visual Nav <code>src/voice_control/visual_nav.py</code> YOLO-based object approach and person following Intent Parser <code>src/voice_control/intent.py</code> Regex fallback intent parser (legacy, used with <code>--no-brain</code>) Session Manager <code>src/session.py</code> Persistent Spot connection context manager Config <code>src/config.py</code> Environment variables (robot IP, credentials) Location Manager <code>src/location_manager.py</code> Save/load named locations to <code>locations.json</code> GraphNav Utils <code>src/graph_nav_utils.py</code> Map upload, localization, waypoint listing Proto Definitions <code>src/voice_control/asr_pb2.py</code> Generated protobuf for ASR bridge communication Entry Point <code>scripts/run_voice_control.py</code> Orchestrates service startup and launches client E-Stop <code>scripts/estop_run.py</code> E-Stop keepalive (must run in separate terminal) Web Panel <code>scripts/web_panel.py</code> Mobile-friendly E-Stop + pipeline control web UI"},{"location":"architecture/sdk-primer/","title":"Boston Dynamics SDK Primer","text":"<p>This page explains the core concepts of the Boston Dynamics (BD) Python SDK that underpin the entire voice control system. If you have never worked with the Spot SDK before, read this before diving into the module reference.</p>"},{"location":"architecture/sdk-primer/#sdk-package-structure","title":"SDK Package Structure","text":"<p>The project uses BD SDK v5.0.1.1. The main packages are:</p> <pre><code>bosdyn-client          # Core SDK \u2014 robot connection, commands, leases\nbosdyn-mission         # Mission/Autowalk API (used by door service)\nbosdyn-choreography-client  # Choreography API (available but not used)\n</code></pre> <p>All SDK code starts with <code>from bosdyn.client import ...</code>.</p>"},{"location":"architecture/sdk-primer/#connecting-to-spot","title":"Connecting to Spot","text":"<p>Every interaction with Spot follows the same pattern:</p> <pre><code>from bosdyn.client import create_standard_sdk\n\n# 1. Create an SDK instance (one per application)\nsdk = create_standard_sdk(\"my_app_name\")\n\n# 2. Create a robot object (does not connect yet)\nrobot = sdk.create_robot(\"192.168.80.3\")\n\n# 3. Authenticate (uses credentials from the admin console)\nrobot.authenticate(\"username\", \"password\")\n\n# 4. Sync clocks (required before any commands)\nrobot.time_sync.wait_for_sync()\n</code></pre> <p>The <code>robot</code> object is your handle to the physical robot. From it, you create service clients to interact with specific subsystems.</p>"},{"location":"architecture/sdk-primer/#service-clients","title":"Service Clients","text":"<p>Each Spot subsystem exposes a gRPC service. You get a client for each one:</p> <pre><code>from bosdyn.client.robot_command import RobotCommandClient\nfrom bosdyn.client.robot_state import RobotStateClient\nfrom bosdyn.client.lease import LeaseClient\nfrom bosdyn.client.power import PowerClient\nfrom bosdyn.client.image import ImageClient\nfrom bosdyn.client.graph_nav import GraphNavClient\n\ncmd_client   = robot.ensure_client(RobotCommandClient.default_service_name)\nstate_client = robot.ensure_client(RobotStateClient.default_service_name)\nlease_client = robot.ensure_client(LeaseClient.default_service_name)\n# ... etc\n</code></pre> <p><code>ensure_client()</code> creates the client once and caches it \u2014 safe to call multiple times.</p>"},{"location":"architecture/sdk-primer/#key-clients-used-in-this-project","title":"Key Clients Used in This Project","text":"Client Purpose Used in <code>RobotCommandClient</code> Send movement commands (walk, turn, stand, sit) <code>spot_dispatch.py</code> <code>RobotStateClient</code> Read battery, pose, motor state, faults <code>spot_dispatch.py</code> <code>LeaseClient</code> Acquire control of the robot <code>session.py</code> <code>PowerClient</code> Power motors on/off <code>session.py</code> <code>ImageClient</code> Capture camera images <code>spot_dispatch.py</code> <code>GraphNavClient</code> Autonomous navigation to waypoints <code>spot_dispatch.py</code> <code>EstopClient</code> Software emergency stop <code>estop_run.py</code>, <code>web_panel.py</code>"},{"location":"architecture/sdk-primer/#leases","title":"Leases","text":"<p>Only one client can control Spot at a time. Leases enforce this.</p> <pre><code>lease_client = robot.ensure_client(LeaseClient.default_service_name)\n\n# Acquire the lease (will fail if another client holds it)\nlease = lease_client.acquire()\n\n# Or forcefully take it (overrides the tablet or other clients)\nlease = lease_client.take()\n</code></pre> <p>The project uses <code>lease_client.take()</code> to always override any existing claim (e.g., from the tablet). This is deliberate \u2014 the voice pipeline should always be able to take control.</p>"},{"location":"architecture/sdk-primer/#lease-keepalive","title":"Lease Keepalive","text":"<p>Leases expire if not refreshed. <code>LeaseKeepAlive</code> handles this automatically:</p> <pre><code>from bosdyn.client.lease import LeaseKeepAlive\n\nkeepalive = LeaseKeepAlive(lease_client, must_acquire=False, return_at_exit=True)\n# Refreshes the lease in the background\n# return_at_exit=True releases the lease when the keepalive is shut down\n</code></pre> <p>Lease contention</p> <p>If someone picks up the Spot tablet and takes control, the tablet acquires the lease and the Jetson loses it. The voice pipeline will re-take the lease on the next command (since <code>spot_dispatch.py</code> maintains a persistent session), but any in-progress navigation will be interrupted.</p>"},{"location":"architecture/sdk-primer/#time-sync","title":"Time Sync","text":"<p>Spot requires all command timestamps to be in the robot's clock. The SDK handles this transparently, but you must sync clocks before sending commands:</p> <pre><code>robot.time_sync.wait_for_sync()\n</code></pre> <p>This is done once during session setup. If time sync fails (e.g., network latency spike), you'll see <code>TimeSyncError</code> \u2014 just retry.</p>"},{"location":"architecture/sdk-primer/#e-stop-emergency-stop","title":"E-Stop (Emergency Stop)","text":"<p>Spot will not move unless at least one E-Stop endpoint is active. This is a safety requirement \u2014 if all E-Stop endpoints disconnect, the robot stops immediately.</p> <pre><code>from bosdyn.client.estop import EstopClient, EstopEndpoint, EstopKeepAlive\n\nestop_client = robot.ensure_client(EstopClient.default_service_name)\nendpoint = EstopEndpoint(estop_client, name=\"my_estop\", estop_timeout=3.0)\nendpoint.force_simple_setup()\n\n# Start keepalive (sends heartbeats every ~1s)\nkeepalive = EstopKeepAlive(endpoint)\nkeepalive.allow()  # Tell the robot it's OK to move\n</code></pre> <p>If the keepalive stops sending heartbeats (process crash, network loss), the robot E-Stops within <code>estop_timeout</code> seconds (3s in this project).</p> <p>In this project, E-Stop is managed by either:</p> <ul> <li><code>scripts/estop_run.py</code> \u2014 terminal-based, Ctrl+C to stop</li> <li><code>scripts/web_panel.py</code> \u2014 browser-based, claim/stop/release buttons</li> </ul> <p>The voice control pipeline (<code>run_voice_control.py</code>) does not run its own E-Stop \u2014 it assumes one is already running.</p>"},{"location":"architecture/sdk-primer/#sending-movement-commands","title":"Sending Movement Commands","text":"<p>All movement goes through <code>RobotCommandBuilder</code> and <code>RobotCommandClient</code>:</p> <pre><code>from bosdyn.client.robot_command import RobotCommandBuilder, blocking_stand\n\n# Stand up\nblocking_stand(cmd_client, timeout_sec=10)\n\n# Sit down\nfrom bosdyn.client.robot_command import blocking_sit\nblocking_sit(cmd_client, timeout_sec=10)\n\n# Walk forward 1 meter\nfrom bosdyn.client.robot_command import RobotCommandBuilder\ncmd = RobotCommandBuilder.synchro_velocity_command(v_x=0.5, v_y=0.0, v_rot=0.0)\ncmd_client.robot_command(command=cmd, end_time_secs=time.time() + 2.0)\n\n# Turn 90 degrees\nimport math\ncmd = RobotCommandBuilder.synchro_velocity_command(v_x=0.0, v_y=0.0, v_rot=math.radians(60))\ncmd_client.robot_command(command=cmd, end_time_secs=time.time() + 1.5)\n</code></pre>"},{"location":"architecture/sdk-primer/#velocity-commands","title":"Velocity Commands","text":"<p><code>synchro_velocity_command(v_x, v_y, v_rot)</code> is the workhorse:</p> Parameter Unit Description <code>v_x</code> m/s Forward (+) / backward (-) <code>v_y</code> m/s Left (+) / right (-) strafe <code>v_rot</code> rad/s Counter-clockwise (+) / clockwise (-) rotation <p>Commands require an <code>end_time_secs</code> \u2014 the robot stops after this time if no new command is sent. This is a safety feature.</p>"},{"location":"architecture/sdk-primer/#body-height","title":"Body Height","text":"<pre><code>from bosdyn.geometry import EulerZXY\n\ncmd = RobotCommandBuilder.synchro_stand_command(\n    body_height=0.1  # meters above default stance (-0.2 to +0.2 range)\n)\ncmd_client.robot_command(command=cmd)\n</code></pre>"},{"location":"architecture/sdk-primer/#reading-robot-state","title":"Reading Robot State","text":"<pre><code>state = state_client.get_robot_state()\n\n# Battery\nbattery = state.battery_states[0]\ncharge = battery.charge_percentage.value  # 0-100\nruntime = battery.estimated_runtime.seconds  # seconds remaining\n\n# Motor power\nis_powered = (state.power_state.motor_power_state ==\n              state.power_state.STATE_ON)\n\n# Faults\nfor fault in state.system_fault_state.faults:\n    print(fault.name, fault.severity)\n</code></pre>"},{"location":"architecture/sdk-primer/#capturing-images","title":"Capturing Images","text":"<p>Spot has 5 fisheye cameras plus depth sensors:</p> <pre><code>from bosdyn.client.image import ImageClient\nfrom bosdyn.api import image_pb2\n\nimage_client = robot.ensure_client(ImageClient.default_service_name)\n\n# Request a JPEG from the front-left camera\nresponses = image_client.get_image_from_sources([\n    image_pb2.ImageRequest(\n        image_source_name=\"frontleft_fisheye_image\",\n        image_format=image_pb2.Image.FORMAT_JPEG,  # 1\n        quality_percent=75,\n    )\n])\n\njpeg_bytes = responses[0].shot.image.data\n</code></pre>"},{"location":"architecture/sdk-primer/#camera-source-names","title":"Camera Source Names","text":"Source Position <code>frontleft_fisheye_image</code> Front-left (primary for VLM) <code>frontright_fisheye_image</code> Front-right <code>left_fisheye_image</code> Left side <code>right_fisheye_image</code> Right side <code>back_fisheye_image</code> Rear <p>Fisheye orientation</p> <p>Fisheye cameras are mounted sideways on Spot. Images must be rotated 90 degrees clockwise for correct display. When mapping pixel coordinates back to the original frame (e.g., for depth lookup), un-rotate: <code>orig_x = rot_y</code>, <code>orig_y = H_orig - 1 - rot_x</code>.</p>"},{"location":"architecture/sdk-primer/#graphnav-autonomous-navigation","title":"GraphNav (Autonomous Navigation)","text":"<p>GraphNav lets Spot navigate recorded maps autonomously:</p> <pre><code>from bosdyn.client.graph_nav import GraphNavClient\n\ngraph_nav = robot.ensure_client(GraphNavClient.default_service_name)\n\n# Navigate to a waypoint by ID\nnav_response = graph_nav.navigate_to(\n    waypoint_id=\"waypoint_abc123...\",\n    cmd_duration=30.0,  # timeout\n)\n\n# Check status\nfrom bosdyn.api.graph_nav import nav_pb2\nstatus = nav_response.status\nif status == nav_pb2.NavigateToResponse.STATUS_OK:\n    print(\"Navigation started\")\nelif status == nav_pb2.NavigateToResponse.STATUS_NO_PATH:\n    print(\"No path found\")\n</code></pre>"},{"location":"architecture/sdk-primer/#navigation-feedback-loop","title":"Navigation Feedback Loop","text":"<p>Navigation is asynchronous. You poll for completion:</p> <pre><code>while True:\n    feedback = graph_nav.navigation_feedback()\n    status = feedback.status\n\n    if status == nav_pb2.NavigationFeedbackResponse.STATUS_REACHED_GOAL:\n        print(\"Arrived!\")\n        break\n    elif status == nav_pb2.NavigationFeedbackResponse.STATUS_STUCK:\n        print(\"Stuck \u2014 retrying with backtrack\")\n        break\n    elif status == nav_pb2.NavigationFeedbackResponse.STATUS_ROBOT_LOST:\n        print(\"Lost \u2014 need to re-localize\")\n        break\n\n    time.sleep(0.5)\n</code></pre> <p>This pattern is exactly what <code>spot_dispatch.py</code> implements in <code>_navigate_waypoint_sequence()</code>, with re-localization and backtrack-on-stuck recovery.</p>"},{"location":"architecture/sdk-primer/#keepalive-policies","title":"Keepalive Policies","text":"<p>BD SDK 5.0+ introduces keepalive policies that automatically stop the robot if a client disconnects unexpectedly. These can become \"stale\" if a previous session crashed without cleaning up.</p> <pre><code>from bosdyn.client.keepalive import KeepaliveClient, remove_all_policies\n\nkeepalive_client = robot.ensure_client(KeepaliveClient.default_service_name)\nremove_all_policies(keepalive_client)\n</code></pre> <p>The project clears stale policies on every session start (<code>session.py</code>) to avoid <code>KeepaliveMotorsOff</code> errors.</p>"},{"location":"architecture/sdk-primer/#common-errors-and-what-they-mean","title":"Common Errors and What They Mean","text":"Error Cause Fix <code>ResourceAlreadyClaimedError</code> Another client holds the lease Use <code>lease_client.take()</code> instead of <code>acquire()</code> <code>KeepaliveMotorsOffError</code> Stale keepalive policy blocking power-on Clear policies with <code>remove_all_policies()</code> <code>EstoppedError</code> Robot is E-Stopped Release E-Stop from tablet or web panel <code>TimeSyncError</code> Clock sync failed Retry <code>robot.time_sync.wait_for_sync()</code> <code>NotLocalizedError</code> GraphNav doesn't know robot's position Re-localize with fiducial or known waypoint <code>RetryableRpcError</code> Transient network issue Retry the request"},{"location":"architecture/sdk-primer/#further-reading","title":"Further Reading","text":"<ul> <li>Boston Dynamics SDK Documentation</li> <li>Python SDK Examples on GitHub</li> <li>API Reference (protobuf)</li> </ul>"},{"location":"architecture/services/","title":"Runtime Services","text":"<p>The voice control system depends on several services running on the Jetson and on the Spot robot itself. This document lists each service, how to start it, and how to verify it is running.</p>"},{"location":"architecture/services/#service-table","title":"Service Table","text":"Service Port Technology Start Command Riva ASR 50051 Docker (Canary-Qwen-2.5B) <code>./scripts/setup_riva.sh start</code> Ollama 11434 systemd service <code>sudo systemctl start ollama</code> ASR Bridge 50055 Python gRPC server Auto-started by <code>run_voice_control.py</code> Spot API 192.168.80.3 BD SDK (robot onboard) Always on when Spot is powered Web Panel 8080 Python HTTP (stdlib) <code>python scripts/web_panel.py</code>"},{"location":"architecture/services/#riva-asr-port-50051","title":"Riva ASR (port 50051)","text":"<p>NVIDIA Riva runs as a Docker container named <code>riva-speech</code>. It hosts the Canary-Qwen-2.5B speech recognition model, which provides high-accuracy English ASR with punctuation. The container uses the NVIDIA runtime for GPU access.</p> <p>First-time setup requires downloading and optimizing models (~15-30 minutes):</p> <pre><code>./scripts/setup_riva.sh          # install client + download quickstart\ncd ~/riva_quickstart &amp;&amp; bash riva_init.sh   # download + TensorRT optimize\n./scripts/setup_riva.sh start    # start the server\n</code></pre>"},{"location":"architecture/services/#ollama-port-11434","title":"Ollama (port 11434)","text":"<p>Ollama serves the text LLM (<code>qwen2.5:7b</code>) and the VLM (<code>qwen2.5vl:7b</code>). Both models share VRAM and swap in/out as needed. The text LLM is kept alive for 30 minutes after each request; the VLM for 5 minutes.</p> <pre><code>sudo systemctl start ollama\nsudo systemctl enable ollama  # persist across reboots\nollama pull qwen2.5:7b       # text LLM (~4.7GB)\nollama pull qwen2.5vl:7b     # vision LLM (~6GB)\n</code></pre> <p>Enable Ollama for boot persistence</p> <p>Without <code>sudo systemctl enable ollama</code>, Ollama will not start after a Jetson reboot. You'd have to manually run <code>sudo systemctl start ollama</code> each time.</p>"},{"location":"architecture/services/#asr-bridge-port-50055","title":"ASR Bridge (port 50055)","text":"<p>The ASR bridge server (<code>src/voice_control/server.py</code>) translates the custom <code>asr.proto</code> protocol to Riva's native API. It is auto-started by <code>run_voice_control.py</code> unless <code>--no-server</code> is passed. The bridge handles hallucination filtering and minimum duration rejection.</p>"},{"location":"architecture/services/#spot-api-192168803","title":"Spot API (192.168.80.3)","text":"<p>The robot exposes its SDK API on a fixed IP (<code>192.168.80.3</code>) over a direct Ethernet connection. No service management is needed -- the API is available whenever Spot is powered on. Connection credentials are loaded from <code>.env</code> or environment variables.</p>"},{"location":"architecture/services/#web-panel-port-8080","title":"Web Panel (port 8080)","text":"<p>A mobile-friendly web UI for E-Stop management and voice pipeline control. No external dependencies (Python stdlib only). Access via the Jetson's Tailscale or local network IP.</p>"},{"location":"architecture/services/#boot-sequence","title":"Boot Sequence","text":"<p>Start services in this order:</p> <pre><code>1. Riva ASR          ./scripts/setup_riva.sh start\n                     (wait for port 50051 to accept connections)\n\n2. Ollama            sudo systemctl start ollama\n                     (wait for port 11434)\n\n3. E-Stop            python scripts/estop_run.py\n                     (or: python scripts/web_panel.py for phone-based E-Stop)\n                     Must run in a separate terminal. Robot cannot move\n                     without an active E-Stop endpoint.\n\n4. Map Upload        python scripts/setup_map.py\n   (optional)        Only needed if using GraphNav navigation.\n                     Robot must see a fiducial for localization.\n\n5. Voice Control     python scripts/run_voice_control.py\n                     Auto-starts Riva, Ollama, and ASR bridge if not running.\n                     Calibrates noise floor, warms up LLM, opens mic.\n</code></pre> <p><code>run_voice_control.py</code> automates steps 1-2 and the ASR bridge. The only manual prerequisite is the E-Stop (step 3).</p>"},{"location":"architecture/services/#checking-service-status","title":"Checking Service Status","text":""},{"location":"architecture/services/#from-the-command-line","title":"From the command line","text":"<pre><code># Riva ASR\ndocker ps --filter name=riva-speech\npython3 -c \"import socket; s=socket.socket(); s.settimeout(1); print('OK' if s.connect_ex(('127.0.0.1',50051))==0 else 'DOWN'); s.close()\"\n\n# Ollama\nsystemctl is-active ollama\ncurl -s http://localhost:11434/api/tags | python3 -m json.tool\n\n# ASR Bridge\npython3 -c \"import socket; s=socket.socket(); s.settimeout(1); print('OK' if s.connect_ex(('127.0.0.1',50055))==0 else 'DOWN'); s.close()\"\n\n# Spot API (from Jetson, Spot must be connected via Ethernet)\nping -c 1 192.168.80.3\n\n# Web Panel\ncurl -s http://localhost:8080/status\n</code></pre>"},{"location":"architecture/services/#from-the-web-panel","title":"From the web panel","text":"<p>Open <code>http://&lt;jetson-ip&gt;:8080</code> in a browser. The status bar shows live indicators for Riva, Ollama, E-Stop, and voice pipeline status, updated every 2 seconds.</p>"},{"location":"architecture/services/#from-within-the-voice-pipeline","title":"From within the voice pipeline","text":"<p><code>run_voice_control.py</code> checks all services on startup and reports status. If Riva is not running, it attempts to start the Docker container. If Ollama is not running, it attempts <code>sudo systemctl start ollama</code>. If either fails, it prints instructions and exits (or falls back to regex-only mode for Ollama).</p>"},{"location":"architecture/services/#resource-usage","title":"Resource Usage","text":"Service GPU VRAM CPU RAM Riva ASR (Canary-Qwen-2.5B) ~3 GB Low ~1 GB Ollama qwen2.5:7b ~5 GB Low ~1 GB Ollama qwen2.5vl:7b ~6 GB Low ~1 GB YOLO-World + YOLOv8n 0 (CPU) Moderate ~500 MB sherpa-onnx KWS 0 (CPU) Minimal ~10 MB sherpa-onnx Kokoro TTS 0 (CPU) Low ~350 MB Voice client (VAD, audio) 0 Low ~50 MB <p>Riva and Ollama share the GPU. The text LLM and VLM models swap in/out of VRAM on demand (Ollama manages this automatically). Total peak VRAM usage is approximately 8-9 GB when both Riva and one Ollama model are loaded.</p>"},{"location":"architecture/voice-pipeline/","title":"Voice Pipeline Deep Dive","text":"<p>The voice pipeline in <code>src/voice_control/client_mic.py</code> handles the full path from raw microphone audio to executed robot commands. This document covers the state machine, audio processing, and timing characteristics.</p>"},{"location":"architecture/voice-pipeline/#state-machine","title":"State Machine","text":"<pre><code>stateDiagram-v2\n    [*] --&gt; WAKE_WORD\n\n    WAKE_WORD --&gt; LISTENING : \"Hey Spot\" detected&lt;br/&gt;(KWS or ASR fallback)\n    WAKE_WORD --&gt; WAKE_WORD : Non-safety speech ignored\n\n    LISTENING --&gt; RECORDING : Speech onset confirmed&lt;br/&gt;(3+ consecutive VAD frames)\n    LISTENING --&gt; WAKE_WORD : 5-minute idle timeout\n\n    RECORDING --&gt; PROCESSING : Silence timeout reached&lt;br/&gt;(0.8s short / 1.5s long)\n    RECORDING --&gt; PROCESSING : Max duration (20s)\n\n    PROCESSING --&gt; LISTENING : Command processed\n    PROCESSING --&gt; WAKE_WORD : Wake phrase detected&lt;br/&gt;(ASR fallback mode)\n\n    note right of WAKE_WORD\n        Safety commands (stop/freeze/estop)\n        always execute regardless of state\n    end note</code></pre> <p>State descriptions:</p> <ul> <li>WAKE_WORD -- Waiting for \"Hey Spot\". The wake word detector   (<code>sherpa-onnx KeywordSpotter</code>) processes every audio frame. Safety commands   still execute via VAD + ASR. All other speech is discarded.</li> <li>LISTENING -- Wake word heard, waiting for speech. VAD monitors energy   levels. Transitions back to WAKE_WORD after 5 minutes of idle.</li> <li>RECORDING -- Speech detected. Audio frames accumulate in a buffer.   Silence timeout triggers processing. Two timeouts: short (0.8s) for brief   commands, long (1.5s) for chained commands (crossover at 2s of speech).</li> </ul>"},{"location":"architecture/voice-pipeline/#audio-flow","title":"Audio Flow","text":"<pre><code>sounddevice callback (stereo -&gt; mono PCM16)\n        |\n        v\n    audio_queue (thread-safe)\n        |\n        v\n    Main loop: extract 30ms frames\n        |\n        +---&gt; Wake word detector (every frame in WAKE_WORD state)\n        |\n        +---&gt; Compute frame RMS energy\n        |\n        +---&gt; Energy gate (frame_rms &gt; threshold?)\n        |         |\n        |         +---&gt; No: update adaptive noise floor (EMA)\n        |         +---&gt; Yes: WebRTC VAD check\n        |                    |\n        |                    +---&gt; Speech onset debounce (3 consecutive frames)\n        |                    +---&gt; Accumulate to speech_buffer\n        |                    +---&gt; Pre-roll buffer (5 frames before onset)\n        |\n        +---&gt; Silence timeout check\n                  |\n                  v\n              process_utterance()\n</code></pre> <p>Key details:</p> <ul> <li>The <code>sounddevice</code> callback runs in a separate OS thread. It selects   channel 0 (beamformed left) from the XVF3800 stereo mic and pushes   30ms frames into a <code>queue.Queue</code>.</li> <li>The mic is muted during TTS playback (<code>_mic_muted</code> flag) to prevent   the robot from hearing its own voice.</li> <li>After processing, stale audio is drained but the last ~1 second is   kept so new speech arriving during processing is not lost.</li> </ul>"},{"location":"architecture/voice-pipeline/#processing-flow-process_utterance","title":"Processing Flow: <code>process_utterance()</code>","text":"<p><code>process_utterance()</code> is the core function called after silence timeout. It takes the accumulated speech buffer and produces robot actions.</p>"},{"location":"architecture/voice-pipeline/#step-1-safety-fast-path-regex-5ms","title":"Step 1: Safety Fast-Path (regex, &lt;5ms)","text":"<pre><code>SAFETY_PATTERNS = [\n    (r\"\\b(?:stop|halt)\\b\",                     \"stop\"),\n    (r\"\\bfreeze\\b\",                            \"freeze\"),\n    (r\"\\b(?:emergency\\s+stop|e[\\s-]?stop)\\b\",  \"estop\"),\n]\n</code></pre> <p>Safety commands are checked via simple regex immediately after ASR returns. If matched, the command is dispatched to Spot instantly -- no LLM involved. Safety commands execute in all states, even WAKE_WORD.</p>"},{"location":"architecture/voice-pipeline/#step-2-wake-phrase-strip","title":"Step 2: Wake Phrase Strip","text":"<p>When using the dedicated wake word detector, the ASR transcript may still contain the wake phrase (\"Hey Spot stand up\"). The wake phrase is stripped before passing to the LLM:</p> <pre><code>\"Hey Spot stand up\" -&gt; \"stand up\"\n</code></pre> <p>This allows users to say \"Hey Spot\" and their command in one breath.</p>"},{"location":"architecture/voice-pipeline/#step-3-llm-brain-structured-json","title":"Step 3: LLM Brain (structured JSON)","text":"<p>The transcript plus current robot state (battery, position, saved locations) is sent to Ollama. The LLM returns structured JSON:</p> <pre><code>{\n  \"actions\": [\n    {\"action\": \"walk\", \"params\": {\"direction\": \"forward\", \"distance\": 2}},\n    {\"action\": \"sit\", \"params\": {}}\n  ],\n  \"response\": \"Walking forward 2 meters and then sitting down!\"\n}\n</code></pre> <p>The response is spoken via TTS before actions execute.</p>"},{"location":"architecture/voice-pipeline/#step-4-vlm-for-describe-action","title":"Step 4: VLM for Describe Action","text":"<p>When the LLM returns a <code>describe</code> action:</p> <ol> <li>Capture a JPEG frame from the specified camera (default: front).</li> <li>If a specific query was given (e.g., \"do you see a chair?\"), run YOLO    detection first to provide a hint to the VLM.</li> <li>Send the image + first-person prompt to the VLM (<code>qwen2.5vl:7b</code>).</li> <li>Speak the VLM response via TTS.</li> </ol> <p>The VLM prompt enforces first-person perspective: \"You are looking around\" rather than \"This image shows\". The VLM is warmed up on-demand (not at startup) because it shares VRAM with the text LLM.</p>"},{"location":"architecture/voice-pipeline/#step-5-command-chaining","title":"Step 5: Command Chaining","text":"<p>Multiple actions are executed sequentially with synchronization waits:</p> Command Type Wait Strategy <code>go_to</code>, <code>tour</code>, <code>patrol</code>, <code>come_back</code>, <code>go_to_object</code>, <code>follow_me</code> <code>_wait_for_nav_complete()</code> -- polls <code>is_navigating()</code> <code>walk</code>, <code>strafe</code>, <code>turn</code> Fixed 3.0s delay <code>sit</code>, <code>stand</code>, <code>selfright</code>, <code>body_height</code> Fixed 4.0s delay <p>If any action in the chain fails, the chain stops immediately.</p>"},{"location":"architecture/voice-pipeline/#key-configuration-parameters","title":"Key Configuration Parameters","text":"Parameter Value Purpose <code>SAMPLE_RATE</code> 16000 Hz Standard for speech recognition <code>FRAME_MS</code> 30 ms Audio frame size (480 samples) <code>VAD_LEVEL</code> 1 WebRTC VAD aggressiveness (0-3); 1 catches softer speech <code>SILENCE_TIMEOUT_SHORT</code> 0.8 s Silence timeout for utterances &lt; 2s <code>SILENCE_TIMEOUT_LONG</code> 1.5 s Silence timeout for utterances &gt; 2s (chains) <code>SILENCE_CROSSOVER</code> 2.0 s Speech duration to switch from short to long timeout <code>SPEECH_ONSET_FRAMES</code> 3 Consecutive VAD+energy frames to confirm speech (~90ms) <code>NAV_ONSET_FRAMES</code> 5 Higher onset threshold during navigation (~150ms) <code>PREROLL_FRAMES</code> 5 Frames kept before onset (~150ms) to capture word beginnings <code>MAX_UTTERANCE_SECONDS</code> 20 s Force-process after this duration <code>NOISE_CALIBRATION_SECONDS</code> 2 s Ambient noise measurement at startup <code>ENERGY_THRESHOLD_MULTIPLIER</code> 2.0x Speech must be this much louder than noise floor <code>NOISE_EMA_ALPHA</code> 0.03 Exponential moving average for adaptive noise floor <code>NOISE_FLOOR_MIN</code> 0.001 Minimum noise floor (prevents threshold dropping to zero) <code>NOISE_FLOOR_MAX</code> 0.05 Maximum noise floor (prevents threshold going too high) <code>NAV_ENERGY_MULT</code> 5.0x Extra energy multiplier during GraphNav navigation <code>LISTENING_TIMEOUT</code> 300 s Return to WAKE_WORD after 5 minutes of idle <code>MIN_SPEECH_DURATION</code> 0.3 s Reject utterances shorter than this (noise bursts)"},{"location":"architecture/voice-pipeline/#typical-latencies","title":"Typical Latencies","text":"Stage Latency Notes Wake word detection ~0 ms Runs inline on every 30ms frame VAD + onset confirmation ~90 ms 3 frames at 30ms each ASR (Riva Canary-Qwen-2.5B) 0.5 - 2.0 s Depends on utterance length LLM (qwen2.5:7b warm) 0.2 - 1.0 s Prompt cached after warm-up LLM (cold start) 10 - 17 s Model loading into VRAM VLM (qwen2.5vl:7b warm) 2 - 6 s Image + text inference VLM (cold start) ~17 s Model loading into VRAM TTS (Kokoro) 0.1 - 0.5 s Generation + playback starts immediately Safety command (stop/estop) &lt; 5 ms Regex match, no LLM Typical end-to-end 1.5 - 4.0 s Wake word to action execution (warm)"},{"location":"commands/reference/","title":"Voice Command Reference","text":"<p>Complete reference for all voice commands supported by the Spot voice control system. Commands are processed by the LLM Brain (<code>qwen2.5:7b</code>) which interprets natural language and maps it to structured actions.</p>"},{"location":"commands/reference/#wake-word","title":"Wake Word","text":"<p>Say \"Hey Spot\" before giving a command. The wake word detector uses sherpa-onnx keyword spotting and runs on every audio frame with near-zero latency.</p> <p>Two usage patterns:</p> <ol> <li>Two-step: Say \"Hey Spot\", wait for the chime, then say your command.</li> <li>One-breath: Say \"Hey Spot stand up\" in a single utterance. The wake    phrase is stripped and the command (\"stand up\") is processed immediately.</li> </ol> <p>After the wake word triggers, the system listens for 5 minutes before requiring \"Hey Spot\" again.</p> <p>Safety commands (<code>stop</code>, <code>freeze</code>, <code>estop</code>) always work regardless of wake word state.</p>"},{"location":"commands/reference/#command-table","title":"Command Table","text":""},{"location":"commands/reference/#safety","title":"Safety","text":"Voice Command Example Phrases Action Parameters stop \"stop\", \"halt\" Stop all movement immediately None freeze \"freeze\" Hold current position None estop \"emergency stop\", \"e-stop\" Emergency stop (cuts motors) None <p>Safety commands bypass the LLM entirely -- they are matched by regex in under 5ms and dispatched directly to the robot.</p>"},{"location":"commands/reference/#posture","title":"Posture","text":"Voice Command Example Phrases Action Parameters stand \"stand up\", \"get up\" Stand up from sitting None sit \"sit down\", \"lay down\" Sit down None selfright \"self right\", \"recover\", \"get up from fall\" Recover from a fall None"},{"location":"commands/reference/#body","title":"Body","text":"Voice Command Example Phrases Action Parameters crouch \"crouch\", \"get low\", \"duck\" Lower body to minimum height <code>height: -0.15</code> stand tall \"stand tall\", \"max height\" Raise body to maximum height <code>height: 0.1</code> normal height \"normal height\" Reset body to default height <code>height: 0.0</code> <p>Body height ranges from -0.15 (crouch) to 0.1 (tall). The LLM can also set intermediate heights when asked (e.g., \"lower yourself a bit\").</p>"},{"location":"commands/reference/#movement","title":"Movement","text":"Voice Command Example Phrases Action Parameters walk forward \"walk forward\", \"go forward 2 meters\" Walk forward <code>direction: forward, distance: &lt;m&gt;</code> walk backward \"walk backward\", \"back up\" Walk backward <code>direction: backward, distance: &lt;m&gt;</code> strafe left \"strafe left\", \"move left\" Sideways movement left <code>direction: left, distance: &lt;m&gt;</code> strafe right \"strafe right\", \"move right\" Sideways movement right <code>direction: right, distance: &lt;m&gt;</code> turn left \"turn left\", \"turn left 45 degrees\" Rotate left in place <code>dir: left, deg: &lt;degrees&gt;</code> turn right \"turn right\" Rotate right in place <code>dir: right, deg: &lt;degrees&gt;</code> turn around \"turn around\" Rotate 180 degrees <code>deg: 180</code> <p>Default values: - Walk distance: 1.0 meters - Strafe distance: 0.5 meters - Turn angle: 90 degrees</p>"},{"location":"commands/reference/#speed","title":"Speed","text":"Voice Command Example Phrases Action Parameters slow \"go slow\", \"slow down\" Set speed to slow <code>speed: slow</code> normal \"normal speed\" Set speed to normal <code>speed: normal</code> fast \"go fast\", \"speed up\" Set speed to fast <code>speed: fast</code>"},{"location":"commands/reference/#navigation","title":"Navigation","text":"Voice Command Example Phrases Action Parameters go to \"go to kitchen\", \"navigate to lab\" Navigate to a saved location <code>location: &lt;name&gt;</code> tour \"tour kitchen then lab\", \"visit everywhere\" Visit locations in sequence (one pass) <code>locations: [&lt;names&gt;]</code> or <code>locations: \"all\"</code> patrol \"patrol the hallway and lab\", \"keep patrolling\" Loop through locations continuously <code>locations: [&lt;names&gt;]</code> or <code>locations: \"all\"</code> come back \"come back\", \"go home\", \"return\" Return to position before last navigation None save location \"save this location as kitchen\" Save current position with a name <code>location: &lt;name&gt;</code> list locations \"list locations\", \"what locations do you know?\" List all saved locations None <p>Navigation uses GraphNav (BD SDK). Locations are stored in <code>locations.json</code> as human-readable names mapped to GraphNav waypoint IDs.</p> <ul> <li>Tour visits each location once in order, then stops.</li> <li>Patrol loops continuously until you say \"stop\".</li> <li>Come back returns to the waypoint the robot was at before the last   <code>go_to</code>, <code>tour</code>, or <code>patrol</code> command.</li> </ul> <p>Location names are automatically normalized to <code>lowercase_with_underscores</code>.</p>"},{"location":"commands/reference/#vision","title":"Vision","text":"Voice Command Example Phrases Action Parameters describe \"what do you see?\", \"look around\" Capture photo and describe scene via VLM <code>camera: &lt;side&gt;</code> describe with query \"do you see a chair?\", \"is there a person?\" Describe scene with YOLO hint for specific object <code>camera: &lt;side&gt;, query: &lt;text&gt;</code> go_to_object \"go to the red chair\", \"find the backpack\" Walk toward a visible object using camera <code>description: &lt;text&gt;</code> follow_me \"follow me\", \"come with me\" Follow the nearest person None <p>Describe uses the VLM (<code>qwen2.5vl:7b</code>). When a specific query is given, YOLO-World runs first to provide a detection hint to the VLM.</p> <p>go_to_object uses YOLO-World (open-vocabulary) to detect the target object across all cameras, then steers toward it. This is for visible objects, not saved map locations. If a saved location matches the name, <code>go_to</code> (GraphNav) is used instead.</p> <p>follow_me uses YOLOv8n for person detection on CPU. The robot follows the nearest detected person, maintaining distance.</p> <p>Camera options: <code>front</code> (default), <code>left</code>, <code>right</code>, <code>back</code>.</p>"},{"location":"commands/reference/#door","title":"Door","text":"Voice Command Example Phrases Action Parameters open_door \"open the door\", \"push the door\" Open a push-bar door None <p>Uses BD DoorService <code>AutoPushCommand</code>. The robot pitches up, detects the push bar via VLM, computes a 3D push point, and pushes the door open. Requires the robot to be positioned ~1m from the door, facing it.</p> <p>Note: Door opening is experimental. Calibration may be required (<code>scripts/calibrate_door.py</code>).</p>"},{"location":"commands/reference/#status","title":"Status","text":"Voice Command Example Phrases Action Parameters battery \"battery level\", \"how much battery?\" Report battery percentage and runtime None status \"status report\", \"how are you doing?\" Full status: battery, posture, location, faults None power off \"power off\", \"shut down\" Safely power off the robot None"},{"location":"commands/reference/#command-chaining","title":"Command Chaining","text":"<p>The LLM supports chaining multiple actions in a single utterance. Actions execute sequentially with synchronization waits between them.</p> <p>Examples:</p> Utterance Actions \"go to kitchen then sit down\" <code>go_to(kitchen)</code> -&gt; wait for nav -&gt; <code>sit</code> \"stand up and walk forward 3 meters\" <code>stand</code> -&gt; wait 4s -&gt; <code>walk(forward, 3)</code> \"go to lab then come back\" <code>go_to(lab)</code> -&gt; wait for nav -&gt; <code>come_back</code> \"turn left and walk forward\" <code>turn(left, 90)</code> -&gt; wait 3s -&gt; <code>walk(forward, 1)</code> <p>If any action in the chain fails, the remaining actions are skipped.</p>"},{"location":"commands/reference/#multi-location-navigation","title":"Multi-Location Navigation","text":"<p>When visiting multiple locations, prefer naming them in a single command. The LLM will use a single <code>tour</code> action instead of chaining multiple <code>go_to</code> actions, which is more efficient.</p> Utterance Parsed As \"go to A then B then C\" <code>tour(locations: [A, B, C])</code> (single action) \"visit everywhere\" <code>tour(locations: \"all\")</code> \"keep patrolling A and B\" <code>patrol(locations: [A, B])</code>"},{"location":"commands/reference/#conversation","title":"Conversation","text":"<p>The LLM also handles conversational queries that do not require physical actions. For example:</p> <ul> <li>\"How are you doing?\" -- responds conversationally (no action)</li> <li>\"What's your name?\" -- responds conversationally (no action)</li> <li>\"Tell me a joke\" -- responds conversationally (no action)</li> </ul> <p>The LLM maintains a conversation history of the last 12 messages (6 turns) for context.</p>"},{"location":"getting-started/daily-operations/","title":"Daily Operations","text":"<p>A quick-reference checklist for every session. This assumes all software and models are already installed.</p>"},{"location":"getting-started/daily-operations/#boot-sequence-after-jetson-reboot","title":"Boot Sequence (After Jetson Reboot)","text":"<p>When the Jetson reboots, Tailscale reconnects automatically but Riva and the voice pipeline need to be restarted. Ollama restarts automatically if you ran <code>sudo systemctl enable ollama</code> during setup.</p> <pre><code># SSH into the Jetson\nssh spotdog@&lt;tailscale-ip&gt;\n\n# Go to the project directory and activate venv\ncd ~/spot/dartmouth_spot_capstone\nsource spot-env/bin/activate\n\n# 1. Start Riva ASR (takes 30-60s to fully initialize)\n./scripts/setup_riva.sh start\n\n# 2. Verify Ollama is running (should auto-start via systemd)\nsystemctl is-active ollama\n# If \"inactive\": sudo systemctl start ollama\n\n# 3. Start E-Stop (keep this terminal open, or use web panel)\npython scripts/estop_run.py\n</code></pre> <p>Use the web panel instead of estop_run.py</p> <p>If you prefer phone-based E-Stop, run <code>python scripts/web_panel.py</code> in a <code>tmux</code> session and open <code>http://&lt;jetson-ip&gt;:8080</code> on your phone. This lets you manage E-Stop without keeping an SSH terminal open.</p>"},{"location":"getting-started/daily-operations/#starting-voice-control","title":"Starting Voice Control","text":"<p>Open a second terminal (or <code>tmux</code> pane):</p> <pre><code>cd ~/spot/dartmouth_spot_capstone\nsource spot-env/bin/activate\npython scripts/run_voice_control.py\n</code></pre> <p>This auto-checks Riva and Ollama, starts the ASR bridge, calibrates the mic, warms up the LLM, and opens the microphone. When you see <code>LISTENING</code>, the system is ready.</p>"},{"location":"getting-started/daily-operations/#pre-flight-checklist","title":"Pre-Flight Checklist","text":"<p>Before giving Spot commands, verify:</p> <ul> <li>[ ] Spot is powered on (fans audible)</li> <li>[ ] Jetson is on Spot's WiFi (<code>ping 192.168.80.3</code>)</li> <li>[ ] E-Stop is active (terminal or web panel)</li> <li>[ ] Microphone and speaker are plugged in</li> <li>[ ] Map is uploaded if using navigation (<code>python scripts/setup_map.py</code> \u2014 only needed once per power cycle)</li> </ul>"},{"location":"getting-started/daily-operations/#uploading-the-map-if-using-navigation","title":"Uploading the Map (If Using Navigation)","text":"<p>GraphNav maps are cleared when Spot reboots. Re-upload after each Spot power cycle:</p> <pre><code>python scripts/setup_map.py --map-path maps/lab_map2\n</code></pre> <p>For localization, either:</p> <ul> <li>Fiducial: Position Spot where it can see an AprilTag from the map (auto-localizes)</li> <li>Waypoint: Drive Spot to a known location and use <code>--waypoint-init</code></li> </ul>"},{"location":"getting-started/daily-operations/#stopping","title":"Stopping","text":"<ol> <li>Say \"sit down\" to Spot (or \"stop\" if mid-motion)</li> <li>Ctrl+C in the voice control terminal \u2014 Spot sits and powers off</li> <li>Ctrl+C in the E-Stop terminal \u2014 releases E-Stop</li> </ol>"},{"location":"getting-started/daily-operations/#using-tmux-for-persistent-sessions","title":"Using tmux for Persistent Sessions","text":"<p>If you're SSHing in, use <code>tmux</code> so processes survive SSH disconnects:</p> <pre><code># Start a new tmux session\ntmux new -s spot\n\n# Split into panes: Ctrl+B then %  (vertical split)\n# Switch panes: Ctrl+B then arrow keys\n\n# Pane 1: E-Stop (or web panel)\npython scripts/estop_run.py\n\n# Pane 2: Voice control\npython scripts/run_voice_control.py\n\n# Detach: Ctrl+B then D\n# Reattach later: tmux attach -t spot\n</code></pre>"},{"location":"getting-started/daily-operations/#quick-service-health-check","title":"Quick Service Health Check","text":"<pre><code># All-in-one status check\ndocker ps --filter name=riva-speech --format \"Riva: {{.Status}}\"\nsystemctl is-active ollama &amp;&amp; echo \"Ollama: OK\" || echo \"Ollama: DOWN\"\npython3 -c \"import socket; s=socket.socket(); s.settimeout(1); print('ASR Bridge: OK' if s.connect_ex(('127.0.0.1',50055))==0 else 'ASR Bridge: DOWN'); s.close()\"\nping -c 1 -W 1 192.168.80.3 &gt; /dev/null 2&gt;&amp;1 &amp;&amp; echo \"Spot: OK\" || echo \"Spot: DOWN\"\n</code></pre> <p>Or open the web panel at <code>http://&lt;jetson-ip&gt;:8080</code> \u2014 the status bar shows live indicators for all services.</p>"},{"location":"getting-started/hardware/","title":"Hardware Setup","text":""},{"location":"getting-started/hardware/#jetson-agx-orin-64gb","title":"Jetson AGX Orin 64GB","text":"<p>The compute platform is an NVIDIA Jetson AGX Orin 64GB Developer Kit. It rides on Spot's back payload rail and runs all inference (ASR, LLM, VLM, TTS, wake word, YOLO) on-device.</p> <p>Key specs:</p> <ul> <li>GPU: Ampere architecture, 2048 CUDA cores, 64 Tensor Cores</li> <li>RAM: 64GB unified LPDDR5 (shared between CPU and GPU)</li> <li>Storage: 64GB eMMC + NVMe SSD recommended for models</li> <li>Power: 15W-60W configurable (use MAXN for best inference speed)</li> </ul>"},{"location":"getting-started/hardware/#jetpack-621","title":"JetPack 6.2.1","text":"<p>The Jetson runs JetPack 6.2.1 (L4T r36.4.3, Ubuntu 22.04). This version was chosen because it provides:</p> <ul> <li>CUDA 12.6 -- required by Riva ASR and Ollama</li> <li>Python 3.10 -- compatible with Boston Dynamics SDK 5.0.1.1</li> <li>glibc 2.35 -- required by sherpa-onnx aarch64 wheels</li> <li>TensorRT 10.3 -- used by Riva for optimized ASR inference</li> <li>Docker + NVIDIA Container Toolkit -- pre-installed for Riva</li> </ul> <p>Do not upgrade to JetPack 6.3+ without testing</p> <p>JetPack upgrades can break CUDA/TensorRT compatibility with Riva and Ollama. Stick with 6.2.1 unless you have a specific reason to upgrade and can re-validate the full stack.</p>"},{"location":"getting-started/hardware/#flashing-the-jetson","title":"Flashing the Jetson","text":"<p>If you need to re-flash (new Jetson or recovery), use NVIDIA SDK Manager on a Windows or Ubuntu x86 host machine.</p>"},{"location":"getting-started/hardware/#prerequisites","title":"Prerequisites","text":"<ul> <li>USB-C cable (Jetson to host)</li> <li>Host machine with NVIDIA SDK Manager installed</li> <li>Jetson power supply connected</li> </ul>"},{"location":"getting-started/hardware/#steps","title":"Steps","text":"<ol> <li> <p>Put the Jetson into Force Recovery Mode:</p> <ul> <li>Power off the Jetson</li> <li>Hold the middle button (Force Recovery) while pressing the power button</li> <li>Release both after 2 seconds</li> <li>Verify on host: <code>lsusb | grep NVIDIA</code> should show the device</li> </ul> </li> <li> <p>Open SDK Manager on the host, select:</p> <ul> <li>Target: Jetson AGX Orin Developer Kit</li> <li>JetPack: 6.2.1</li> <li>Flash both OS and SDK components</li> </ul> </li> <li> <p>Flash takes 15-30 minutes. The Jetson will reboot and show the Ubuntu setup wizard.</p> </li> </ol>"},{"location":"getting-started/hardware/#post-flash-verification","title":"Post-Flash Verification","text":"<p>After first boot, verify the key components:</p> <pre><code># CUDA\nnvcc --version\n# Expected: Cuda compilation tools, release 12.6\n\n# Python\npython3 --version\n# Expected: Python 3.10.x\n\n# Docker\ndocker --version\nsudo docker run --rm --runtime=nvidia --gpus all nvidia/cuda:12.6.0-base-ubuntu22.04 nvidia-smi\n# Should show GPU info\n\n# Disk space\ndf -h /\n# Check available space (need ~20GB for models + Riva)\n</code></pre>"},{"location":"getting-started/hardware/#spot-robot-connection","title":"Spot Robot Connection","text":"<p>The Jetson connects to Spot over WiFi. Spot acts as a WiFi access point.</p> Setting Value Spot WiFi SSID Spot's network (check robot admin console) Spot IP <code>192.168.80.3</code> Admin console <code>https://192.168.80.3</code> (browser) SDK port 443 (gRPC over HTTPS) <p>To connect:</p> <ol> <li>Power on Spot (press the power button on the battery, wait for the fans)</li> <li>Connect the Jetson to Spot's WiFi network</li> <li>Verify connectivity:</li> </ol> <pre><code>ping 192.168.80.3\n</code></pre> <ol> <li>Verify SDK access:</li> </ol> <pre><code>source spot-env/bin/activate\npython -c \"\nfrom bosdyn.client import create_standard_sdk\nsdk = create_standard_sdk('test')\nrobot = sdk.create_robot('192.168.80.3')\nrobot.authenticate('USERNAME', 'PASSWORD')\nprint('Connected:', robot.get_id())\n\"\n</code></pre> <p>Robot credentials</p> <p>The robot username and password are set in the <code>.env</code> file. Ask a team member or check the robot admin console at <code>https://192.168.80.3</code>.</p>"},{"location":"getting-started/hardware/#microphone","title":"Microphone","text":"<p>The system uses a ReSpeaker XVF3800 USB 4-mic array (appears as a 2-channel USB audio device).</p> <ul> <li>Channel 0 (left): beamformed audio -- used for ASR</li> <li>Channel 1 (right): reference/raw -- not used by default</li> <li>Sample rate: 16kHz (set in software)</li> <li>Device index: typically <code>24</code> on the Jetson (verify with <code>--list-devices</code>)</li> </ul> <p>To find the device index:</p> <pre><code>source spot-env/bin/activate\npython src/voice_control/client_mic.py --list-devices\n</code></pre> <p>Look for a device named <code>XVF3800</code> or similar. Pass its index with <code>--device N</code> if it is not 24.</p>"},{"location":"getting-started/hardware/#audio-output","title":"Audio Output","text":"<p>Any USB speaker or 3.5mm audio output works for TTS playback. The system uses <code>sounddevice</code> which routes to the default ALSA output device.</p> <p>To test audio output:</p> <pre><code>speaker-test -t sine -f 440 -l 1\n</code></pre>"},{"location":"getting-started/hardware/#physical-mounting","title":"Physical Mounting","text":"<p>The Jetson AGX Orin mounts on Spot's top payload rail (the flat area on the robot's back).</p>"},{"location":"getting-started/hardware/#power","title":"Power","text":"<p>The Jetson can be powered in two ways:</p> Method Details Spot payload port Spot provides 24V through the payload connector. Use a 24V-to-19V DC-DC converter (barrel jack) to power the Jetson. This is the cleanest setup \u2014 no external batteries. External battery A USB-C PD power bank (65W+) or separate AC adapter. Useful for bench testing without Spot."},{"location":"getting-started/hardware/#usb-connections","title":"USB Connections","text":"<p>The Jetson has multiple USB ports. Typical setup:</p> Port Device USB-A ReSpeaker XVF3800 microphone array USB-A USB speaker (or use 3.5mm audio jack) USB-C Power (if using external power) <p>If you need more USB ports, use a powered USB hub.</p>"},{"location":"getting-started/hardware/#cable-management","title":"Cable Management","text":"<p>Route USB cables along the payload rail and secure with zip ties or velcro straps. Keep cables away from Spot's hip joints to avoid snagging during movement. The mic should face forward (toward Spot's head) for best voice pickup.</p>"},{"location":"getting-started/hardware/#jetson-power-mode","title":"Jetson Power Mode","text":"<p>For best inference performance, set the Jetson to MAXN power mode:</p> <pre><code>sudo nvpmodel -m 0       # MAXN (60W, all cores, full GPU)\nsudo jetson_clocks        # Lock clocks to max frequency\n</code></pre> <p>Verify with:</p> <pre><code>sudo nvpmodel -q          # Should show \"MAXN\"\n</code></pre>"},{"location":"getting-started/hardware/#network-topology","title":"Network Topology","text":"<pre><code>[Spot Robot]\n    |\n    | WiFi (192.168.80.x)\n    |\n[Jetson AGX Orin]\n    |--- Riva ASR (Docker, localhost:50051)\n    |--- Ollama LLM/VLM (localhost:11434)\n    |--- ASR Bridge (localhost:50055)\n    |--- Web Panel (port 8080, accessible via Tailscale or LAN)\n    |\n    | USB\n    |\n[ReSpeaker XVF3800 Mic]\n[USB/3.5mm Speaker]\n</code></pre> <p>The Jetson and Spot must be on the same network. If the Jetson also needs internet access (for model downloads, SSH), connect it to a second network (Ethernet or Tailscale).</p>"},{"location":"getting-started/models/","title":"Model Downloads","text":"<p>All models are downloaded locally and gitignored. Nothing is fetched at runtime except by Ollama (which caches models after the first pull).</p>"},{"location":"getting-started/models/#summary-table","title":"Summary Table","text":"Model Size Location Setup Command Kokoro TTS (v0.19) ~340MB <code>models/tts/kokoro-en-v0_19/</code> <code>python scripts/setup_kokoro.py</code> KWS Wake Word (zipformer int8) ~5MB <code>models/kws/</code> <code>python scripts/setup_kws.py</code> YOLOv8n (person detection) ~7MB auto-downloaded by ultralytics first use YOLO-World v2 (open-vocab) ~28MB auto-downloaded by ultralytics first use qwen2.5:7b (text LLM) ~4.7GB Ollama cache (<code>~/.ollama/</code>) <code>ollama pull qwen2.5:7b</code> qwen2.5vl:7b (vision LLM) ~4.7GB Ollama cache (<code>~/.ollama/</code>) <code>ollama pull qwen2.5vl:7b</code> Riva Canary-Qwen-2.5B (ASR) ~5GB Riva Docker volume <code>bash riva_init.sh</code> <p>Total disk usage: approximately 15GB across all models.</p>"},{"location":"getting-started/models/#tts-kokoro-sherpa-onnx","title":"TTS: Kokoro (sherpa-onnx)","text":"<p>The Kokoro-en-v0_19 model provides high-quality neural text-to-speech with 11 English speakers. It runs on CPU via sherpa-onnx (no GPU needed).</p> <pre><code>python scripts/setup_kokoro.py\n</code></pre> <p>This downloads and extracts the model pack (~340MB) to <code>models/tts/kokoro-en-v0_19/</code>. The download streams directly into tar extraction to avoid doubling disk usage.</p> <p>Contents after setup:</p> <pre><code>models/tts/kokoro-en-v0_19/\n    model.onnx       # ~330MB neural TTS model\n    voices.bin        # Speaker embeddings\n    tokens.txt        # Tokenizer vocabulary\n    espeak-ng-data/   # Phonemizer data\n</code></pre> <p>The default speaker is <code>af_sarah</code> (speaker ID 3), a warm American English female voice. Speaker can be changed in <code>src/voice_control/spot_tts.py</code>.</p> <p>Verify:</p> <pre><code>python src/voice_control/spot_tts.py\n</code></pre>"},{"location":"getting-started/models/#wake-word-sherpa-onnx-kws","title":"Wake Word: sherpa-onnx KWS","text":"<p>The keyword spotter detects \"Hey Spot\" in streaming audio with near-zero latency. Uses a zipformer model trained on GigaSpeech XL, int8 quantized.</p> <pre><code>python scripts/setup_kws.py\n</code></pre> <p>This downloads individual model files (~5MB total) from HuggingFace to <code>models/kws/</code> and creates <code>keywords.txt</code> with the BPE-encoded \"HEY SPOT\" keyword.</p> <p>Contents after setup:</p> <pre><code>models/kws/\n    encoder-epoch-12-avg-2-chunk-16-left-64.int8.onnx\n    decoder-epoch-12-avg-2-chunk-16-left-64.int8.onnx\n    joiner-epoch-12-avg-2-chunk-16-left-64.int8.onnx\n    tokens.txt\n    bpe.model\n    keywords.txt    # Generated by setup script\n</code></pre> <p>Verify:</p> <pre><code>python scripts/setup_kws.py --check\n</code></pre> <p>Test with microphone:</p> <pre><code>python src/voice_control/wake_word.py --test-mic --device 24\n</code></pre>"},{"location":"getting-started/models/#yolo-object-detection","title":"YOLO: Object Detection","text":"<p>YOLO models are used for visual navigation (\"go to the red chair\") and person following. They are managed by the <code>ultralytics</code> package and auto-downloaded on first use.</p> <ul> <li>YOLOv8n (~7MB) -- fast person detection for follow mode (class 0 = person)</li> <li>YOLOv8s-worldv2 (~28MB) -- open-vocabulary detection for arbitrary objects</li> </ul> <p>Both run on CPU to keep GPU free for LLM/VLM inference.</p> <p>No manual setup needed. On first run, ultralytics downloads the weights to the project root. They are gitignored.</p>"},{"location":"getting-started/models/#llm-vlm-ollama","title":"LLM / VLM: Ollama","text":"<p>Ollama manages the text and vision language models. Models are stored in Ollama's cache directory (<code>~/.ollama/models/</code>).</p> <pre><code>ollama pull qwen2.5:7b       # Text LLM for command interpretation\nollama pull qwen2.5vl:7b     # Vision-Language Model for scene description\n</code></pre> <p>These two models share GPU VRAM. Ollama automatically swaps them as needed:</p> <ul> <li>qwen2.5:7b is loaded at startup and kept warm for 30 minutes between requests</li> <li>qwen2.5vl:7b is loaded on-demand when a visual query is made (first load ~15s, then ~5s warm)</li> </ul> <p>To check installed models:</p> <pre><code>ollama list\n</code></pre>"},{"location":"getting-started/models/#riva-asr-docker","title":"Riva ASR: Docker","text":"<p>The Riva ASR model (Canary-Qwen-2.5B) is managed entirely through Docker. It is downloaded and optimized during <code>riva_init.sh</code> (part of the Software Setup). No separate model download step is needed.</p> <p>The optimized TensorRT engines are stored in the Riva Docker volume and persist across container restarts.</p>"},{"location":"getting-started/quickstart/","title":"Quick Start","text":"<p>Get Spot responding to voice commands in 5 minutes. This assumes all software and models are already installed.</p>"},{"location":"getting-started/quickstart/#prerequisites-checklist","title":"Prerequisites Checklist","text":"<p>Before starting, confirm:</p> <ul> <li>[ ] Spot is powered on (fans running, not e-stopped from tablet)</li> <li>[ ] Jetson is connected to Spot's WiFi (<code>ping 192.168.80.3</code> succeeds)</li> <li>[ ] <code>.env</code> file exists with correct robot credentials</li> <li>[ ] Python venv is set up with all dependencies installed</li> <li>[ ] Riva Docker container has been initialized (<code>riva_init.sh</code> run at least once)</li> <li>[ ] Ollama models are pulled (<code>ollama list</code> shows qwen2.5:7b and qwen2.5vl:7b)</li> <li>[ ] TTS and KWS models are downloaded (<code>models/tts/</code> and <code>models/kws/</code> exist)</li> <li>[ ] Microphone (XVF3800) is plugged into Jetson USB</li> </ul>"},{"location":"getting-started/quickstart/#step-1-start-e-stop","title":"Step 1: Start E-Stop","text":"<p>Open a terminal and start the E-Stop keepalive. This must stay running the entire time you use Spot.</p> <pre><code>cd ~/spot/dartmouth_spot_capstone\nsource spot-env/bin/activate\npython scripts/estop_run.py\n</code></pre> <p>Expected output:</p> <pre><code>Starting E-Stop keepalive. Press Ctrl+C to stop (will issue STOP).\nThis will claim the E-Stop even if another client holds it.\n\n[E-Stop] Claimed and active as 'dartmouth_estop'\n</code></pre> <p>Do not close this terminal</p> <p>If the E-Stop process exits, the robot will stop all movement within 3 seconds. Keep this terminal open and running.</p> <p>Alternative: Use the web panel for E-Stop control from your phone:</p> <pre><code>python scripts/web_panel.py\n</code></pre> <p>Then open <code>http://&lt;jetson-ip&gt;:8080</code> on your phone or laptop.</p>"},{"location":"getting-started/quickstart/#step-2-start-voice-control","title":"Step 2: Start Voice Control","text":"<p>Open a second terminal:</p> <pre><code>cd ~/spot/dartmouth_spot_capstone\nsource spot-env/bin/activate\npython scripts/run_voice_control.py\n</code></pre> <p>The startup sequence takes 30-60 seconds. Here is what happens:</p> <ol> <li>Riva check -- verifies the ASR Docker container is running (starts it if stopped)</li> <li>Ollama check -- verifies the LLM service is running</li> <li>ASR bridge -- starts the gRPC proxy server on port 50055</li> <li>Noise calibration -- records 2 seconds of silence to set the energy threshold</li> <li>LLM warm-up -- sends a throwaway prompt to pre-load the model into VRAM (~10-15s on cold start)</li> <li>Wake word init -- loads the sherpa-onnx keyword spotter</li> <li>YOLO preload -- begins loading object detection models in the background</li> </ol> <p>When you see this, the system is ready:</p> <pre><code>============================================================\nLISTENING -- Mode: LLM Brain (qwen2.5:7b)\n  Wake word: ON (sherpa-onnx keyword spotter)\n  Safety commands (stop/freeze/estop) always instant\n  Everything else goes through the LLM brain\n============================================================\n</code></pre>"},{"location":"getting-started/quickstart/#step-3-talk-to-spot","title":"Step 3: Talk to Spot","text":"<p>Say \"Hey Spot\" clearly into the microphone. You will hear a confirmation beep. Then give a command:</p> <ul> <li>\"Stand up\" -- Spot stands from sitting position</li> <li>\"Walk forward\" -- Spot walks forward 1 meter</li> <li>\"What do you see?\" -- Spot takes a photo and describes the scene</li> <li>\"Sit down\" -- Spot sits back down</li> </ul> <p>You can also say the wake word and command in one breath: \"Hey Spot, stand up\".</p>"},{"location":"getting-started/quickstart/#example-session","title":"Example Session","text":"<p>Here is what a typical session looks like in the terminal:</p> <pre><code>[Say 'Hey Spot' to activate...]\n&gt;&gt;&gt; Wake word detected!\n\n&gt;&gt;&gt; Speech detected...\n    Recording: 0.5s\n\n&gt;&gt;&gt; Processing...\n[Sending 0.8s to ASR...]\n[Timing] ASR: 0.34s\n\n============================================================\nHEARD: \"Stand up\"\n============================================================\n[Brain] Actions: stand\n[Timing] LLM: 0.82s\n\nSPOT: \"Getting up now!\"\n&gt;&gt;&gt; SUCCESS\n[Timing] Total: 1.43s\n\n[Listening...]\n\n&gt;&gt;&gt; Speech detected...\n    Recording: 1.2s\n\n&gt;&gt;&gt; Processing...\n[Sending 1.4s to ASR...]\n[Timing] ASR: 0.41s\n\n============================================================\nHEARD: \"What do you see in front of you?\"\n============================================================\n[Brain] Actions: describe\n[Timing] LLM: 1.12s\n\nSPOT: \"Let me take a look...\"\n[Brain] Querying VLM (qwen2.5vl:7b)...\n[Brain] VLM responded in 4.2s\n\nSPOT (VLM): \"I can see a long hallway with fluorescent lighting. There are\nseveral doors on both sides and a whiteboard mounted on the wall to my left.\"\n[Timing] Total: 6.15s\n</code></pre>"},{"location":"getting-started/quickstart/#common-options","title":"Common Options","text":"<pre><code># Skip wake word (always listening, no \"Hey Spot\" needed)\npython scripts/run_voice_control.py --no-wake-word\n\n# Disable TTS (silent mode, text-only output)\npython scripts/run_voice_control.py --no-tts\n\n# Disable LLM (regex-only command parsing, no conversation)\npython scripts/run_voice_control.py --no-brain\n\n# Use a different microphone device\npython scripts/run_voice_control.py --device 10\n\n# Debug audio levels (shows RMS/threshold for mic troubleshooting)\npython scripts/run_voice_control.py --debug-audio\n\n# Skip auto-starting Riva/Ollama (if you manage them manually)\npython scripts/run_voice_control.py --skip-services\n</code></pre>"},{"location":"getting-started/quickstart/#stopping","title":"Stopping","text":"<ol> <li>Press Ctrl+C in the voice control terminal. Spot will sit down and power off.</li> <li>Press Ctrl+C in the E-Stop terminal. The E-Stop will be released.</li> </ol> <p>To emergency stop at any time, say \"stop\", \"freeze\", or \"e-stop\". These safety commands bypass the LLM and execute immediately, even without the wake word.</p>"},{"location":"getting-started/software/","title":"Software Setup","text":"<p>Complete software setup for a fresh Jetson. Follow these steps in order.</p>"},{"location":"getting-started/software/#1-install-docker-and-nvidia-container-toolkit","title":"1. Install Docker and NVIDIA Container Toolkit","text":"<p>Riva ASR runs as a Docker container with GPU access. JetPack 6.2.1 may include Docker pre-installed, but the NVIDIA container runtime often needs manual setup.</p> <pre><code># Install Docker (skip if 'docker --version' already works)\nsudo apt-get update\nsudo apt-get install -y docker.io\n\n# Install NVIDIA Container Toolkit (required for GPU access in Docker)\nsudo apt-get install -y nvidia-container-toolkit\nsudo systemctl restart docker\n\n# Allow running Docker without sudo\nsudo usermod -aG docker $USER\n</code></pre> <p>Log out and back in</p> <p>The <code>usermod</code> group change requires a new login session. Log out and SSH back in before continuing. Verify with <code>groups | grep docker</code>.</p> <p>Verify Docker can access the GPU:</p> <pre><code>docker run --rm --runtime=nvidia --gpus all nvidia/cuda:12.6.0-base-ubuntu22.04 nvidia-smi\n</code></pre> <p>If you see GPU info, Docker is ready. If you get <code>could not select device driver \"nvidia\"</code>, the nvidia-container-toolkit is not installed correctly \u2014 re-run <code>sudo apt-get install -y nvidia-container-toolkit &amp;&amp; sudo systemctl restart docker</code>.</p>"},{"location":"getting-started/software/#2-clone-the-repository","title":"2. Clone the Repository","text":"<pre><code>cd ~/spot\ngit clone https://github.com/matteo-corrado/dartmouth_spot_capstone.git\ncd dartmouth_spot_capstone\ngit checkout feature/llm-brain\n</code></pre>"},{"location":"getting-started/software/#3-environment-variables","title":"3. Environment Variables","text":"<p>Create a <code>.env</code> file in the project root with your Spot robot credentials:</p> <pre><code>cat &gt; .env &lt;&lt; 'EOF'\nBOSDYN_CLIENT_USERNAME=your_username_here\nBOSDYN_CLIENT_PASSWORD=your_password_here\nBOSDYN_ROBOT_IP=192.168.80.3\nEOF\n</code></pre> <p>Do not commit <code>.env</code></p> <p>The <code>.env</code> file contains robot credentials and is listed in <code>.gitignore</code>. Never commit it. Ask a team member for the actual values.</p> <p>The IP defaults to <code>192.168.80.3</code> if not set. Username and password must match what is configured in Spot's admin console (<code>https://192.168.80.3</code>).</p>"},{"location":"getting-started/software/#4-python-virtual-environment","title":"4. Python Virtual Environment","text":"<pre><code>python3 -m venv spot-env\nsource spot-env/bin/activate\n</code></pre> <p>You must activate this venv in every terminal session before running any project scripts.</p>"},{"location":"getting-started/software/#5-install-pytorch","title":"5. Install PyTorch","text":"<p>PyTorch must be installed from NVIDIA's JetPack 6 wheel index (standard PyPI wheels do not work on aarch64 Jetson):</p> <pre><code>pip install --upgrade pip\npip install torch torchvision --extra-index-url https://developer.download.nvidia.com/compute/redist/jp/v61\n</code></pre> <p>Verify:</p> <pre><code>python -c \"import torch; print(torch.__version__); print('CUDA:', torch.cuda.is_available())\"\n</code></pre> <p>Expected output: a PyTorch version and <code>CUDA: True</code>.</p> <p>PyTorch version</p> <p>The exact version depends on what NVIDIA publishes for your JetPack. As of JetPack 6.2.1, PyTorch 2.5.x is available. The project does not pin a specific PyTorch version -- any JP6-compatible build works.</p> <p>Install PyTorch before requirements.txt</p> <p>PyTorch must be installed first because <code>ultralytics</code> (YOLO) depends on it. If you run <code>pip install -r requirements.txt</code> without PyTorch, pip will try to install a CPU-only PyTorch from PyPI which will not work on the Jetson.</p>"},{"location":"getting-started/software/#6-install-python-dependencies","title":"6. Install Python Dependencies","text":"<pre><code>pip install -r requirements.txt\n</code></pre> <p>This installs:</p> <ul> <li><code>bosdyn-client</code>, <code>bosdyn-mission</code>, <code>bosdyn-choreography-client</code> (v5.0.1.1) -- Spot SDK</li> <li><code>nvidia-riva-client</code> -- Riva ASR Python client</li> <li><code>sounddevice</code>, <code>webrtcvad</code> -- audio capture and voice activity detection</li> <li><code>grpcio</code>, <code>grpcio-tools</code> -- gRPC for ASR bridge</li> <li><code>sherpa-onnx</code> -- Kokoro TTS and wake word (bundles its own ONNX runtime for aarch64)</li> <li><code>ultralytics</code> -- YOLO object detection</li> <li><code>python-dotenv</code>, <code>numpy</code>, <code>requests</code> -- utilities</li> </ul>"},{"location":"getting-started/software/#7-nvidia-riva-asr-docker","title":"7. NVIDIA Riva ASR (Docker)","text":"<p>Riva provides the speech-to-text engine. It runs as a Docker container with the Canary-Qwen-2.5B ASR model.</p>"},{"location":"getting-started/software/#first-time-setup","title":"First-Time Setup","text":"<pre><code>chmod +x scripts/setup_riva.sh\n./scripts/setup_riva.sh\n</code></pre> <p>This downloads the Riva quickstart scripts and configures ASR-only mode. Then initialize the models (downloads ~5GB and runs TensorRT optimization -- takes 15-30 minutes on first run):</p> <pre><code>cd ~/riva_quickstart &amp;&amp; bash riva_init.sh\n</code></pre>"},{"location":"getting-started/software/#starting-riva","title":"Starting Riva","text":"<pre><code>./scripts/setup_riva.sh start\n</code></pre> <p>Or manually:</p> <pre><code>cd ~/riva_quickstart &amp;&amp; bash riva_start.sh\n</code></pre>"},{"location":"getting-started/software/#verify-riva","title":"Verify Riva","text":"<pre><code>./scripts/setup_riva.sh test\n</code></pre> <p>Or check the Docker container:</p> <pre><code>docker ps | grep riva\n# Should show riva-speech container running\n</code></pre> <p>Riva starts automatically</p> <p><code>run_voice_control.py</code> checks if the Riva container is running and starts it automatically. You only need to do the first-time setup manually.</p>"},{"location":"getting-started/software/#8-ollama-llm-vlm","title":"8. Ollama (LLM / VLM)","text":"<p>Ollama serves the text LLM and vision-language model for Spot's brain.</p>"},{"location":"getting-started/software/#install-ollama","title":"Install Ollama","text":"<pre><code>curl -fsSL https://ollama.com/install.sh | sh\n</code></pre> <p>Ollama installs as a systemd service and starts automatically. Enable it so it persists across reboots:</p> <pre><code>sudo systemctl enable ollama\n</code></pre>"},{"location":"getting-started/software/#pull-models","title":"Pull Models","text":"<pre><code>ollama pull qwen2.5:7b       # Text LLM (~4.7GB)\nollama pull qwen2.5vl:7b     # Vision-Language Model (~4.7GB)\n</code></pre>"},{"location":"getting-started/software/#verify-ollama","title":"Verify Ollama","text":"<pre><code># Check service is running\nsystemctl status ollama\n\n# Test inference\nollama run qwen2.5:7b \"Say hello in one sentence\"\n</code></pre> <p>VRAM sharing</p> <p>The text LLM and VLM share GPU memory. Ollama swaps models in and out as needed. The first request after a swap takes 5-15 seconds (model loading). Subsequent requests are fast (~1-2s).</p>"},{"location":"getting-started/software/#9-download-models","title":"9. Download Models","text":"<p>TTS and wake word models must be downloaded separately:</p> <pre><code>python scripts/setup_kokoro.py    # Kokoro TTS (~340MB)\npython scripts/setup_kws.py       # Wake word KWS (~5MB)\n</code></pre> <p>See Model Downloads for details.</p>"},{"location":"getting-started/software/#verify-everything","title":"Verify Everything","text":"<p>Run these checks to confirm the full stack is working:</p> <pre><code># 1. Spot connection\npython -c \"\nfrom src.config import BOSDYN_ROBOT_IP, BOSDYN_CLIENT_USERNAME, BOSDYN_CLIENT_PASSWORD\nfrom bosdyn.client import create_standard_sdk\nsdk = create_standard_sdk('test')\nrobot = sdk.create_robot(BOSDYN_ROBOT_IP)\nrobot.authenticate(BOSDYN_CLIENT_USERNAME, BOSDYN_CLIENT_PASSWORD)\nprint('Spot: OK')\n\"\n\n# 2. Riva ASR\n./scripts/setup_riva.sh test\n\n# 3. Ollama\nollama list\n# Should show qwen2.5:7b and qwen2.5vl:7b\n\n# 4. TTS\npython src/voice_control/spot_tts.py\n# Should speak a test sentence\n\n# 5. Wake word\npython scripts/setup_kws.py --check\n# Should report \"KWS model OK\"\n\n# 6. Microphone\npython src/voice_control/client_mic.py --list-devices\n# Should list audio devices including XVF3800\n</code></pre> <p>If all checks pass, proceed to the Quick Start.</p>"},{"location":"getting-started/tailscale/","title":"Remote Access with Tailscale","text":"<p>Tailscale provides secure SSH access to the Jetson from anywhere \u2014 on campus, at home, or on the go. It creates a private mesh VPN so you can connect without port forwarding or firewall rules.</p>"},{"location":"getting-started/tailscale/#why-tailscale","title":"Why Tailscale?","text":"<p>The Jetson sits on Spot's WiFi network (<code>192.168.80.x</code>), which is a private network with no internet access. Without Tailscale, you can only reach the Jetson when physically connected to the same network. Tailscale gives the Jetson a stable IP on a private overlay network (the \"tailnet\") that works from anywhere with internet.</p> <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510     Tailscale      \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510     Spot WiFi     \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  Your       \u2502 \u25c4\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba \u2502  Jetson AGX Orin  \u2502 \u25c4\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba \u2502  Spot Robot \u2502\n\u2502  Laptop     \u2502   (100.x.x.x)    \u2502  (192.168.80.x)   \u2502   (192.168.80.3)  \u2502             \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518                   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518                   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>The Jetson connects to both networks simultaneously: Spot's WiFi for robot control, and Tailscale (over Ethernet or a second WiFi adapter) for remote access.</p>"},{"location":"getting-started/tailscale/#install-tailscale-on-the-jetson","title":"Install Tailscale on the Jetson","text":"<pre><code># Install Tailscale (one-line installer)\ncurl -fsSL https://tailscale.com/install.sh | sh\n\n# Start Tailscale and authenticate\nsudo tailscale up\n\n# Follow the printed URL to log in with your Tailscale account\n# The Jetson will appear in your tailnet as \"jetson-agx-orin\" (or the hostname)\n</code></pre> <p>After authentication, verify:</p> <pre><code>tailscale ip -4\n# Prints something like: 100.64.0.5\n</code></pre> <p>This <code>100.x.x.x</code> address is your Jetson's Tailscale IP. It stays the same even if the Jetson's local IP changes.</p> <p>Tailnet account</p> <p>Ask a team member which Tailscale account (tailnet) to use. Everyone on the team should join the same tailnet to access the Jetson.</p>"},{"location":"getting-started/tailscale/#enable-tailscale-ssh","title":"Enable Tailscale SSH","text":"<p>Tailscale SSH lets you connect without managing SSH keys:</p> <pre><code>sudo tailscale up --ssh\n</code></pre> <p>This enables Tailscale's built-in SSH server. You can now SSH into the Jetson from any machine on the same tailnet.</p>"},{"location":"getting-started/tailscale/#connect-from-your-laptop","title":"Connect from Your Laptop","text":""},{"location":"getting-started/tailscale/#1-install-tailscale-on-your-machine","title":"1. Install Tailscale on your machine","text":"<p>Download from tailscale.com/download and log in to the same tailnet.</p>"},{"location":"getting-started/tailscale/#2-ssh-into-the-jetson","title":"2. SSH into the Jetson","text":"<pre><code># Using Tailscale IP\nssh spotdog@100.x.x.x\n\n# Or using the Tailscale hostname (check your admin console)\nssh spotdog@jetson-agx-orin\n</code></pre> <p>If you enabled Tailscale SSH (<code>--ssh</code>), no password or key is needed \u2014 Tailscale handles authentication.</p>"},{"location":"getting-started/tailscale/#3-verify-you-can-reach-spot-through-the-jetson","title":"3. Verify you can reach Spot through the Jetson","text":"<p>Once SSHed in, confirm the Jetson can still talk to Spot:</p> <pre><code>ping 192.168.80.3\n</code></pre>"},{"location":"getting-started/tailscale/#access-the-web-panel-remotely","title":"Access the Web Panel Remotely","text":"<p>The web panel (<code>scripts/web_panel.py</code>) binds to <code>0.0.0.0:8080</code>, so it is accessible via the Jetson's Tailscale IP:</p> <pre><code>http://100.x.x.x:8080\n</code></pre> <p>Open this URL on your phone or laptop (as long as you are on the same tailnet) to control E-Stop and the voice pipeline remotely.</p> <p>No authentication on the web panel</p> <p>The web panel has no password protection. Anyone on your tailnet can trigger E-Stop or start/stop the voice pipeline. This is acceptable for a private tailnet but keep the tailnet membership restricted.</p>"},{"location":"getting-started/tailscale/#ensure-tailscale-starts-on-boot","title":"Ensure Tailscale Starts on Boot","text":"<p>Tailscale installs as a systemd service and starts automatically. Verify:</p> <pre><code>sudo systemctl is-enabled tailscaled\n# Should print: enabled\n\n# If not:\nsudo systemctl enable tailscaled\n</code></pre> <p>After a Jetson reboot, Tailscale reconnects automatically within 10-20 seconds. You do not need to run <code>tailscale up</code> again.</p>"},{"location":"getting-started/tailscale/#internet-access-on-the-jetson","title":"Internet Access on the Jetson","text":"<p>The Jetson needs internet for:</p> <ul> <li>Pulling Ollama models (<code>ollama pull</code>)</li> <li>Downloading YOLO weights (first run)</li> <li>Tailscale connectivity</li> <li><code>apt</code> updates and package installs</li> </ul> <p>Spot's WiFi does not provide internet. You need a second network connection:</p> Method Setup Ethernet (recommended) Plug an Ethernet cable from a campus network port into the Jetson USB WiFi adapter Add a second WiFi interface for campus/home WiFi USB tethering Share your phone's internet via USB <p>Tailscale handles routing automatically \u2014 traffic to <code>192.168.80.x</code> goes over Spot's WiFi, everything else goes over the internet-connected interface.</p>"},{"location":"getting-started/tailscale/#troubleshooting","title":"Troubleshooting","text":"Problem Fix <code>tailscale up</code> hangs Check internet connectivity: <code>ping 8.8.8.8</code>. Tailscale needs internet to authenticate. Can't SSH after reboot Wait 20 seconds for Tailscale to reconnect, then retry. Check <code>sudo systemctl status tailscaled</code>. Tailscale IP changed Run <code>tailscale ip -4</code> on the Jetson. IPs should be stable, but check if the node was removed/re-added. Web panel unreachable via Tailscale Verify <code>web_panel.py</code> is running. Try <code>curl http://localhost:8080</code> on the Jetson first. Spot unreachable from Jetson Tailscale shouldn't affect local routing. Check <code>ip route</code> and confirm <code>192.168.80.0/24</code> still routes over the Spot WiFi interface."},{"location":"guides/adding-commands/","title":"Adding New Voice Commands","text":"<p>How to add new voice commands to the system. There are two layers: the LLM brain (primary) and the regex parser (fallback).</p>"},{"location":"guides/adding-commands/#architecture-overview","title":"Architecture Overview","text":"<p>When a user speaks a command, it flows through:</p> <ol> <li>Safety regex (<code>client_mic.py</code>) \u2014 instant match for stop/freeze/estop</li> <li>LLM Brain (<code>llm_brain.py</code>) \u2014 structured JSON output with actions + response</li> <li>Regex fallback (<code>intent.py</code>) \u2014 used in <code>--no-brain</code> mode or as LLM backup</li> </ol> <p>To add a new command, you typically modify all three layers.</p>"},{"location":"guides/adding-commands/#step-1-add-to-the-llm-system-prompt","title":"Step 1: Add to the LLM System Prompt","text":"<p>Edit <code>src/voice_control/llm_brain.py</code>, find the <code>SYSTEM_PROMPT</code> string.</p>"},{"location":"guides/adding-commands/#add-the-action-definition","title":"Add the action definition","text":"<p>In the <code>AVAILABLE ACTIONS:</code> section, add a line:</p> <pre><code>- my_action: Description of what it does. Params: {\"param1\": \"&lt;type&gt;\", \"param2\": &lt;type&gt;}.\n</code></pre>"},{"location":"guides/adding-commands/#add-a-non-obvious-example-optional","title":"Add a non-obvious example (optional)","text":"<p>Only add an example if the mapping is ambiguous. Simple commands like \"do X\" \u2192 <code>my_action</code> don't need examples.</p> <p>Add examples for: - Commands where multiple actions could match - Commands with unusual parameter extraction - Commands that differ from similar-sounding ones</p>"},{"location":"guides/adding-commands/#add-rules-if-needed","title":"Add rules (if needed)","text":"<p>If there's disambiguation logic (like go_to vs go_to_object), add a RULES entry.</p>"},{"location":"guides/adding-commands/#step-2-add-to-the-regex-parser","title":"Step 2: Add to the Regex Parser","text":"<p>Edit <code>src/voice_control/intent.py</code>, add a pattern to the <code>COMMANDS</code> list:</p> <pre><code>COMMANDS = [\n    # ... existing patterns ...\n\n    # === MY NEW COMMAND ===\n    (r\"\\b(?:do something|activate widget)\\s+(?P&lt;target&gt;.+?)(?:\\s*\\.|$)\",\n     \"my_action\", {\"target\": \"$target\"}),\n]\n</code></pre> <p>Pattern syntax: - Use <code>\\b</code> word boundaries - Use <code>(?:...)</code> non-capturing groups for alternatives - Use <code>(?P&lt;name&gt;...)</code> named groups for parameter extraction - Use <code>$name</code> in params to reference captured groups</p> <p>The parser returns <code>{\"intent\": \"my_action\", \"params\": {\"target\": \"...\"}, \"raw\": \"original text\"}</code>.</p>"},{"location":"guides/adding-commands/#step-3-add-the-executor","title":"Step 3: Add the Executor","text":"<p>Edit <code>src/voice_control/spot_dispatch.py</code>, find the <code>dispatch_intent()</code> function.</p> <p>Add a new case:</p> <pre><code>def dispatch_intent(intent):\n    cmd = intent[\"intent\"]\n    params = intent.get(\"params\", {})\n\n    # ... existing cases ...\n\n    elif cmd == \"my_action\":\n        target = params.get(\"target\", \"default\")\n        return _execute_my_action(target)\n</code></pre> <p>Then implement the handler function:</p> <pre><code>def _execute_my_action(target):\n    \"\"\"Execute my custom action.\"\"\"\n    session = ensure_spot_session()\n    if not session:\n        return False\n\n    # Use BD SDK to control Spot\n    robot = session[\"robot\"]\n    cmd_client = session[\"cmd\"]\n\n    # ... your implementation ...\n    return True\n</code></pre>"},{"location":"guides/adding-commands/#step-4-test","title":"Step 4: Test","text":""},{"location":"guides/adding-commands/#test-llm-understanding-no-robot-needed","title":"Test LLM understanding (no robot needed)","text":"<pre><code>python src/voice_control/llm_brain.py\n</code></pre> <p>Type your command and verify the JSON output contains the correct action and params.</p>"},{"location":"guides/adding-commands/#test-regex-parsing","title":"Test regex parsing","text":"<pre><code>from src.voice_control.intent import parse_intent\nresult = parse_intent(\"do something with the widget\")\nprint(result)\n# Should output: {\"intent\": \"my_action\", \"params\": {\"target\": \"widget\"}, \"raw\": \"...\"}\n</code></pre>"},{"location":"guides/adding-commands/#test-full-pipeline","title":"Test full pipeline","text":"<pre><code>python scripts/run_voice_control.py\n</code></pre> <p>Say the command and verify it executes correctly.</p>"},{"location":"guides/adding-commands/#example-adding-a-spin-command","title":"Example: Adding a \"spin\" Command","text":""},{"location":"guides/adding-commands/#1-llm-prompt-llm_brainpy","title":"1. LLM Prompt (llm_brain.py)","text":"<pre><code>- spin: Spin in a circle. Params: {\"speed\": \"slow\"|\"fast\"}. Default \"fast\".\n</code></pre>"},{"location":"guides/adding-commands/#2-regex-intentpy","title":"2. Regex (intent.py)","text":"<pre><code>(r\"\\b(?:spin|do a spin|pirouette)\\b\", \"spin\", {\"speed\": \"fast\"}),\n(r\"\\bslow spin\\b\", \"spin\", {\"speed\": \"slow\"}),\n</code></pre>"},{"location":"guides/adding-commands/#3-executor-spot_dispatchpy","title":"3. Executor (spot_dispatch.py)","text":"<pre><code>elif cmd == \"spin\":\n    speed = params.get(\"speed\", \"fast\")\n    deg_per_sec = 180 if speed == \"fast\" else 60\n    # Turn 360 degrees using existing turn logic\n    from bosdyn.client.robot_command import RobotCommandBuilder\n    cmd_client = session[\"cmd\"]\n    # ... build and send rotation command ...\n    return True\n</code></pre>"},{"location":"guides/adding-commands/#adding-a-background-threaded-command","title":"Adding a Background (Threaded) Command","text":"<p>Some commands run for an extended time \u2014 navigation, following, patrols. These run in a background thread so the voice pipeline can keep listening for \"stop\" or other commands.</p> <p>The pattern used by <code>go_to</code>, <code>follow_me</code>, and <code>tour</code>:</p>"},{"location":"guides/adding-commands/#1-thread-stop-event-in-spot_dispatchpy","title":"1. Thread + stop event in spot_dispatch.py","text":"<pre><code>import threading\n\n# Module-level globals (already exist in spot_dispatch.py)\n_nav_thread = None\n_nav_stop_event = threading.Event()\n\ndef _execute_my_long_action(target):\n    \"\"\"Background action that can be cancelled.\"\"\"\n    global _nav_thread, _nav_stop_event\n\n    # Cancel any existing background action\n    _cancel_nav()\n\n    session = ensure_spot_session()\n    if not session:\n        return False\n\n    _nav_stop_event.clear()\n\n    def _worker():\n        # Your long-running logic here\n        while not _nav_stop_event.is_set():\n            # Do work in small increments\n            # Check _nav_stop_event frequently so \"stop\" is responsive\n            time.sleep(0.5)\n\n    _nav_thread = threading.Thread(target=_worker, daemon=True)\n    _nav_thread.start()\n    return True\n</code></pre>"},{"location":"guides/adding-commands/#2-cancellation-support","title":"2. Cancellation support","text":"<p>The <code>_cancel_nav()</code> function (already in <code>spot_dispatch.py</code>) sets the stop event and waits for the thread:</p> <pre><code>def _cancel_nav():\n    global _nav_thread\n    if _nav_thread and _nav_thread.is_alive():\n        _nav_stop_event.set()\n        _nav_thread.join(timeout=5)\n    _nav_thread = None\n</code></pre> <p>When the user says \"stop\", <code>dispatch_intent</code> calls <code>_cancel_nav()</code> which signals the background thread to exit.</p>"},{"location":"guides/adding-commands/#3-key-rules-for-background-commands","title":"3. Key rules for background commands","text":"<ul> <li>Always check <code>_nav_stop_event.is_set()</code> in your worker loop (every 0.5-1s)</li> <li>Call <code>_cancel_nav()</code> at the start to cancel any prior background action</li> <li>Use <code>daemon=True</code> threads so they don't block program exit</li> <li>Store the thread in <code>_nav_thread</code> so the stop system knows about it</li> </ul>"},{"location":"guides/adding-commands/#tips","title":"Tips","text":"<ul> <li>Keep action names lowercase with underscores</li> <li>Match the intent name across all three files (prompt, regex, dispatch)</li> <li>The LLM is the primary path \u2014 regex is fallback for <code>--no-brain</code> mode</li> <li>Test the LLM interactively before adding regex patterns</li> <li>For reading robot state in your handler, use <code>session[\"state\"].get_robot_state()</code></li> <li>For capturing camera images, see <code>capture_frame()</code> in <code>spot_dispatch.py</code></li> <li>To test without a physical robot, use <code>--no-spot</code> in test scripts that support it</li> </ul>"},{"location":"guides/door-opening/","title":"Door Opening","text":"<p>Spot can open push-bar doors using its arm and the AutoPush command from the BD SDK.</p> <p>Status: On Ice</p> <p>Door opening is implemented but needs further testing with the physical robot. The <code>--vlm --depth 0.5</code> configuration has not been fully validated.</p>"},{"location":"guides/door-opening/#how-it-works","title":"How It Works","text":"<ol> <li>Calibrate \u2014 Use <code>calibrate_door.py</code> to identify the push point on the door from the front camera</li> <li>Configure \u2014 Save door parameters to <code>door_config.json</code></li> <li>Execute \u2014 Voice command \"open the door\" triggers the AutoPush sequence</li> </ol> <p>The system uses the BD SDK's <code>AutoPushCommand</code>, which takes: - A 3D push point (from camera + depth estimation) - The hinge side (left or right)</p>"},{"location":"guides/door-opening/#calibration","title":"Calibration","text":""},{"location":"guides/door-opening/#step-1-position-spot-facing-the-door","title":"Step 1: Position Spot facing the door","text":"<p>Drive Spot to stand ~1.5m in front of the door, facing it directly.</p>"},{"location":"guides/door-opening/#step-2-run-the-calibration-script","title":"Step 2: Run the calibration script","text":"<pre><code>python scripts/calibrate_door.py\n</code></pre> <p>This captures the front camera image (fisheye, rotated 90 CW for display) and lets you click on the push bar location. The pixel coordinates and hinge side are saved to <code>door_config.json</code>.</p>"},{"location":"guides/door-opening/#step-3-verify-the-config","title":"Step 3: Verify the config","text":"<pre><code>{\n  \"camera_source\": \"frontleft_fisheye_image\",\n  \"handle_pixel_x_rotated\": 140.0,\n  \"handle_pixel_y_rotated\": 165.0,\n  \"hinge_side\": \"left\"\n}\n</code></pre>"},{"location":"guides/door-opening/#testing","title":"Testing","text":""},{"location":"guides/door-opening/#standalone-test-no-voice-control","title":"Standalone test (no voice control)","text":"<pre><code>python scripts/test_door_standalone.py\n</code></pre> <p>Options: <pre><code>python scripts/test_door_standalone.py --vlm          # Use VLM to find push point\npython scripts/test_door_standalone.py --depth 0.5    # Override depth estimate (meters)\npython scripts/test_door_standalone.py --vlm --depth 0.5  # Recommended next test\n</code></pre></p>"},{"location":"guides/door-opening/#via-voice-command","title":"Via voice command","text":"<pre><code>python scripts/run_voice_control.py\n</code></pre> <p>Say: \"Hey Spot, open the door\"</p>"},{"location":"guides/door-opening/#fisheye-coordinate-system","title":"Fisheye Coordinate System","text":"<p>Spot's fisheye cameras are mounted sideways. For door calibration:</p> <ul> <li>Display: Images rotated 90 CW for upright viewing</li> <li>API calls: Coordinates must be un-rotated back to the original frame</li> <li>Formula: <code>orig_x = rot_y</code>, <code>orig_y = H_orig - 1 - rot_x</code> (use <code>raw_rows</code>, not <code>raw_cols</code>)</li> </ul>"},{"location":"guides/door-opening/#door-service-autowalk-integration","title":"Door Service (AutoWalk Integration)","text":"<p>For AutoWalk missions, the door service runs as a Remote Mission Service:</p> <pre><code>src/door_service/door_mission_service.py\n</code></pre> <p>This is a gRPC service that Spot calls at configured door waypoints during mission playback. See the POST_FLASH_MIGRATION guide for setup details.</p>"},{"location":"guides/door-opening/#supported-door-types","title":"Supported Door Types","text":"Type Method Status Push-bar doors AutoPushCommand Implemented, needs testing Handle doors Grasp + push/pull Implemented in door_mission_service, not tested Automatic doors Button press Implemented in door_mission_service, not tested"},{"location":"guides/mapping/","title":"Mapping Guide","text":"<p>How to record, download, and manage GraphNav maps for Spot navigation.</p>"},{"location":"guides/mapping/#overview","title":"Overview","text":"<p>Spot uses Boston Dynamics' GraphNav for autonomous navigation. The workflow is:</p> <ol> <li>Record a map on the tablet (walk Spot through the space)</li> <li>Download the map to the Jetson</li> <li>Upload the map to Spot's GraphNav memory</li> <li>Localize the robot on the map</li> <li>Use voice commands to navigate</li> </ol>"},{"location":"guides/mapping/#step-1-record-a-map-tablet","title":"Step 1: Record a Map (Tablet)","text":"<ol> <li>Open Spot Controller app on the tablet</li> <li>Tap Menu &gt; Autowalk &gt; Record a Mission</li> <li>Drive Spot slowly through all areas you want mapped</li> <li>At key locations, tap \"+\" to add a waypoint and give it a short name:<ul> <li>Use lowercase, no spaces: <code>kitchen</code>, <code>lab_door</code>, <code>lobby_entrance</code></li> <li>Avoid generic names: <code>waypoint_1</code>, <code>spot</code>, <code>here</code></li> </ul> </li> <li>Return to the starting point to close the loop</li> <li>Tap Stop Recording &gt; Save</li> </ol> <p>Tips for Better Maps</p> <ul> <li>Drive slowly for better visual data</li> <li>Create loops (return to start) for alternative routes</li> <li>Place AprilTag fiducials on walls for auto-localization</li> <li>Record in good lighting</li> <li>Mark stairs/ramps during recording</li> </ul>"},{"location":"guides/mapping/#step-2-download-map-to-jetson","title":"Step 2: Download Map to Jetson","text":"<pre><code>cd ~/dartmouth_spot_capstone\nsource spot-env/bin/activate\n\npython scripts/download_map_from_spot.py --output maps/my_map\n</code></pre> <p>This downloads the graph, waypoint snapshots, and edge snapshots. It also auto-extracts waypoint names to <code>locations.json</code>.</p>"},{"location":"guides/mapping/#step-3-upload-map-to-graphnav","title":"Step 3: Upload Map to GraphNav","text":"<pre><code>python scripts/setup_map.py --map-path maps/my_map\n</code></pre> <p>This uploads the map to Spot's active GraphNav memory. The tablet recording saves to disk, but GraphNav memory is separate \u2014 this step is required.</p>"},{"location":"guides/mapping/#step-4-localize-the-robot","title":"Step 4: Localize the Robot","text":"<p>The robot needs to know where it is on the map before navigating.</p>"},{"location":"guides/mapping/#option-a-fiducial-based-automatic","title":"Option A: Fiducial-based (automatic)","text":"<p>Place Spot where it can see an AprilTag from the map. Run:</p> <pre><code>python scripts/setup_map.py --map-path maps/my_map\n</code></pre> <p>The <code>setup_map.py</code> script attempts fiducial localization by default.</p>"},{"location":"guides/mapping/#option-b-waypoint-based","title":"Option B: Waypoint-based","text":"<p>Drive Spot to a known waypoint, then:</p> <pre><code>python scripts/setup_map.py --map-path maps/my_map --waypoint-init\n</code></pre>"},{"location":"guides/mapping/#option-c-tablet","title":"Option C: Tablet","text":"<ol> <li>In Autowalk, open your map</li> <li>Drag the robot icon to Spot's current position</li> <li>Rotate to match orientation</li> <li>Tap Set Location</li> </ol>"},{"location":"guides/mapping/#step-5-navigate-with-voice","title":"Step 5: Navigate with Voice","text":"<pre><code>python scripts/run_voice_control.py\n</code></pre> <p>Say: \"Hey Spot, go to kitchen\"</p>"},{"location":"guides/mapping/#managing-locations","title":"Managing Locations","text":""},{"location":"guides/mapping/#list-all-waypoints","title":"List all waypoints","text":"<pre><code>python scripts/map_waypoints.py --list\n</code></pre>"},{"location":"guides/mapping/#add-a-location-via-voice","title":"Add a location via voice","text":"<p>While voice control is running, drive Spot to a new position and say:</p> <p>\"Save this location as front desk\"</p> <p>This saves the current waypoint to <code>locations.json</code>.</p>"},{"location":"guides/mapping/#edit-locations-manually","title":"Edit locations manually","text":"<p>Edit <code>locations.json</code> in the project root:</p> <pre><code>{\n  \"kitchen\": \"waypoint_abc123...\",\n  \"lab\": \"waypoint_def456...\",\n  \"new_location\": \"waypoint_ghi789...\"\n}\n</code></pre> <p>Find waypoint IDs with <code>python scripts/map_waypoints.py --list</code>.</p>"},{"location":"guides/mapping/#map-directory-structure","title":"Map Directory Structure","text":"<pre><code>maps/\n  my_map/\n    graph                  # Map topology\n    waypoint_snapshots/    # Visual data at each waypoint\n    edge_snapshots/        # Visual data along paths\n</code></pre> <p>Maps are gitignored (robot-specific). Each Jetson downloads its own map from Spot.</p>"},{"location":"guides/mapping/#daily-usage-map-already-exists","title":"Daily Usage (Map Already Exists)","text":"<pre><code># 1. Localize robot (tablet or script)\n# 2. Start voice control\npython scripts/run_voice_control.py\n# 3. Give navigation commands\n</code></pre> <p>No need to re-upload the map unless it has changed.</p>"},{"location":"guides/troubleshooting/","title":"Troubleshooting","text":"<p>Common issues and fixes.</p>"},{"location":"guides/troubleshooting/#services","title":"Services","text":"Problem Fix <code>Riva server NOT detected on port 50051</code> Run <code>./scripts/setup_riva.sh start</code> <code>Cannot connect to Ollama</code> Run <code>sudo systemctl start ollama</code> <code>Kokoro model files not found</code> Run <code>python scripts/setup_kokoro.py</code> <code>KWS model not found</code> Run <code>python scripts/setup_kws.py</code> <code>ModuleNotFoundError</code> Activate venv: <code>source spot-env/bin/activate</code> <code>docker: Error response from daemon: could not select device driver \"nvidia\"</code> Install nvidia-container-toolkit: <code>sudo apt-get install -y nvidia-container-toolkit &amp;&amp; sudo systemctl restart docker</code> Riva takes 60-90s to start Normal \u2014 Docker container initialization + TensorRT model loading. Wait for port 50051 to accept connections. Ollama not running after reboot Enable persistence: <code>sudo systemctl enable ollama</code>"},{"location":"guides/troubleshooting/#robot-connection","title":"Robot Connection","text":"Problem Fix <code>Could not connect to robot</code> Check network, verify <code>ping 192.168.80.3</code> Robot credentials error Check <code>.env</code> has correct BOSDYN_CLIENT_USERNAME and BOSDYN_CLIENT_PASSWORD <code>E-Stop error</code> / robot won't move Ensure <code>python scripts/estop_run.py</code> is running in a separate terminal Robot stops unexpectedly E-Stop process may have died \u2014 restart it <code>KeepaliveMotorsOff</code> error Stale keepalive policy from a crashed session. The code auto-clears these, but if persistent: restart Spot or use <code>remove_all_policies()</code> (see SDK Primer) <code>Timeout waiting for time sync</code> Network issue between Jetson and Spot. Verify <code>ping 192.168.80.3</code>, then retry Lease contention (tablet vs Jetson) The voice pipeline uses <code>take()</code> to override. If the tablet keeps stealing the lease, disconnect it from Spot's network"},{"location":"guides/troubleshooting/#navigation","title":"Navigation","text":"Problem Fix <code>Location 'X' not found</code> Check <code>locations.json</code> for exact spelling, or run <code>python scripts/map_waypoints.py --list</code> <code>No waypoints in map</code> Upload a map: <code>python scripts/setup_map.py --map-path maps/YOUR_MAP</code> <code>Not localized</code> Run <code>python scripts/setup_map.py --waypoint-init</code> or localize via tablet Robot is stuck during navigation Clear obstacles, or the robot will retry up to 2 times then skip Navigation takes a long path GraphNav uses shortest path by default (fewest edges). Re-record map with more direct paths if needed."},{"location":"guides/troubleshooting/#voice-recognition","title":"Voice Recognition","text":"Problem Fix No speech detected Check mic is connected, find device with <code>--list-devices</code> ASR gives empty/wrong results Verify Riva is running, check mic device index matches Wake word not triggering Try speaking louder/closer, or adjust KEYWORDS_THRESHOLD in wake_word.py Wake word triggers too easily Increase KEYWORDS_THRESHOLD (default 0.25, try 0.35) \"Stop\" doesn't cancel navigation Ensure you're on the latest code (safety commands bypass LLM) Commands not understood Check <code>[Brain] LLM responded</code> logs. If LLM is not running, system falls back to regex"},{"location":"guides/troubleshooting/#audio","title":"Audio","text":"Problem Fix No TTS audio output Check speaker is connected. Run <code>python src/voice_control/spot_tts.py</code> to test TTS feedback loop (echo) The mic should auto-mute during TTS playback. Check on_mute callbacks ALSA errors in logs These are suppressed by audio_feedback.py. If persistent, check audio device config Audio device not found Run <code>python -c \"import sounddevice; print(sounddevice.query_devices())\"</code> to list devices"},{"location":"guides/troubleshooting/#llmvlm","title":"LLM/VLM","text":"Problem Fix LLM very slow (&gt;10s) First request loads model (~17s). Subsequent should be 0.2-1s. Check <code>ollama list</code> VLM slow after LLM command Normal \u2014 VLM and LLM share VRAM, models swap (~2-3s). One-time per switch LLM returns wrong action Check system prompt in <code>llm_brain.py</code>. Verify saved_locations matches command JSON parse error Rare with <code>format=\"json\"</code>. Check Ollama version is up to date YOLO model downloading on first run Normal \u2014 takes 30-60s to download weights (~6-25MB). Looks like a hang but is not. Subsequent runs use cached models."},{"location":"guides/troubleshooting/#port-reference","title":"Port Reference","text":"Port Service Check Command 50051 Riva ASR (Docker) <code>curl -s localhost:50051 &amp;&amp; echo OK</code> 50055 ASR bridge (server.py) Auto-started by run_voice_control.py 11434 Ollama LLM/VLM <code>curl -s localhost:11434/api/tags</code> 8080 Web panel <code>python scripts/web_panel.py</code>"},{"location":"guides/troubleshooting/#boot-sequence-after-reboot","title":"Boot Sequence (After Reboot)","text":"<p>See Daily Operations for the full boot sequence checklist.</p> <pre><code>cd ~/spot/dartmouth_spot_capstone\nsource spot-env/bin/activate\n\n# 1. Start infrastructure\n./scripts/setup_riva.sh start\nsudo systemctl start ollama    # skip if 'systemctl enable ollama' was run\n\n# 2. Start E-Stop (separate terminal, keep open)\npython scripts/estop_run.py\n\n# 3. Upload map + localize (if not already loaded)\npython scripts/setup_map.py --map-path maps/lab_map2 --waypoint-init\n\n# 4. Start voice control\npython scripts/run_voice_control.py\n</code></pre>"},{"location":"guides/web-panel/","title":"Web Panel","text":"<p>The web panel (<code>scripts/web_panel.py</code>) is a mobile-friendly control interface for managing Spot's E-Stop and voice pipeline from a phone or laptop browser. It uses no external dependencies \u2014 just Python's built-in HTTP server.</p>"},{"location":"guides/web-panel/#starting-the-web-panel","title":"Starting the Web Panel","text":"<pre><code>cd ~/spot/dartmouth_spot_capstone\nsource spot-env/bin/activate\npython scripts/web_panel.py\n</code></pre> <p>Output:</p> <pre><code>==================================================\n  Spot Web Control Panel\n==================================================\n\n  Listening on port 8080\n  Local:     http://localhost:8080\n  Network:   http://192.168.80.2:8080\n\n  Open this URL on your phone to control Spot.\n  Press Ctrl+C to stop.\n</code></pre> <p>Open the Network URL on your phone or laptop. If using Tailscale, use <code>http://&lt;tailscale-ip&gt;:8080</code>.</p>"},{"location":"guides/web-panel/#what-it-does","title":"What It Does","text":"<p>The panel has two main sections:</p>"},{"location":"guides/web-panel/#e-stop-controls","title":"E-Stop Controls","text":"Button Action Claim Connects to Spot and registers a software E-Stop endpoint. The robot cannot move until an E-Stop is claimed. E-STOP (large red button) Immediately cuts motor power. The robot will collapse to the ground. Use in emergencies. Release Re-enables motor power after an E-Stop. The robot can move again once released. <p>Workflow: Claim first, then the robot can accept commands. Hit E-STOP if anything goes wrong. Hit Release to resume.</p>"},{"location":"guides/web-panel/#voice-pipeline-controls","title":"Voice Pipeline Controls","text":"Button Action Start Launches <code>run_voice_control.py</code> as a background process on the Jetson Stop Kills the voice control process <p>This lets you start/stop the voice pipeline without an SSH terminal.</p>"},{"location":"guides/web-panel/#status-bar","title":"Status Bar","text":"<p>Four live indicators update every 2 seconds:</p> Indicator Green Red Yellow Riva ASR Docker container running (port 50051) Container stopped \u2014 Ollama LLM service running (port 11434) Service stopped \u2014 E-Stop E-Stop claimed and active E-Stop triggered (motors cut) Not claimed Voice Voice pipeline process running Pipeline stopped \u2014"},{"location":"guides/web-panel/#log-panel","title":"Log Panel","text":"<p>A scrollable log at the bottom shows all actions and their results (last 50 entries). Resets on page refresh.</p>"},{"location":"guides/web-panel/#options","title":"Options","text":"<pre><code># Custom port\npython scripts/web_panel.py --port 9090\n\n# UI-only mode (no Spot connection, for testing the UI)\npython scripts/web_panel.py --no-spot\n</code></pre>"},{"location":"guides/web-panel/#running-as-a-background-service","title":"Running as a Background Service","text":"<p>To keep the web panel running even after disconnecting SSH:</p> <pre><code># Option 1: tmux\ntmux new -s panel\npython scripts/web_panel.py\n# Ctrl+B, D to detach\n\n# Option 2: nohup\nnohup python scripts/web_panel.py &gt; /dev/null 2&gt;&amp;1 &amp;\n</code></pre>"},{"location":"guides/web-panel/#using-the-web-panel-as-your-only-e-stop","title":"Using the Web Panel as Your Only E-Stop","text":"<p>The web panel can fully replace <code>estop_run.py</code>. Instead of keeping an SSH terminal open with <code>estop_run.py</code>, start the web panel and use the Claim button from your phone. The panel runs its own E-Stop keepalive in a background thread.</p> <p>Keep the browser tab open</p> <p>The E-Stop keepalive runs on the Jetson (server-side), not in the browser. The browser is just a control interface. If you close the browser tab, the E-Stop remains active until you either press E-STOP or kill the <code>web_panel.py</code> process. Killing the process will trigger E-Stop (motors cut).</p>"},{"location":"guides/web-panel/#security-considerations","title":"Security Considerations","text":"<p>The web panel has no authentication. Anyone who can reach port 8080 on the Jetson can control the robot. This is acceptable when:</p> <ul> <li>The Jetson is only accessible via Spot's private WiFi network</li> <li>Access is restricted to your Tailscale tailnet</li> </ul> <p>Do not expose port 8080 to the public internet.</p>"},{"location":"guides/web-panel/#architecture","title":"Architecture","text":"<pre><code>Phone/Laptop                    Jetson (web_panel.py)             Spot\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510   HTTP GET /    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502 Browser   \u2502 \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba\u2502 HTML page served   \u2502\n\u2502           \u2502                \u2502                    \u2502\n\u2502           \u2502  POST /estop/* \u2502 EStopManager       \u2502\u25c4\u2500\u2500 BD SDK \u2500\u2500\u25ba E-Stop service\n\u2502           \u2502 \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba\u2502 (keepalive thread) \u2502\n\u2502           \u2502                \u2502                    \u2502\n\u2502           \u2502 POST /voice/*  \u2502 VoicePipelineManager\u2502\u2500\u2500\u25ba subprocess: run_voice_control.py\n\u2502           \u2502 \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba\u2502                    \u2502\n\u2502           \u2502                \u2502                    \u2502\n\u2502           \u2502  GET /status   \u2502 Port checks +      \u2502\n\u2502           \u2502 \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba\u2502 manager status     \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518   (every 2s)   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"modules/audio-feedback/","title":"audio_feedback.py","text":"<p><code>src/voice_control/audio_feedback.py</code> -- Audio tone feedback.</p> <p>Short beeps and chimes (60-250ms) that give the user instant audio feedback for voice control events. Uses numpy-generated sine waves played through sounddevice. Suppresses ALSA error messages via ctypes.</p>"},{"location":"modules/audio-feedback/#singleton","title":"Singleton","text":"<pre><code>from audio_feedback import beep\n</code></pre> <p>The module exports a pre-instantiated <code>AudioFeedback</code> singleton named <code>beep</code>.</p>"},{"location":"modules/audio-feedback/#methods","title":"Methods","text":"<p>All methods are non-blocking -- each spawns a daemon thread for playback.</p>"},{"location":"modules/audio-feedback/#beepwake_detected","title":"<code>beep.wake_detected()</code>","text":"<p>Rising chime -- wake word heard. Three tones: C5 (523 Hz) -&gt; E5 (659 Hz) -&gt; G5 (784 Hz), 70ms each, total ~210ms.</p>"},{"location":"modules/audio-feedback/#beeplistening","title":"<code>beep.listening()</code>","text":"<p>Soft blip -- ready for command. Single G5 (784 Hz) tone, 80ms.</p>"},{"location":"modules/audio-feedback/#beepcommand_ok","title":"<code>beep.command_ok()</code>","text":"<p>Happy double-beep -- command accepted. C5 (523 Hz, 70ms) + C6 (1047 Hz, 60ms) with 20ms gap, total ~150ms.</p>"},{"location":"modules/audio-feedback/#beeperror","title":"<code>beep.error()</code>","text":"<p>Low buzz -- something failed. A3 (220 Hz, 120ms) -&gt; F3 (175 Hz, 120ms) descending, total ~250ms.</p>"},{"location":"modules/audio-feedback/#beepchain_next","title":"<code>beep.chain_next()</code>","text":"<p>Short tick -- next action in a multi-command chain. E5 (659 Hz), 60ms.</p>"},{"location":"modules/audio-feedback/#class","title":"Class","text":""},{"location":"modules/audio-feedback/#audiofeedbackoutput_devicenone","title":"<code>AudioFeedback(output_device=None)</code>","text":"<pre><code>class AudioFeedback:\n    def __init__(self, output_device=None)\n</code></pre> <p><code>output_device</code> is a sounddevice device index. The default singleton uses the system default output.</p>"},{"location":"modules/audio-feedback/#audio-generation","title":"Audio Generation","text":"<ul> <li>Sample rate: 48 kHz (standard ALSA rate, avoids underrun with short clips).</li> <li>Sine wave tones with fade-in/fade-out ramps to avoid clicks.</li> <li>80ms silence tail appended to each tone to let ALSA buffers drain.</li> <li>Volume range: 0.15 to 0.25 (quiet enough to not interfere with ASR).</li> </ul>"},{"location":"modules/audio-feedback/#alsa-error-suppression","title":"ALSA Error Suppression","text":"<p>At module load, ALSA's error handler is replaced with a null handler via ctypes to suppress noisy warnings like \"underrun occurred\" that clutter the terminal but are harmless for short tones.</p> <pre><code>_libasound = ctypes.cdll.LoadLibrary(\"libasound.so.2\")\n_libasound.snd_lib_error_set_handler(_c_null_handler)\n</code></pre>"},{"location":"modules/audio-feedback/#usage-example","title":"Usage Example","text":"<pre><code>from audio_feedback import beep\n\nbeep.wake_detected()   # Rising chime\nbeep.command_ok()      # Double-beep\nbeep.error()           # Low buzz\n</code></pre> <pre><code># Play all tones sequentially\npython src/voice_control/audio_feedback.py\n</code></pre>"},{"location":"modules/audio-feedback/#dependencies","title":"Dependencies","text":"<p>Project modules: none (standalone)</p> <p>External packages: <code>sounddevice</code>, <code>numpy</code>, <code>ctypes</code> (stdlib)</p>"},{"location":"modules/client-mic/","title":"client_mic.py","text":"<p><code>src/voice_control/client_mic.py</code> -- Main voice control loop.</p> <p>Captures audio from the microphone, runs VAD to detect speech, sends audio to ASR, and routes the transcript through the LLM brain (or regex fallback) for action dispatch on Spot. Safety commands (stop/freeze/estop) bypass the LLM for zero-latency execution.</p>"},{"location":"modules/client-mic/#state-machine","title":"State Machine","text":"<pre><code>VoiceState.WAKE_WORD  --&gt;  VoiceState.LISTENING  --&gt;  VoiceState.RECORDING\n       |                         |                          |\n  (wake word detected)    (VAD speech onset)        (silence timeout)\n                                                     --&gt; process_utterance()\n</code></pre> <ul> <li>WAKE_WORD: Waiting for \"Hey Spot\". Only safety commands execute.</li> <li>LISTENING: Wake word heard, waiting for speech onset. Times out after   <code>LISTENING_TIMEOUT</code> (300s) and returns to WAKE_WORD.</li> <li>RECORDING: Speech detected, accumulating audio frames until silence   timeout or max duration.</li> </ul>"},{"location":"modules/client-mic/#key-functions","title":"Key Functions","text":""},{"location":"modules/client-mic/#main","title":"<code>main()</code>","text":"<p>CLI entry point. Parses arguments, initializes all services (ASR, LLM brain, TTS, wake word detector, YOLO), opens the audio stream, and runs the main event loop.</p> <pre><code>Args (argparse):\n    --list-devices     List audio devices and exit\n    --device INT       Audio device index (default: 24, XVF3800)\n    --no-brain         Disable LLM brain, use regex-only mode\n    --no-tts           Disable text-to-speech\n    --no-wake-word     Always listening, skip wake word requirement\n    --debug-audio      Print audio levels each status tick\n</code></pre>"},{"location":"modules/client-mic/#process_utterancestub-speech_buffer-speech_float_buffer-brain-safety_only-tts-has_wake_detector","title":"<code>process_utterance(stub, speech_buffer, speech_float_buffer, brain, safety_only, tts, has_wake_detector)</code>","text":"<p>Core NLP pipeline: ASR -&gt; safety check -&gt; wake phrase strip -&gt; LLM -&gt; dispatch.</p> <pre><code>def process_utterance(\n    stub,                    # ASRStub gRPC channel\n    speech_buffer: bytearray,       # Raw PCM16 bytes\n    speech_float_buffer: list,      # List of float32 numpy arrays\n    brain=None,              # SpotBrain instance (None = regex mode)\n    safety_only=False,       # True when in WAKE_WORD state\n    tts=None,                # SpotTTS instance\n    has_wake_detector=False  # True if dedicated KWS is active\n) -&gt; str | None\n</code></pre> <p>Returns <code>\"wake_detected\"</code> if wake phrase found via ASR fallback, <code>None</code> otherwise.</p>"},{"location":"modules/client-mic/#send_to_asrstub-pcm_bytes-str","title":"<code>send_to_asr(stub, pcm_bytes) -&gt; str</code>","text":"<p>Stream PCM16 audio to the Riva ASR gRPC server. Sends config first, then audio chunks. Returns the final transcript string.</p>"},{"location":"modules/client-mic/#check_safety_commandtext-dict-none","title":"<code>check_safety_command(text) -&gt; dict | None</code>","text":"<p>Regex check for stop/freeze/estop. Returns intent dict on match, None otherwise. Patterns: <code>stop|halt</code>, <code>freeze</code>, <code>emergency stop|e-stop</code>.</p>"},{"location":"modules/client-mic/#execute_on_spotintent-bool","title":"<code>execute_on_spot(intent) -&gt; bool</code>","text":"<p>Import and call <code>spot_dispatch.dispatch_intent</code>. Returns success boolean.</p>"},{"location":"modules/client-mic/#get_spot_state-dict","title":"<code>get_spot_state() -&gt; dict</code>","text":"<p>Import and call <code>spot_dispatch.get_robot_state_dict</code>. Returns state dict for LLM context (battery, posture, location, estop).</p>"},{"location":"modules/client-mic/#calibrate_noise_floorduration_sec-device-float","title":"<code>calibrate_noise_floor(duration_sec, device) -&gt; float</code>","text":"<p>Record ambient audio for <code>duration_sec</code> seconds, compute RMS. Caps high noise (&gt;0.003, likely motor noise) to 0.001. Seeds the adaptive noise tracker.</p>"},{"location":"modules/client-mic/#audio_callbackindata-frames-time_info-status","title":"<code>audio_callback(indata, frames, time_info, status)</code>","text":"<p>sounddevice stream callback. Selects mono channel from stereo input (<code>MIC_CHANNEL</code>, default 0 = left/beamformed), converts to PCM16, pushes to <code>audio_queue</code>. Discards audio when <code>_mic_muted</code> is True (during TTS).</p>"},{"location":"modules/client-mic/#configuration-constants","title":"Configuration Constants","text":"Name Default Description <code>SAMPLE_RATE</code> 16000 Audio sample rate (Hz) <code>FRAME_MS</code> 30 Frame duration (ms) <code>VAD_LEVEL</code> 1 webrtcvad aggressiveness (0-3) <code>SILENCE_TIMEOUT_SHORT</code> 0.8 Silence timeout for short utterances (&lt;2s) <code>SILENCE_TIMEOUT_LONG</code> 1.5 Silence timeout for longer utterances (&gt;2s) <code>SILENCE_CROSSOVER</code> 2.0 Speech duration threshold to switch timeouts <code>SPEECH_ONSET_FRAMES</code> 3 Consecutive VAD+energy frames to confirm onset (~90ms) <code>NAV_ONSET_FRAMES</code> 5 Higher onset threshold during navigation (~150ms) <code>PREROLL_FRAMES</code> 5 Pre-onset ring buffer size (~150ms) <code>MAX_UTTERANCE_SECONDS</code> 20 Force-send after this duration <code>ENERGY_THRESHOLD_MULTIPLIER</code> 2.0 Speech energy must exceed noise floor by this factor <code>NAV_ENERGY_MULT</code> 5.0 Extra energy multiplier during navigation <code>NOISE_EMA_ALPHA</code> 0.03 EMA smoothing for adaptive noise floor <code>NOISE_FLOOR_MIN</code> 0.001 Minimum noise floor <code>NOISE_FLOOR_MAX</code> 0.05 Maximum noise floor <code>NOISE_CALIBRATION_SECONDS</code> 2 Initial calibration recording duration <code>LISTENING_TIMEOUT</code> 300.0 Seconds idle before returning to WAKE_WORD <code>MIN_SPEECH_DURATION</code> 0.3 Reject utterances shorter than this"},{"location":"modules/client-mic/#usage-example","title":"Usage Example","text":"<pre><code># Default: LLM brain + TTS + wake word on device 24\npython src/voice_control/client_mic.py --device 24\n\n# Regex-only mode, no TTS, always listening\npython src/voice_control/client_mic.py --no-brain --no-tts --no-wake-word\n\n# Debug audio levels\npython src/voice_control/client_mic.py --debug-audio\n</code></pre>"},{"location":"modules/client-mic/#dependencies","title":"Dependencies","text":"<p>Project modules: <code>intent</code>, <code>llm_brain</code>, <code>spot_tts</code>, <code>audio_feedback</code>, <code>spot_dispatch</code>, <code>visual_nav</code>, <code>wake_word</code>, <code>asr_pb2</code>, <code>asr_pb2_grpc</code></p> <p>External packages: <code>sounddevice</code>, <code>numpy</code>, <code>webrtcvad</code>, <code>grpc</code></p>"},{"location":"modules/graph-nav/","title":"graph_nav_utils.py","text":"<p><code>src/graph_nav_utils.py</code> -- GraphNav map management.</p> <p>Handles uploading recorded maps to the robot and initializing localization. Maps consist of a graph file, waypoint snapshots, and edge snapshots stored in a directory structure.</p>"},{"location":"modules/graph-nav/#key-functions","title":"Key Functions","text":""},{"location":"modules/graph-nav/#upload_graph_and_snapshotsrobot-upload_path-bool","title":"<code>upload_graph_and_snapshots(robot, upload_path) -&gt; bool</code>","text":"<p>Upload a complete GraphNav map to the robot.</p> <pre><code>def upload_graph_and_snapshots(robot, upload_path: str) -&gt; bool\n</code></pre> <p>Args: - <code>robot</code> -- Authenticated <code>bosdyn.client.Robot</code> instance. - <code>upload_path</code> -- Directory containing <code>graph</code>, <code>waypoint_snapshots/</code>,   and <code>edge_snapshots/</code>.</p> <p>Clears existing map on the robot before uploading. Uploads graph structure first, then waypoint snapshots, then edge snapshots. Prints progress every 5 snapshots.</p>"},{"location":"modules/graph-nav/#initialize_localizationrobot-use_fiducial-bool","title":"<code>initialize_localization(robot, use_fiducial) -&gt; bool</code>","text":"<p>Initialize robot localization to the uploaded map.</p> <pre><code>def initialize_localization(robot, use_fiducial: bool = True) -&gt; bool\n</code></pre> <p>Two strategies: - Fiducial-based (<code>use_fiducial=True</code>): Robot must see a fiducial   (AprilTag) from the map. Most accurate. - Waypoint-based (<code>use_fiducial=False</code>): Robot must be physically near   the first waypoint. Uses odometry with +/-20cm / +/-20 deg search.</p> <p>Skips initialization if already localized.</p>"},{"location":"modules/graph-nav/#list_waypointsrobot-liststr","title":"<code>list_waypoints(robot) -&gt; list[str]</code>","text":"<p>Download the graph and return all waypoint IDs.</p> <pre><code>def list_waypoints(robot) -&gt; List[str]\n</code></pre>"},{"location":"modules/graph-nav/#get_graph_waypoint_orderrobot-liststr","title":"<code>get_graph_waypoint_order(robot) -&gt; list[str]</code>","text":"<p>Return all waypoint IDs in graph recording order. The graph's waypoints list preserves the order they were recorded, representing the natural traversal path.</p> <pre><code>def get_graph_waypoint_order(robot) -&gt; List[str]\n</code></pre>"},{"location":"modules/graph-nav/#get_localization_staterobot-dict-none","title":"<code>get_localization_state(robot) -&gt; dict | None</code>","text":"<p>Get current localization state.</p> <pre><code>def get_localization_state(robot) -&gt; Optional[dict]\n</code></pre> <p>Returns: <pre><code>{\"waypoint_id\": \"abc-123\", \"is_localized\": True}\n# or\nNone  # on error\n</code></pre></p>"},{"location":"modules/graph-nav/#map-directory-structure","title":"Map Directory Structure","text":"<pre><code>maps/building1/\n    graph                    # Serialized Graph protobuf\n    waypoint_snapshots/\n        snapshot_0001        # Serialized WaypointSnapshot protobufs\n        snapshot_0002\n        ...\n    edge_snapshots/\n        edge_snapshot_0001   # Serialized EdgeSnapshot protobufs\n        ...\n</code></pre> <p>Maps are recorded using BD's tablet app or the <code>graph_nav_command_line</code> example. The <code>maps/</code> directory is in <code>.gitignore</code> (maps are device-specific).</p>"},{"location":"modules/graph-nav/#usage-example","title":"Usage Example","text":"<pre><code>from src.graph_nav_utils import upload_graph_and_snapshots, initialize_localization\n\n# Upload map and localize (typically done in spot_session)\nif upload_graph_and_snapshots(robot, \"maps/building1\"):\n    initialize_localization(robot, use_fiducial=True)\n</code></pre>"},{"location":"modules/graph-nav/#dependencies","title":"Dependencies","text":"<p>Project modules: none (standalone)</p> <p>External packages: <code>bosdyn.client</code> (graph_nav.GraphNavClient, frame_helpers), <code>bosdyn.api</code> (graph_nav_pb2, map_pb2, nav_pb2)</p>"},{"location":"modules/intent/","title":"intent.py","text":"<p><code>src/voice_control/intent.py</code> -- Regex-based intent parser.</p> <p>Maps free-text voice commands to structured intent dicts using precompiled regex patterns. Used as the primary parser in <code>--no-brain</code> mode and as the safety command fast-path in LLM mode.</p>"},{"location":"modules/intent/#key-function","title":"Key Function","text":""},{"location":"modules/intent/#parse_intenttext-dict-none","title":"<code>parse_intent(text) -&gt; dict | None</code>","text":"<pre><code>def parse_intent(text: str) -&gt; dict | None\n</code></pre> <p>Cleans Whisper punctuation artifacts (removes periods), then searches 80+ precompiled patterns in priority order. Returns on first match.</p> <p>Returns: <pre><code>{\"intent\": \"walk\", \"params\": {\"direction\": \"forward\", \"distance\": 2.0}, \"raw\": \"walk forward 2 meters\"}\n# or\nNone  # no match\n</code></pre></p>"},{"location":"modules/intent/#pattern-categories","title":"Pattern Categories","text":"Category Example Intents Safety <code>stop</code>, <code>freeze</code>, <code>estop</code> Posture <code>stand</code>, <code>sit</code>, <code>selfright</code> Body height <code>body_height</code> (crouch/tall/normal) Walking <code>walk</code> (forward/backward with optional distance) Strafing <code>strafe</code> (left/right with optional distance) Turning <code>turn</code> (left/right with optional degrees, \"turn around\" = 180) Speed <code>set_speed</code> (slow/normal/fast) Visual nav <code>go_to_object</code> (find/look for/search), <code>follow_me</code> Navigation <code>go_to</code>, <code>tour</code>, <code>patrol</code>, <code>come_back</code>, <code>save_location</code> Status <code>battery_status</code>, <code>status</code>, <code>power_off</code> Door <code>open_door</code> Other <code>list_locations</code>"},{"location":"modules/intent/#pattern-features","title":"Pattern Features","text":"<ul> <li>All patterns are case-insensitive (<code>re.IGNORECASE</code>).</li> <li>Named capture groups extract parameters (e.g. <code>(?P&lt;dist&gt;\\d+(?:\\.\\d+)?)</code>).</li> <li>Template values use <code>$name</code> syntax to substitute captured groups.</li> <li>Numeric parameters are auto-converted: <code>\"2\"</code> -&gt; <code>int(2)</code>, <code>\"1.5\"</code> -&gt; <code>float(1.5)</code>.</li> <li>Location names are normalized to <code>lowercase_with_underscores</code>.</li> <li>Degree values are clamped to [0, 360].</li> <li>Patterns are ordered by specificity (distance variants before bare commands).</li> </ul>"},{"location":"modules/intent/#internal-helpers","title":"Internal Helpers","text":""},{"location":"modules/intent/#_convert_numerics-int-float-str","title":"<code>_convert_numeric(s) -&gt; int | float | str</code>","text":"<p>Convert string numbers to int or float. Returns original value if not numeric.</p>"},{"location":"modules/intent/#_resolve_paramvalue-match-any","title":"<code>_resolve_param(value, match) -&gt; Any</code>","text":"<p>Resolve a param template against a regex match. Supports <code>$name</code> group substitution, callable values, and recursive list/dict resolution.</p>"},{"location":"modules/intent/#usage-example","title":"Usage Example","text":"<pre><code>from intent import parse_intent\n\nresult = parse_intent(\"turn left 45 degrees\")\n# {\"intent\": \"turn\", \"params\": {\"deg\": 45, \"dir\": \"left\"}, \"raw\": \"turn left 45 degrees\"}\n\nresult = parse_intent(\"go to the kitchen\")\n# {\"intent\": \"go_to\", \"params\": {\"location\": \"kitchen\"}, \"raw\": \"go to the kitchen\"}\n\nresult = parse_intent(\"find the red chair\")\n# {\"intent\": \"go_to_object\", \"params\": {\"description\": \"red chair\"}, \"raw\": \"find the red chair\"}\n\nresult = parse_intent(\"hello there\")\n# None\n</code></pre>"},{"location":"modules/intent/#dependencies","title":"Dependencies","text":"<p>Project modules: none (standalone)</p> <p>External packages: <code>re</code> (stdlib)</p>"},{"location":"modules/llm-brain/","title":"llm_brain.py","text":"<p><code>src/voice_control/llm_brain.py</code> -- LLM decision engine (Ollama structured JSON).</p> <p>Takes a user transcript and robot state, sends them to a local Ollama model, and receives back a structured JSON object containing actions to execute and a spoken response. Uses <code>format=\"json\"</code> in the Ollama API to guarantee valid JSON output at the grammar level.</p>"},{"location":"modules/llm-brain/#architecture","title":"Architecture","text":"<p>The system prompt includes a full action catalog listing every command Spot can execute (stop, walk, go_to, describe, follow_me, etc.) with parameter schemas and usage rules. The model fills in a JSON object:</p> <pre><code>{\"actions\": [{\"action\": \"walk\", \"params\": {\"direction\": \"forward\", \"distance\": 2.0}}],\n \"response\": \"Walking forward two meters!\"}\n</code></pre> <p>For conversation only (no physical action), <code>actions</code> is an empty list. For chained commands (\"go to kitchen then sit\"), <code>actions</code> contains multiple entries executed sequentially.</p> <p>History management: The last <code>MAX_HISTORY</code> (12) messages are kept in <code>self.history</code> and prepended to each request. The system prompt is rebuilt each call with fresh robot state.</p> <p>Model sharing: The text LLM (<code>qwen2.5:7b</code>) and VLM (<code>qwen2.5vl:7b</code>) share VRAM on Jetson. Ollama swaps them in/out automatically. VLM warm-up is deferred to first use to avoid evicting the text LLM.</p>"},{"location":"modules/llm-brain/#key-class","title":"Key Class","text":""},{"location":"modules/llm-brain/#spotbrainmodel-ollama_url","title":"<code>SpotBrain(model, ollama_url)</code>","text":"<pre><code>class SpotBrain:\n    def __init__(self, model: str = DEFAULT_MODEL, ollama_url: str = OLLAMA_URL)\n</code></pre>"},{"location":"modules/llm-brain/#is_available-bool","title":"<code>is_available() -&gt; bool</code>","text":"<p>Check if Ollama is running and the specified model is pulled. Caches result.</p>"},{"location":"modules/llm-brain/#warm_up","title":"<code>warm_up()</code>","text":"<p>Send a trivial prompt with the full system prompt to pre-load the model into VRAM and cache the KV state. Makes the first real command fast (~0.2s prompt eval instead of ~1.5s cold). Called at startup.</p>"},{"location":"modules/llm-brain/#warm_up_vlm","title":"<code>warm_up_vlm()</code>","text":"<p>Pre-load the VLM by sending a tiny 1x1 JPEG. Called on-demand before the first <code>query_vlm()</code> call, not at startup (would evict the text LLM).</p>"},{"location":"modules/llm-brain/#processtranscript-state-dict","title":"<code>process(transcript, state) -&gt; dict</code>","text":"<p>Main inference method.</p> <pre><code>def process(self, transcript: str, state: dict | None = None) -&gt; dict\n</code></pre> <p>Args: - <code>transcript</code> -- User's speech (ASR output). - <code>state</code> -- Current robot state dict (battery, location, etc.).</p> <p>Returns: <pre><code>{\n    \"actions\": [{\"intent\": str, \"params\": dict}, ...],\n    \"response\": str,   # What the robot says back\n    \"raw_llm\": str     # Raw LLM output for debugging\n}\n</code></pre></p> <p>Ollama options: <code>temperature=0.3</code>, <code>top_p=0.9</code>, <code>num_predict=150</code>, <code>num_ctx=4096</code>.</p>"},{"location":"modules/llm-brain/#query_vlmimage_bytes-question-yolo_hint-str","title":"<code>query_vlm(image_bytes, question, yolo_hint) -&gt; str</code>","text":"<p>Send JPEG image + question to the VLM for visual description.</p> <pre><code>def query_vlm(self, image_bytes: bytes, question: str, yolo_hint: str = \"\") -&gt; str\n</code></pre> <p>The VLM prompt instructs the model to speak in first person (\"what I see\") and never reference \"image\", \"photo\", or \"angle\". An optional <code>yolo_hint</code> from YOLO-World detection results guides the VLM's attention.</p>"},{"location":"modules/llm-brain/#clear_history","title":"<code>clear_history()</code>","text":"<p>Clear conversation history.</p>"},{"location":"modules/llm-brain/#module-level-convenience","title":"Module-Level Convenience","text":"<pre><code>def get_brain(model: str = DEFAULT_MODEL) -&gt; SpotBrain   # Singleton\ndef process_with_brain(transcript, state, model) -&gt; dict  # One-shot\n</code></pre>"},{"location":"modules/llm-brain/#configuration-constants","title":"Configuration Constants","text":"Name Default Description <code>DEFAULT_MODEL</code> <code>\"qwen2.5:7b\"</code> Text LLM model name <code>VLM_MODEL</code> <code>\"qwen2.5vl:7b\"</code> Vision-language model name <code>OLLAMA_URL</code> <code>\"http://localhost:11434\"</code> Ollama API base URL <code>MAX_HISTORY</code> 12 Max conversation messages kept (6 exchanges) <code>REQUEST_TIMEOUT</code> 30.0 Seconds per normal request <code>FIRST_REQUEST_TIMEOUT</code> 120.0 Seconds for first request (model loading) <code>VLM_TIMEOUT</code> 60.0 Seconds for VLM inference"},{"location":"modules/llm-brain/#usage-example","title":"Usage Example","text":"<pre><code>from llm_brain import SpotBrain\n\nbrain = SpotBrain(model=\"qwen2.5:7b\")\nif brain.is_available():\n    brain.warm_up()\n\n    state = {\"battery_percent\": 85, \"is_standing\": True,\n             \"saved_locations\": \"kitchen, lab\"}\n\n    result = brain.process(\"go to the kitchen and sit down\", state)\n    # result[\"actions\"] = [\n    #     {\"intent\": \"go_to\", \"params\": {\"location\": \"kitchen\"}},\n    #     {\"intent\": \"sit\", \"params\": {}}\n    # ]\n    # result[\"response\"] = \"Heading to the kitchen, then I'll sit down!\"\n</code></pre> <pre><code># Interactive CLI test\npython src/voice_control/llm_brain.py\npython src/voice_control/llm_brain.py qwen2.5:14b\n</code></pre>"},{"location":"modules/llm-brain/#dependencies","title":"Dependencies","text":"<p>Project modules: none (standalone)</p> <p>External packages: <code>requests</code>, <code>json</code>, <code>base64</code></p> <p>External services: Ollama (<code>sudo systemctl start ollama</code>)</p>"},{"location":"modules/location-manager/","title":"location_manager.py","text":"<p><code>src/location_manager.py</code> -- Waypoint name storage (JSON-backed).</p> <p>Persists human-readable names for GraphNav waypoint IDs. When the user says \"save location kitchen\", the current waypoint ID is stored under the key <code>\"kitchen\"</code>. Later, \"go to kitchen\" resolves to that waypoint ID.</p>"},{"location":"modules/location-manager/#key-functions","title":"Key Functions","text":""},{"location":"modules/location-manager/#save_locationname-waypoint_id-bool","title":"<code>save_location(name, waypoint_id) -&gt; bool</code>","text":"<p>Save a named location mapping.</p> <pre><code>def save_location(name: str, waypoint_id: str) -&gt; bool\n</code></pre> <p>Names are normalized to lowercase. Creates the file and parent directory if they do not exist. Overwrites if the name already exists.</p>"},{"location":"modules/location-manager/#load_locationname-str-none","title":"<code>load_location(name) -&gt; str | None</code>","text":"<p>Look up a waypoint ID by name.</p> <pre><code>def load_location(name: str) -&gt; Optional[str]\n</code></pre> <p>Returns the waypoint ID string, or None if not found. Lookup is case-insensitive (stored lowercase).</p>"},{"location":"modules/location-manager/#load_all_locations-dict","title":"<code>load_all_locations() -&gt; dict</code>","text":"<p>Load the full name-to-waypoint mapping.</p> <pre><code>def load_all_locations() -&gt; Dict[str, str]\n</code></pre> <p>Returns an empty dict if the file does not exist or is malformed.</p>"},{"location":"modules/location-manager/#list_locations-dict","title":"<code>list_locations() -&gt; dict</code>","text":"<p>Alias for <code>load_all_locations()</code>. Returns all saved location mappings.</p>"},{"location":"modules/location-manager/#storage","title":"Storage","text":"<p>File: <code>locations.json</code> in the project root.</p> <pre><code>{\n  \"kitchen\": \"quiet-toad-B5X.hMjvdIhVc7t6FuAFNQ==\",\n  \"lab\": \"dizzy-cat-K2R.pLmQwNjXc9u8GuBGOA==\",\n  \"hallway\": \"brave-owl-M3T.xYnRzPkZd0v9HvCHPA==\"\n}\n</code></pre> <p>Names are normalized to lowercase. Spaces in names passed from the LLM or regex parser are converted to underscores by the caller (<code>llm_brain.py</code> normalizes to <code>lowercase_with_underscores</code>, <code>intent.py</code> does the same).</p>"},{"location":"modules/location-manager/#usage-example","title":"Usage Example","text":"<pre><code>from src.location_manager import save_location, load_location, list_locations\n\n# Save current position\nsave_location(\"kitchen\", \"quiet-toad-B5X.hMjvdIhVc7t6FuAFNQ==\")\n\n# Look up later\nwp_id = load_location(\"kitchen\")\nif wp_id:\n    graph_nav_client.navigate_to(wp_id, ...)\n\n# List all saved locations\nall_locs = list_locations()\n# {\"kitchen\": \"quiet-toad-...\", \"lab\": \"dizzy-cat-...\"}\n</code></pre>"},{"location":"modules/location-manager/#dependencies","title":"Dependencies","text":"<p>Project modules: none (standalone)</p> <p>External packages: <code>json</code> (stdlib), <code>pathlib</code> (stdlib)</p>"},{"location":"modules/session/","title":"session.py","text":"<p><code>src/session.py</code> -- Spot robot session context manager.</p> <p>Wraps the full lifecycle of a Spot connection: authentication, time sync, lease acquisition, power-on, stand, and cleanup on exit. Used by <code>spot_dispatch.py</code> to maintain a persistent robot session.</p>"},{"location":"modules/session/#key-function","title":"Key Function","text":""},{"location":"modules/session/#spot_session","title":"<code>spot_session(...)</code>","text":"<p>Context manager that yields a session dict with all BD SDK clients.</p> <pre><code>@contextmanager\ndef spot_session(\n    hostname: str = BOSDYN_ROBOT_IP,\n    username: str = BOSDYN_CLIENT_USERNAME,\n    password: str = BOSDYN_CLIENT_PASSWORD,\n    stand_on_enter: bool = True,\n    sit_on_exit: bool = True,\n    upload_map: bool = False,\n    map_path: str = None,\n    auto_localize: bool = False\n)\n</code></pre> <p>Yields: <pre><code>{\n    \"robot\": robot,              # bosdyn.client.Robot\n    \"lease_client\": lease_client,\n    \"cmd\": cmd_client,           # RobotCommandClient\n    \"power\": power_client,       # PowerClient\n    \"state\": state_client,       # RobotStateClient\n}\n</code></pre></p>"},{"location":"modules/session/#lifecycle","title":"Lifecycle","text":""},{"location":"modules/session/#on-enter","title":"On enter","text":"<ol> <li>Create SDK and robot instance.</li> <li>Authenticate with username/password.</li> <li>Wait for time sync.</li> <li>Clear stale keepalive policies (prevents \"KeepaliveMotorsOff\" errors from    previous crashed sessions).</li> <li>Force-take lease (overrides stale claims).</li> <li>Start lease keepalive.</li> <li>If <code>stand_on_enter</code>:</li> <li>Power on motors (with retry on keepalive/estop errors).</li> <li>Stand via <code>blocking_stand()</code>.</li> <li>If <code>upload_map</code> and <code>map_path</code>:</li> <li>Upload GraphNav map.</li> <li>Optionally auto-localize via fiducial.</li> </ol>"},{"location":"modules/session/#on-exit","title":"On exit","text":"<ol> <li>If <code>sit_on_exit</code>: <code>blocking_sit()</code>.</li> <li>Power off motors (graceful, not cut immediately).</li> <li>Shutdown lease keepalive (returns lease).</li> </ol>"},{"location":"modules/session/#configuration","title":"Configuration","text":"<p>Credentials are loaded from <code>src/config.py</code> which reads environment variables:</p> Variable Description <code>BOSDYN_ROBOT_IP</code> Robot hostname or IP <code>BOSDYN_CLIENT_USERNAME</code> Authentication username <code>BOSDYN_CLIENT_PASSWORD</code> Authentication password"},{"location":"modules/session/#usage-example","title":"Usage Example","text":"<pre><code>from src.session import spot_session\n\n# Full lifecycle (stand on enter, sit + power off on exit)\nwith spot_session() as session:\n    cmd = session[\"cmd\"]\n    state = session[\"state\"]\n    robot_state = state.get_robot_state()\n    print(f\"Battery: {robot_state.power_state.locomotion_charge_percentage.value}%\")\n\n# Keep robot standing after context exits (for voice control)\nwith spot_session(sit_on_exit=False) as session:\n    pass  # Robot stays standing\n\n# With map upload and auto-localization\nwith spot_session(upload_map=True, map_path=\"maps/building1\",\n                  auto_localize=True) as session:\n    pass\n</code></pre>"},{"location":"modules/session/#dependencies","title":"Dependencies","text":"<p>Project modules: <code>src.config</code> (credentials), <code>src.graph_nav_utils</code> (optional, for map upload)</p> <p>External packages: <code>bosdyn.client</code> (create_standard_sdk, LeaseClient, RobotStateClient, PowerClient, TimeSyncClient, RobotCommandClient, KeepaliveClient)</p>"},{"location":"modules/spot-dispatch/","title":"spot_dispatch.py","text":"<p><code>src/voice_control/spot_dispatch.py</code> -- Command execution and navigation.</p> <p>Central dispatcher that translates parsed intents into Boston Dynamics SDK calls. Manages a persistent Spot session (lazy singleton), background navigation threads, and camera capture.</p>"},{"location":"modules/spot-dispatch/#key-functions","title":"Key Functions","text":""},{"location":"modules/spot-dispatch/#dispatch_intentintent-bool","title":"<code>dispatch_intent(intent) -&gt; bool</code>","text":"<p>Main executor for all commands. Receives an intent dict from the LLM brain or regex parser and calls the appropriate BD SDK methods.</p> <pre><code>def dispatch_intent(intent: dict) -&gt; bool\n</code></pre> <p>Args: - <code>intent</code> -- Dict with <code>\"intent\"</code> (str) and <code>\"params\"</code> (dict).</p> <p>Returns: True if command executed successfully.</p> <p>Supported intents: <code>stop</code>, <code>freeze</code>, <code>estop</code>, <code>stand</code>, <code>sit</code>, <code>selfright</code>, <code>walk</code>, <code>strafe</code>, <code>turn</code>, <code>body_height</code>, <code>set_speed</code>, <code>go_to</code>, <code>tour</code>, <code>patrol</code>, <code>come_back</code>, <code>save_location</code>, <code>list_locations</code>, <code>open_door</code>, <code>go_to_object</code>, <code>follow_me</code>, <code>describe</code>, <code>battery_status</code>, <code>status</code>, <code>power_off</code>.</p>"},{"location":"modules/spot-dispatch/#ensure_spot_session-dict","title":"<code>ensure_spot_session() -&gt; dict</code>","text":"<p>Lazy singleton pattern. On first call, opens a <code>spot_session()</code> context manager with <code>stand_on_enter=True</code>, <code>sit_on_exit=False</code> (keeps robot standing for voice commands). Subsequent calls return the cached session.</p>"},{"location":"modules/spot-dispatch/#close_spot_session","title":"<code>close_spot_session()</code>","text":"<p>Graceful shutdown: sit, power off, exit context manager. Called from <code>client_mic.py</code> on Ctrl+C.</p>"},{"location":"modules/spot-dispatch/#get_robot_state_dict-dict","title":"<code>get_robot_state_dict() -&gt; dict</code>","text":"<p>Collect current robot state for LLM context. Returns:</p> <pre><code>{\n    \"battery_percent\": int,\n    \"estimated_runtime_minutes\": int,\n    \"is_powered\": bool,\n    \"is_standing\": str,\n    \"current_location\": str,      # Friendly name or waypoint ID\n    \"saved_locations\": str,       # Comma-separated names\n    \"estop_status\": str,          # \"not_cut\", \"cut\", \"soft_stop\"\n}\n</code></pre>"},{"location":"modules/spot-dispatch/#capture_framecamera-bytes","title":"<code>capture_frame(camera) -&gt; bytes</code>","text":"<p>Capture a JPEG frame from any of Spot's fisheye cameras.</p> <pre><code>def capture_frame(camera: str = \"front\") -&gt; bytes\n</code></pre>"},{"location":"modules/spot-dispatch/#is_navigating-bool","title":"<code>is_navigating() -&gt; bool</code>","text":"<p>Returns True if a navigation thread is alive (motor noise expected).</p>"},{"location":"modules/spot-dispatch/#is_follow_mode-bool","title":"<code>is_follow_mode() -&gt; bool</code>","text":"<p>Returns True if the active navigation mode is \"follow\".</p>"},{"location":"modules/spot-dispatch/#navigation-architecture","title":"Navigation Architecture","text":"<p>Navigation commands (<code>go_to</code>, <code>tour</code>, <code>patrol</code>, <code>come_back</code>, <code>go_to_object</code>, <code>follow_me</code>) run in background threads so the main voice loop stays responsive to safety commands.</p> <ul> <li><code>_nav_thread</code> -- The active navigation Thread.</li> <li><code>_nav_stop_event</code> -- Threading Event; set to cancel navigation.</li> <li><code>_nav_mode</code> -- One of <code>\"graphnav\"</code>, <code>\"visual_nav\"</code>, <code>\"follow\"</code>, or None.</li> <li><code>_home_waypoint</code> -- Saved departure waypoint for <code>come_back</code>.</li> </ul> <p>New navigation commands cancel any in-progress navigation via <code>_cancel_nav()</code> before starting.</p>"},{"location":"modules/spot-dispatch/#graphnav-navigation","title":"GraphNav Navigation","text":"<p><code>go_to</code>, <code>tour</code>, and <code>patrol</code> use BD's GraphNav API. The sequence handler <code>_navigate_waypoint_sequence()</code> supports:</p> <ul> <li>Multi-waypoint sequences with per-waypoint retry on STATUS_STUCK</li> <li>Continuous patrol looping (<code>repeat=True</code>)</li> <li>Re-localization on STATUS_LOST</li> <li>Skipping unreachable waypoints in multi-waypoint tours</li> </ul>"},{"location":"modules/spot-dispatch/#visual-navigation","title":"Visual Navigation","text":"<p><code>go_to_object</code> delegates to <code>visual_nav.navigate_to_object()</code> (YOLO-World). <code>follow_me</code> delegates to <code>visual_nav.follow_person()</code> (YOLOv8n).</p>"},{"location":"modules/spot-dispatch/#camera-sources","title":"Camera Sources","text":"<pre><code>CAMERA_SOURCES = {\n    \"front\":       \"frontleft_fisheye_image\",\n    \"front_left\":  \"frontleft_fisheye_image\",\n    \"front_right\": \"frontright_fisheye_image\",\n    \"left\":        \"left_fisheye_image\",\n    \"right\":       \"right_fisheye_image\",\n    \"back\":        \"back_fisheye_image\",\n}\n</code></pre>"},{"location":"modules/spot-dispatch/#speed-multipliers","title":"Speed Multipliers","text":"Speed Multiplier slow 0.5 normal 1.0 fast 1.5"},{"location":"modules/spot-dispatch/#usage-example","title":"Usage Example","text":"<pre><code>from spot_dispatch import dispatch_intent, get_robot_state_dict, close_spot_session\n\n# Execute a command (session auto-initializes on first call)\ndispatch_intent({\"intent\": \"walk\", \"params\": {\"direction\": \"forward\", \"distance\": 2.0}})\n\n# Get robot state for LLM context\nstate = get_robot_state_dict()\n\n# Graceful shutdown\nclose_spot_session()\n</code></pre>"},{"location":"modules/spot-dispatch/#dependencies","title":"Dependencies","text":"<p>Project modules: <code>src.session</code> (<code>spot_session</code>), <code>src.location_manager</code> (<code>list_locations</code>), <code>visual_nav</code> (<code>navigate_to_object</code>, <code>follow_person</code>)</p> <p>External packages: <code>bosdyn.client</code> (robot_command, frame_helpers, math_helpers, image, graph_nav)</p>"},{"location":"modules/spot-tts/","title":"spot_tts.py","text":"<p><code>src/voice_control/spot_tts.py</code> -- Text-to-speech via sherpa-onnx Kokoro.</p> <p>Non-blocking TTS that runs on CPU. Includes mic mute/unmute callbacks to prevent TTS audio from feeding back into the ASR pipeline.</p>"},{"location":"modules/spot-tts/#key-class","title":"Key Class","text":""},{"location":"modules/spot-tts/#spotttsspeaker_id-speed-output_device-on_mute-on_unmute","title":"<code>SpotTTS(speaker_id, speed, output_device, on_mute, on_unmute)</code>","text":"<pre><code>class SpotTTS:\n    def __init__(\n        self,\n        speaker_id: int = 3,          # af_sarah\n        speed: float = 1.1,\n        output_device=None,            # sounddevice device index\n        on_mute=None,                  # Callback before playback\n        on_unmute=None                 # Callback after playback\n    )\n</code></pre>"},{"location":"modules/spot-tts/#speaktext","title":"<code>speak(text)</code>","text":"<p>Non-blocking. Spawns a daemon thread for synthesis and playback. Cancels any in-progress speech before starting new synthesis.</p>"},{"location":"modules/spot-tts/#speak_synctext","title":"<code>speak_sync(text)</code>","text":"<p>Blocking. Generates and plays audio in the calling thread. Returns after playback completes.</p>"},{"location":"modules/spot-tts/#wait","title":"<code>wait()</code>","text":"<p>Block until current background speech finishes. No-op if nothing is playing.</p>"},{"location":"modules/spot-tts/#is_speaking-bool","title":"<code>is_speaking() -&gt; bool</code>","text":"<p>Returns True if audio is currently playing.</p>"},{"location":"modules/spot-tts/#is_available-bool","title":"<code>is_available() -&gt; bool</code>","text":"<p>Returns True if the model loaded successfully and sounddevice is available.</p>"},{"location":"modules/spot-tts/#module-level-convenience","title":"Module-Level Convenience","text":"<pre><code>def get_tts(speaker_id=3, output_device=None) -&gt; SpotTTS  # Singleton\n</code></pre>"},{"location":"modules/spot-tts/#speaker-configuration","title":"Speaker Configuration","text":"<p>Model: Kokoro v0.19 (kokoro-en-v0_19), 24 kHz sample rate.</p> ID Name Description 0 af American female 1 af_bella American female (Bella) 2 af_nicole American female (Nicole) 3 af_sarah American female (Sarah) -- default 4 af_sky American female (Sky) 5 am_adam American male (Adam) 6 am_michael American male (Michael) 7 bf_emma British female (Emma) 8 bf_isabella British female (Isabella) 9 bm_george British male (George) 10 bm_lewis British male (Lewis)"},{"location":"modules/spot-tts/#mute-callbacks","title":"Mute Callbacks","text":"<p>When <code>on_mute</code> and <code>on_unmute</code> are provided (typically <code>_mute_mic</code> and <code>_unmute_mic</code> from <code>client_mic.py</code>), the mic is paused during TTS playback. This prevents TTS audio captured by the mic from triggering VAD or corrupting ASR transcripts.</p> <p>Flow: <code>on_mute()</code> -&gt; synthesize -&gt; play -&gt; wait -&gt; <code>on_unmute()</code>.</p>"},{"location":"modules/spot-tts/#configuration-constants","title":"Configuration Constants","text":"Name Default Description <code>DEFAULT_SPEAKER_ID</code> 3 af_sarah speaker <code>DEFAULT_SPEED</code> 1.1 Slightly faster than natural <code>MODEL_DIR</code> <code>models/tts/kokoro-en-v0_19</code> Model directory path"},{"location":"modules/spot-tts/#usage-example","title":"Usage Example","text":"<pre><code>from spot_tts import SpotTTS\n\ntts = SpotTTS(speaker_id=3, speed=1.1)\nif tts.is_available():\n    tts.speak(\"Hello! I am Spot.\")   # non-blocking\n    tts.wait()                        # wait for completion\n    tts.speak_sync(\"Goodbye!\")        # blocking\n</code></pre> <pre><code># CLI test\npython src/voice_control/spot_tts.py \"Hello from Spot\"\npython src/voice_control/spot_tts.py --sid 5 --speed 0.9 \"Slow Adam voice\"\npython src/voice_control/spot_tts.py --wav output.wav \"Save to file\"\n</code></pre>"},{"location":"modules/spot-tts/#dependencies","title":"Dependencies","text":"<p>Project modules: none (standalone)</p> <p>External packages: <code>sherpa_onnx</code>, <code>sounddevice</code>, <code>numpy</code></p> <p>Setup: <code>pip install sherpa-onnx &amp;&amp; python scripts/setup_kokoro.py</code></p>"},{"location":"modules/visual-nav/","title":"visual_nav.py","text":"<p><code>src/voice_control/visual_nav.py</code> -- YOLO detection and visual navigation.</p> <p>Provides camera-based object seeking (YOLO-World open-vocabulary) and person following (YOLOv8n). Both models run on CPU to avoid GPU/VRAM competition with the LLM and VLM.</p>"},{"location":"modules/visual-nav/#architecture","title":"Architecture","text":""},{"location":"modules/visual-nav/#object-approach-navigate_to_object","title":"Object approach (<code>navigate_to_object</code>)","text":"<ol> <li>Scan phase: Check all cameras (front, front_right, left, right, back)    without moving. If found on a side/back camera, turn to face it.</li> <li>Approach phase: Repeatedly capture from front camera, detect object,    compute steering correction, step forward. Stop when bbox height exceeds    <code>APPROACH_DONE_FRAC</code> of frame height.</li> </ol>"},{"location":"modules/visual-nav/#person-following-follow_person","title":"Person following (<code>follow_person</code>)","text":"<p>Two-thread architecture for smooth, responsive tracking:</p> <ul> <li>Detector thread (~2-3 Hz): Captures from all cameras, runs YOLOv8n,   stores latest detection (bbox, camera, depth, timestamp) in shared state.   Per-camera person locking prevents target switching when multiple people   are visible.</li> <li>Control thread (10 Hz): Reads latest detection, computes forward   velocity and yaw rate, sends velocity commands. EMA smoothing prevents   jerky motion.</li> </ul> <p>Depth from stereo cameras is used when available for precise distance control. Falls back to bbox size heuristic when depth is unavailable.</p>"},{"location":"modules/visual-nav/#fisheye-handling","title":"Fisheye handling","text":"<p>Spot's fisheye cameras are mounted sideways. Images are rotated 90 degrees CW for YOLO detection. Depth coordinate un-rotation: <code>orig_x = rot_y</code>, <code>orig_y = H_orig - 1 - rot_x</code> (uses <code>raw_rows</code> not <code>raw_cols</code>).</p>"},{"location":"modules/visual-nav/#key-functions","title":"Key Functions","text":""},{"location":"modules/visual-nav/#preload_models","title":"<code>preload_models()</code>","text":"<p>Background-load YOLO-World and YOLOv8n models in a daemon thread. Called during startup to avoid first-use latency. Safe to call multiple times.</p>"},{"location":"modules/visual-nav/#detect_in_imageimage_bytes-description-dict","title":"<code>detect_in_image(image_bytes, description) -&gt; dict</code>","text":"<p>Run YOLO-World on raw JPEG bytes.</p> <pre><code>def detect_in_image(image_bytes: bytes, description: str) -&gt; dict\n</code></pre> <p>Returns: <pre><code>{\"found\": True, \"confidence\": 0.72, \"position\": \"in the center\"}\n# or\n{\"found\": False}\n</code></pre></p> <p>Position is one of: <code>\"on the left side\"</code>, <code>\"in the center\"</code>, <code>\"on the right side\"</code>.</p>"},{"location":"modules/visual-nav/#navigate_to_objectdescription-session-stop_event-bool","title":"<code>navigate_to_object(description, session, stop_event) -&gt; bool</code>","text":"<p>Visual servo to approach a described object.</p> <pre><code>def navigate_to_object(\n    description: str,                    # e.g. \"red chair\"\n    session: dict,                       # Spot session\n    stop_event: threading.Event          # Set to cancel\n) -&gt; bool\n</code></pre>"},{"location":"modules/visual-nav/#follow_personsession-stop_event-bool","title":"<code>follow_person(session, stop_event) -&gt; bool</code>","text":"<p>Follow the nearest person using continuous velocity control.</p> <pre><code>def follow_person(\n    session: dict,\n    stop_event: threading.Event\n) -&gt; bool\n</code></pre> <p>Returns True if cleanly stopped, False if person lost.</p>"},{"location":"modules/visual-nav/#configuration-constants","title":"Configuration Constants","text":"Name Default Description <code>CONF_THRESH</code> 0.25 YOLO confidence threshold <code>DEVICE</code> <code>\"cpu\"</code> Inference device <code>APPROACH_STEP</code> 0.5 Meters forward per approach loop <code>APPROACH_DONE_FRAC</code> 0.50 Bbox height fraction to consider \"arrived\" <code>APPROACH_YAW_GAIN</code> 1.0 Steering proportional gain <code>APPROACH_MAX_YAW</code> 0.35 Max yaw correction per step (rad, ~20 deg) <code>FOLLOW_DIST_MARGIN</code> 1.5 Desired following distance (meters) <code>FOLLOW_MAX_VEL</code> 0.8 Max forward velocity (m/s) <code>FOLLOW_FAR_VEL</code> 0.4 Forward velocity when using bbox only (no depth) <code>FOLLOW_CLOSE_BBOX</code> 0.55 Bbox height fraction = \"close enough\" <code>FOLLOW_CONTROL_HZ</code> 10 Velocity command rate (Hz) <code>FOLLOW_VEL_SMOOTH</code> 0.6 Velocity EMA smoothing (0=instant, 1=frozen) <code>FOLLOW_HOLD_EXIT</code> 80 Consecutive hold cycles before exiting (~8s) <code>FOLLOW_LOCK_DIST</code> 0.3 Max normalized distance to match locked person <code>MOVE_SETTLE</code> 1.5 Seconds to let a step complete <code>LOST_TIMEOUT</code> 10.0 Seconds without detection before giving up"},{"location":"modules/visual-nav/#usage-example","title":"Usage Example","text":"<pre><code>from visual_nav import detect_in_image, navigate_to_object, preload_models\nimport threading\n\npreload_models()\n\n# Check if object is visible\nresult = detect_in_image(jpeg_bytes, \"blue backpack\")\nif result[\"found\"]:\n    print(f\"Found at {result['position']}, conf={result['confidence']}\")\n\n# Approach the object (runs in calling thread until done or cancelled)\nstop = threading.Event()\nsuccess = navigate_to_object(\"blue backpack\", spot_session, stop)\n</code></pre>"},{"location":"modules/visual-nav/#dependencies","title":"Dependencies","text":"<p>Project modules: none (receives session dict from caller)</p> <p>External packages: <code>ultralytics</code> (YOLO, YOLOWorld), <code>PIL</code> (Pillow), <code>numpy</code>, <code>bosdyn.client</code> (robot_command, image), <code>bosdyn.api</code> (image_pb2)</p>"},{"location":"modules/wake-word/","title":"wake_word.py","text":"<p><code>src/voice_control/wake_word.py</code> -- Wake word detection (\"Hey Spot\").</p> <p>Streaming keyword spotter using sherpa-onnx's zipformer model. Runs on CPU with ~0ms detection latency. Shares the sherpa-onnx dependency with TTS (no additional packages needed).</p>"},{"location":"modules/wake-word/#key-class","title":"Key Class","text":""},{"location":"modules/wake-word/#wakeworddetectormodel_dir","title":"<code>WakeWordDetector(model_dir)</code>","text":"<pre><code>class WakeWordDetector:\n    def __init__(self, model_dir=None)  # Default: models/kws/\n</code></pre>"},{"location":"modules/wake-word/#process_framepcm16_bytes-bool","title":"<code>process_frame(pcm16_bytes) -&gt; bool</code>","text":"<p>Feed a 16 kHz PCM16 audio frame (any size, typically 30ms / 960 bytes). Returns True when \"Hey Spot\" is detected. Automatically resets the internal stream after detection.</p> <pre><code>def process_frame(self, pcm16_bytes: bytes) -&gt; bool\n</code></pre>"},{"location":"modules/wake-word/#reset","title":"<code>reset()</code>","text":"<p>Reset the detector stream for next detection. Called automatically after a positive detection, but can also be called manually (e.g. when returning to WAKE_WORD state after listening timeout).</p>"},{"location":"modules/wake-word/#is_available-bool","title":"<code>is_available() -&gt; bool</code>","text":"<p>Returns True if the model loaded successfully.</p>"},{"location":"modules/wake-word/#model","title":"Model","text":"<p>sherpa-onnx-kws-zipformer-gigaspeech-3.3M (int8 quantized, ~5 MB total).</p> <p>Files in <code>models/kws/</code>: - <code>encoder-epoch-12-avg-2-chunk-16-left-64.int8.onnx</code> - <code>decoder-epoch-12-avg-2-chunk-16-left-64.int8.onnx</code> - <code>joiner-epoch-12-avg-2-chunk-16-left-64.int8.onnx</code> - <code>tokens.txt</code> - <code>keywords.txt</code> (contains \"h ey s p aa t\" phoneme sequence)</p> <p>Download: <code>python scripts/setup_kws.py</code></p>"},{"location":"modules/wake-word/#configuration-constants","title":"Configuration Constants","text":"Name Default Description <code>KEYWORDS_SCORE</code> 1.5 Boost keyword score (higher = easier to trigger) <code>KEYWORDS_THRESHOLD</code> 0.25 Detection threshold (higher = harder to trigger) <code>NUM_TRAILING_BLANKS</code> 1 Blank frames after keyword before firing"},{"location":"modules/wake-word/#integration-with-client_micpy","title":"Integration with client_mic.py","text":"<p>The wake word detector runs on every audio frame in the <code>WAKE_WORD</code> state. When detected:</p> <ol> <li><code>beep.wake_detected()</code> plays a rising chime.</li> <li>State transitions to <code>LISTENING</code>.</li> <li>Audio is NOT drained -- remaining speech in the buffer (e.g. \"stand up\"    from \"Hey Spot stand up\") is preserved for VAD to pick up.</li> <li><code>client_mic.py</code> strips the wake phrase from the ASR transcript before    sending to the LLM.</li> </ol> <p>If the dedicated detector is unavailable, <code>client_mic.py</code> falls back to ASR-based wake phrase detection (regex on the transcript).</p>"},{"location":"modules/wake-word/#usage-example","title":"Usage Example","text":"<pre><code>from wake_word import WakeWordDetector\n\ndetector = WakeWordDetector()\nif detector.is_available():\n    # Feed audio frames from mic\n    if detector.process_frame(pcm16_bytes):\n        print(\"Hey Spot detected!\")\n        # detector auto-resets after detection\n</code></pre> <pre><code># Standalone mic test\npython src/voice_control/wake_word.py --test-mic --device 24\n\n# Quick load + silence test\npython src/voice_control/wake_word.py\n</code></pre>"},{"location":"modules/wake-word/#dependencies","title":"Dependencies","text":"<p>Project modules: none (standalone)</p> <p>External packages: <code>sherpa_onnx</code>, <code>numpy</code></p> <p>Setup: <code>python scripts/setup_kws.py</code></p>"},{"location":"project/dartmouth-setup/","title":"Dartmouth Setup","text":"<p>Campus-specific configuration for the Spot robot at Dartmouth College.</p>"},{"location":"project/dartmouth-setup/#hardware","title":"Hardware","text":"<ul> <li>Robot: Boston Dynamics Spot (firmware 5.0.1.1)</li> <li>Compute: NVIDIA Jetson AGX Orin 64GB (JetPack 6.2.1)</li> <li>Microphone: ReSpeaker XVF3800 USB 4-Mic Array</li> <li>Username on Jetson: <code>spotdog</code></li> </ul>"},{"location":"project/dartmouth-setup/#network","title":"Network","text":"<p>Spot and the Jetson must be on the same network.</p> <ul> <li>Spot IP: <code>192.168.80.3</code> (configured in <code>.env</code>)</li> <li>Robot credentials: Set in <code>.env</code> as <code>BOSDYN_CLIENT_USERNAME</code> and <code>BOSDYN_CLIENT_PASSWORD</code></li> </ul> <p>To verify connectivity:</p> <pre><code>ping 192.168.80.3\n</code></pre>"},{"location":"project/dartmouth-setup/#connecting-to-the-jetson","title":"Connecting to the Jetson","text":""},{"location":"project/dartmouth-setup/#via-tailscale-remote-recommended","title":"Via Tailscale (Remote \u2014 Recommended)","text":"<p>See the Remote Access (Tailscale) guide for full setup. Once configured:</p> <pre><code>ssh spotdog@&lt;tailscale-ip&gt;\n</code></pre> <p>This works from anywhere \u2014 on campus, at home, or on the go.</p>"},{"location":"project/dartmouth-setup/#via-local-ssh-on-spots-network","title":"Via Local SSH (On Spot's Network)","text":"<p>If you're connected to Spot's WiFi, the Jetson is reachable at its local IP:</p> <pre><code>ssh spotdog@&lt;jetson-local-ip&gt;\n</code></pre>"},{"location":"project/dartmouth-setup/#via-serial-console-usb-last-resort","title":"Via Serial Console (USB \u2014 Last Resort)","text":"<p>For initial setup or when the network is down. Connect a USB-C cable from the Jetson to your laptop.</p> <p>On macOS:</p> <pre><code>ls /dev/cu.usbmodem*\nsudo screen /dev/cu.usbmodem&lt;DEVICE_NUMBER&gt; 115200\n</code></pre> <p>On Linux:</p> <pre><code>ls /dev/ttyACM*\nsudo screen /dev/ttyACM0 115200\n</code></pre> <p>Exit: <code>Ctrl+A</code>, then <code>K</code>, then <code>Y</code></p>"},{"location":"project/dartmouth-setup/#map-locations","title":"Map Locations","text":"<p>The lab map is stored in <code>maps/lab_map3/</code>. Named waypoints are in <code>locations.json</code>.</p> <p>To add or rename locations:</p> <pre><code>python scripts/map_waypoints.py --list    # See all waypoints\npython scripts/map_waypoints.py           # Interactive naming\n</code></pre> <p>Or via voice: \"Save this location as reception desk\"</p>"},{"location":"project/dartmouth-setup/#audio-device","title":"Audio Device","text":"<p>The ReSpeaker XVF3800 typically appears as device index 24. Verify with:</p> <pre><code>python -c \"import sounddevice; print(sounddevice.query_devices())\"\n</code></pre> <p>If the index differs, pass it explicitly:</p> <pre><code>python scripts/run_voice_control.py --device &lt;INDEX&gt;\n</code></pre>"},{"location":"project/dartmouth-setup/#project-repository","title":"Project Repository","text":"<pre><code>https://github.com/matteo-corrado/dartmouth_spot_capstone\n</code></pre> <ul> <li>Main branch: <code>main</code></li> <li>Feature branch: <code>feature/llm-brain</code> (voice control + LLM)</li> <li>Push requires manual authentication (HTTPS, no credential helper on Jetson)</li> </ul>"},{"location":"project/status/","title":"Feature Status","text":"<p>Current implementation status of all features.</p>"},{"location":"project/status/#complete","title":"Complete","text":"Feature Description Key Files Voice Pipeline Mic \u2192 VAD \u2192 ASR \u2192 LLM \u2192 Dispatch <code>client_mic.py</code>, <code>server.py</code> LLM Brain Structured JSON command parsing via Ollama <code>llm_brain.py</code> Wake Word \"Hey Spot\" detection via sherpa-onnx KWS <code>wake_word.py</code> Text-to-Speech Spoken responses via Kokoro TTS <code>spot_tts.py</code> Audio Feedback Beep/chime tones for status <code>audio_feedback.py</code> Movement Walk, strafe, turn with parameters <code>spot_dispatch.py</code> Posture Stand, sit, crouch, self-right, body height <code>spot_dispatch.py</code> Navigation Go-to, tour, patrol, come-back via GraphNav <code>spot_dispatch.py</code>, <code>graph_nav_utils.py</code> Location Management Save/load named waypoints <code>location_manager.py</code> Safety Stop, freeze, e-stop (instant, bypasses LLM) <code>client_mic.py</code> Status Queries Battery, robot status, power off <code>spot_dispatch.py</code> Visual Detection YOLO object detection on camera feeds <code>visual_nav.py</code> Object Navigation \"Go to the red chair\" via YOLO + approach <code>visual_nav.py</code> VLM Descriptions \"What do you see?\" via qwen2.5vl <code>llm_brain.py</code> Web Panel Browser-based E-Stop + control <code>web_panel.py</code> Command Chaining \"Go to kitchen then sit down\" <code>client_mic.py</code>, <code>llm_brain.py</code> One-Breath Commands \"Hey Spot stand up\" in one phrase <code>client_mic.py</code> Regex Fallback 80+ patterns for --no-brain mode <code>intent.py</code>"},{"location":"project/status/#in-progress","title":"In Progress","text":"Feature Description Status Next Step Follow Mode Person following YOLO unreliable on fisheye Switch to AprilTag fiducials via WorldObjectClient Door Opening Push-bar doors via AutoPush Implemented, untested Test with <code>--vlm --depth 0.5</code>"},{"location":"project/status/#planned","title":"Planned","text":"Feature Description Notes AprilTag Follow Follow person wearing AprilTag Zero Jetson CPU cost, 3D position from Spot Stair Handling Follow mode stair ascent/descent Spot descends backwards, needs special logic"},{"location":"project/status/#not-planned","title":"Not Planned","text":"Feature Reason PTZ Camera Requires additional payload hardware Cloud ASR All inference is on-device by design"},{"location":"project/status/#latency-optimizations-applied","title":"Latency Optimizations (Applied)","text":"Optimization Change Impact System prompt compression Cut 10 obvious examples, keep 5 ~25% fewer prompt tokens per LLM call History reduction MAX_HISTORY 20 \u2192 12 ~500-800 fewer tokens after 6+ exchanges YOLO pre-loading Background model load at startup Eliminates 2-3s first-use delay Timing instrumentation [Timing] prints for ASR, LLM, total Enables data-driven optimization GraphNav route recovery RouteGenParams backtrack on stuck Better stuck recovery during navigation"},{"location":"scripts/reference/","title":"Scripts Reference","text":"<p>All runnable scripts in the <code>scripts/</code> directory. Scripts assume the Python virtual environment is activated (<code>source spot-env/bin/activate</code>) and the working directory is the project root.</p>"},{"location":"scripts/reference/#run_voice_controlpy","title":"run_voice_control.py","text":"<p>Main entry point for the voice control system. Auto-starts Riva, Ollama, and the ASR bridge server, then launches the microphone client.</p> <pre><code>python scripts/run_voice_control.py\npython scripts/run_voice_control.py --no-tts\npython scripts/run_voice_control.py --skip-services --no-wake-word\npython scripts/run_voice_control.py --server-only\npython scripts/run_voice_control.py --device 24 --debug-audio\n</code></pre> Flag Description <code>--skip-services</code> Do not auto-start Riva or Ollama (assume already running) <code>--no-brain</code> Disable LLM brain; use regex-only intent parsing (legacy mode) <code>--no-tts</code> Disable text-to-speech output <code>--no-wake-word</code> Always listening (skip \"Hey Spot\" requirement) <code>--server-only</code> Start only the ASR bridge server; do not start the mic client <code>--device INDEX</code> Audio input device index (default: 24 = XVF3800 beamforming mic) <code>--debug-audio</code> Print frame-level audio energy, noise floor, and threshold <p>Prerequisite: E-Stop must be running in a separate terminal.</p>"},{"location":"scripts/reference/#estop_runpy","title":"estop_run.py","text":"<p>E-Stop keepalive. Must run in a separate terminal for the entire session. Claims the E-Stop endpoint and sends heartbeats every 0.5s. If this script exits, the robot's motors are cut within 3 seconds.</p> <pre><code>python scripts/estop_run.py\n</code></pre> <p>No flags. Press Ctrl+C to stop (triggers motor cutoff).</p> <p>Alternative: Use <code>web_panel.py</code> for phone-based E-Stop control.</p>"},{"location":"scripts/reference/#setup_kwspy","title":"setup_kws.py","text":"<p>Download the sherpa-onnx keyword spotting model for \"Hey Spot\" wake word detection. Downloads ~5MB of int8-quantized ONNX files from HuggingFace and creates the <code>keywords.txt</code> file.</p> <pre><code>python scripts/setup_kws.py          # download and setup\npython scripts/setup_kws.py --check  # verify existing install\n</code></pre> Flag Description <code>--check</code> Verify all required files exist without downloading <p>Model: <code>sherpa-onnx-kws-zipformer-gigaspeech-3.3M-2024-01-01</code> Target: <code>models/kws/</code></p>"},{"location":"scripts/reference/#setup_kokoropy","title":"setup_kokoro.py","text":"<p>Download the sherpa-onnx Kokoro TTS model pack (~340MB). Uses streaming download + extraction to avoid doubling disk usage.</p> <pre><code>python scripts/setup_kokoro.py\n</code></pre> <p>No flags. The script checks if the model already exists and skips if so. Requires <code>wget</code> to be installed.</p> <p>Model: <code>kokoro-en-v0_19</code> (English, 11 speaker voices) Target: <code>models/tts/kokoro-en-v0_19/</code></p>"},{"location":"scripts/reference/#setup_mappy","title":"setup_map.py","text":"<p>Upload a GraphNav map to Spot and initialize localization. The robot must be standing and able to see a fiducial marker from the map for localization.</p> <pre><code>python scripts/setup_map.py\npython scripts/setup_map.py --map-path maps/my_custom_map\npython scripts/setup_map.py --no-localize\npython scripts/setup_map.py --waypoint-init\n</code></pre> Flag Description <code>--map-path PATH</code> Path to map directory (default: <code>maps/lab_map2</code>) <code>--no-localize</code> Upload map but skip localization initialization <code>--waypoint-init</code> Use waypoint-based localization instead of fiducial-based <p>Prerequisite: E-Stop must be running.</p>"},{"location":"scripts/reference/#setup_rivash","title":"setup_riva.sh","text":"<p>Manage the NVIDIA Riva ASR Docker container. Handles first-time setup, model initialization, and server lifecycle.</p> <pre><code>./scripts/setup_riva.sh              # full setup (first time)\n./scripts/setup_riva.sh init         # download + TensorRT optimize models\n./scripts/setup_riva.sh start        # start Riva server container\n./scripts/setup_riva.sh stop         # stop Riva server container\n./scripts/setup_riva.sh restart      # restart Riva server\n./scripts/setup_riva.sh test         # test ASR connection\n</code></pre> Subcommand Description <code>setup</code> Install nvidia-riva-client + download Riva quickstart (default) <code>init</code> Download and optimize models with TensorRT (15-30 min first run) <code>start</code> Start the <code>riva-speech</code> Docker container <code>stop</code> Stop the <code>riva-speech</code> Docker container <code>restart</code> Stop then start the container <code>test</code> Send 1 second of silence to Riva and verify response"},{"location":"scripts/reference/#download_map_from_spotpy","title":"download_map_from_spot.py","text":"<p>Fetch the current GraphNav map from Spot's onboard storage and save it locally. Also extracts named waypoints into <code>locations.json</code>.</p> <pre><code>python scripts/download_map_from_spot.py\npython scripts/download_map_from_spot.py --output maps/my_new_map\n</code></pre> Flag Description <code>--output DIR</code> Output directory (default: <code>maps/downloaded_map</code>) <p>Downloads the graph, waypoint snapshots, and edge snapshots. Does not require E-Stop or standing.</p>"},{"location":"scripts/reference/#map_waypointspy","title":"map_waypoints.py","text":"<p>Interactive tool for listing GraphNav waypoints and mapping them to human-readable location names. Saves mappings to <code>locations.json</code>.</p> <pre><code>python scripts/map_waypoints.py\n</code></pre> <p>No flags. The script connects to Spot (no standing required), lists all waypoints in the uploaded map, and provides an interactive prompt:</p> <pre><code>&gt; 3 kitchen          # map waypoint #3 to \"kitchen\"\n&gt; current lab        # save robot's current position as \"lab\"\n&gt; list               # show all existing mappings\n&gt; q                  # quit\n</code></pre>"},{"location":"scripts/reference/#calibrate_doorpy","title":"calibrate_door.py","text":"<p>Calibrate the push bar handle position for door opening. Three modes:</p> <pre><code># Step 1: Capture images from Spot's cameras\npython scripts/calibrate_door.py capture\n\n# Step 2: Set handle coordinates from inspected images\npython scripts/calibrate_door.py set \\\n    --handle-x 400 --handle-y 250 \\\n    --hinge left \\\n    --camera left\n\n# All-in-one GUI mode (requires OpenCV display)\npython scripts/calibrate_door.py interactive\n</code></pre> <p>capture mode flags: None. Saves images to <code>door_calibration/</code>.</p> <p>set mode flags:</p> Flag Description <code>--handle-x X</code> Handle X pixel coordinate (required) <code>--handle-y Y</code> Handle Y pixel coordinate (required) <code>--hinge left\\|right</code> Which side the door hinge is on (required) <code>--camera left\\|right</code> Camera: left=frontleft, right=frontright (default: left) <code>--from-sbs</code> Coordinates are from the side-by-side image (auto-adjusts) <p>interactive mode flags: None. Requires OpenCV with display support.</p> <p>Saves calibration to <code>door_config.json</code>.</p>"},{"location":"scripts/reference/#test_door_standalonepy","title":"test_door_standalone.py","text":"<p>Test push-bar door opening using the BD DoorService <code>AutoPushCommand</code>. The robot must be standing ~1m from the door, facing it.</p> <pre><code>python scripts/test_door_standalone.py --vlm\npython scripts/test_door_standalone.py --vlm --depth 0.5\npython scripts/test_door_standalone.py --vlm --hinge right --camera left\npython scripts/test_door_standalone.py --vision-xyz 1.65,2.68,0.81\n</code></pre> Flag Description <code>--vlm</code> Use VLM to auto-detect push bar pixel location <code>--vision-xyz X,Y,Z</code> Direct 3D vision-frame coordinates (skip VLM) <code>--hinge left\\|right</code> Door hinge side (default: left) <code>--depth METERS</code> Estimated depth to door for VLM mode (default: 0.5) <code>--camera left\\|right</code> Which front camera for VLM mode (default: left) <p>Must specify either <code>--vlm</code> or <code>--vision-xyz</code>.</p> <p>Prerequisites: E-Stop running, Ollama running (for <code>--vlm</code>).</p>"},{"location":"scripts/reference/#test_visual_navpy","title":"test_visual_nav.py","text":"<p>Test YOLO detection on Spot's camera feed. Runs YOLO-World (open-vocabulary) and YOLOv8n (person detection), prints bounding boxes, confidence scores, and steering math.</p> <pre><code>python scripts/test_visual_nav.py                        # detect \"person\"\npython scripts/test_visual_nav.py --target \"red chair\"   # detect specific object\npython scripts/test_visual_nav.py --save                  # save annotated image\npython scripts/test_visual_nav.py --no-spot               # use synthetic test image\n</code></pre> Flag Description <code>--target TEXT</code> Object to search for with YOLO-World (default: \"person\") <code>--save</code> Save annotated image to <code>/tmp/visual_nav_test.jpg</code> <code>--no-spot</code> Use a synthetic gray test image instead of Spot's camera"},{"location":"scripts/reference/#web_panelpy","title":"web_panel.py","text":"<p>Mobile-friendly web control panel for E-Stop management and voice pipeline start/stop. No external dependencies (stdlib HTTP server). Accessible from any device on the same network or via Tailscale.</p> <pre><code>python scripts/web_panel.py\npython scripts/web_panel.py --port 9090\npython scripts/web_panel.py --no-spot\n</code></pre> Flag Description <code>--port PORT</code> HTTP port (default: 8080) <code>--no-spot</code> UI-only mode: E-Stop buttons disabled, no Spot connection <p>The web panel provides: - E-Stop controls: Claim, E-Stop (cut motors), Release - Voice pipeline controls: Start, Stop - Status indicators: Riva, Ollama, E-Stop, Voice pipeline (polled every 2s)</p>"}]}